---
phase: 09-performance-hardening
plan: 01
title: Benchmark harness foundation
status: pending
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-performance-hardening/run-performance-benchmarks.sh
  - .planning/phases/09-performance-hardening/evidence/README.md
  - docs/release.md
autonomous: true
requirements: ["PHASE-09"]
requirements_addressed: ["D-01", "D-02", "D-03", "D-04", "D-05", "D-06", "D-07", "D-08"]
must_haves:
  truths:
    - "Phase 9 has a checked-in benchmark runner that can be invoked from a clean checkout."
    - "The runner writes privacy-safe JSON and Markdown evidence with observed values, targets, and pass/fail status."
    - "The runner does not depend on private shell content, screenshots, telemetry, or nanosecond support from BSD date."
  artifacts:
    - path: ".planning/phases/09-performance-hardening/run-performance-benchmarks.sh"
      provides: "Phase 9 benchmark runner"
      contains: "PHASE9_PERFORMANCE_REPORT"
    - path: ".planning/phases/09-performance-hardening/evidence/README.md"
      provides: "Initial evidence report placeholder"
      contains: "Phase 9 performance hardening"
  key_links:
    - from: ".planning/phases/09-performance-hardening/run-performance-benchmarks.sh"
      to: ".planning/phases/09-performance-hardening/09-CONTEXT.md"
      via: "Runner encodes context targets and privacy constraints"
      pattern: "cold_start_ms"
---

<objective>
Create the benchmark harness foundation for Phase 9.

Purpose: before optimizing, Phase 9 needs a repeatable runner that can launch the built app, wait for markers, sample process metrics, and write a report format later tasks can fill.
Output: benchmark runner script, initial evidence directory/report, and release documentation for invoking the suite.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-performance-hardening/09-CONTEXT.md
@.planning/phases/09-performance-hardening/09-RESEARCH.md
@.planning/phases/09-performance-hardening/09-VALIDATION.md
@docs/release.md
@project.yml
</context>

<tasks>

<task id="09-01-01" type="auto">
  <title>Add benchmark runner and report schema</title>
  <name>Add benchmark runner and report schema</name>
  <files>.planning/phases/09-performance-hardening/run-performance-benchmarks.sh, .planning/phases/09-performance-hardening/evidence/README.md</files>
  <read_first>.planning/phases/09-performance-hardening/09-CONTEXT.md, .planning/phases/09-performance-hardening/09-RESEARCH.md, docs/release.md</read_first>
  <action>Create executable shell script `.planning/phases/09-performance-hardening/run-performance-benchmarks.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. It must define exact marker/report constants `PHASE9_PERFORMANCE_REPORT`, `PHASE9_READY`, `PHASE9_INPUT_LATENCY`, `PHASE9_HEAVY_OUTPUT_DONE`, `phase9-results.json`, and `evidence/README.md`. Use `/usr/bin/perl -MTime::HiRes=time -e 'printf "%.6f\n", time'` for sub-second timing instead of `date +%s%N`. Add helper functions named exactly `now_seconds`, `resolve_app_binary`, `wait_for_file`, `sample_process`, `write_json_report`, and `write_markdown_report`. The initial JSON schema must include top-level keys `generated_at`, `configuration`, `targets`, `results`, and `misses`; `targets` must include `cold_start_ms`, `rss_mb`, `idle_cpu_percent`, `input_latency_ms`, and `frame_pacing`. Implement a `--quick` flag that runs a syntax/lightweight path and writes placeholder `pending` results until later plans fill scenarios. Create `.planning/phases/09-performance-hardening/evidence/README.md` with heading `# Phase 9 performance hardening` and sections `Phase 9 final gate`, `Benchmark invocation`, `Targets`, `Results`, `Misses and mitigations`, and `Known limitations`. The placeholder report must state that live measurements are pending until Plans 09-02 and 09-03.</action>
  <acceptance_criteria>
    - `.planning/phases/09-performance-hardening/run-performance-benchmarks.sh` exists
    - `.planning/phases/09-performance-hardening/run-performance-benchmarks.sh` contains `PHASE9_PERFORMANCE_REPORT`
    - `.planning/phases/09-performance-hardening/run-performance-benchmarks.sh` contains `now_seconds`
    - `.planning/phases/09-performance-hardening/run-performance-benchmarks.sh` contains `/usr/bin/perl -MTime::HiRes=time`
    - `.planning/phases/09-performance-hardening/run-performance-benchmarks.sh` contains `cold_start_ms`
    - `.planning/phases/09-performance-hardening/run-performance-benchmarks.sh` contains `idle_cpu_percent`
    - `.planning/phases/09-performance-hardening/run-performance-benchmarks.sh` contains `rss_mb`
    - `.planning/phases/09-performance-hardening/evidence/README.md` contains `# Phase 9 performance hardening`
    - `.planning/phases/09-performance-hardening/evidence/README.md` contains `Misses and mitigations`
    - `bash -n .planning/phases/09-performance-hardening/run-performance-benchmarks.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n .planning/phases/09-performance-hardening/run-performance-benchmarks.sh && rg 'PHASE9_PERFORMANCE_REPORT|now_seconds|cold_start_ms|idle_cpu_percent|rss_mb|Misses and mitigations' .planning/phases/09-performance-hardening/run-performance-benchmarks.sh .planning/phases/09-performance-hardening/evidence/README.md</automated>
  </verify>
  <done>Phase 9 has a checked-in benchmark runner and report schema.</done>
</task>

<task id="09-01-02" type="auto">
  <title>Document benchmark invocation in release docs</title>
  <name>Document benchmark invocation in release docs</name>
  <files>docs/release.md</files>
  <read_first>docs/release.md, .planning/phases/09-performance-hardening/run-performance-benchmarks.sh</read_first>
  <action>Append a new `## Phase 9 performance hardening` section to `docs/release.md`. Include the exact command `.planning/phases/09-performance-hardening/run-performance-benchmarks.sh --quick`, the output paths `.planning/phases/09-performance-hardening/evidence/phase9-results.json` and `.planning/phases/09-performance-hardening/evidence/README.md`, and a note that full benchmark mode may attempt `xcrun xctrace` while `--quick` keeps CI/local smoke lightweight. Include privacy copy: `Phase 9 benchmark evidence uses synthetic terminal markers and must not commit private shell history, terminal transcripts, environment variables, API keys, or screenshots containing user content.`</action>
  <acceptance_criteria>
    - `docs/release.md` contains `## Phase 9 performance hardening`
    - `docs/release.md` contains `.planning/phases/09-performance-hardening/run-performance-benchmarks.sh --quick`
    - `docs/release.md` contains `phase9-results.json`
    - `docs/release.md` contains `xcrun xctrace`
    - `docs/release.md` contains `must not commit private shell history`
    - `git diff --check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg 'Phase 9 performance hardening|run-performance-benchmarks.sh --quick|phase9-results.json|xcrun xctrace|must not commit private shell history' docs/release.md && git diff --check</automated>
  </verify>
  <done>Release docs explain how to run Phase 9 benchmark smoke and where evidence lands.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
bash -n .planning/phases/09-performance-hardening/run-performance-benchmarks.sh
rg 'PHASE9_PERFORMANCE_REPORT|cold_start_ms|idle_cpu_percent|rss_mb|Phase 9 performance hardening|phase9-results.json' .planning/phases/09-performance-hardening docs/release.md
git diff --check
```
</verification>
