---
phase: 05-aesthetic-modes
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - Sources/GridOSApp/RootView.swift
  - Sources/GridOSApp/GridOSApp.swift
  - Sources/GridOSApp/SettingsView.swift
autonomous: true
requirements: ["PHASE-05"]
requirements_addressed: ["D-06", "D-07", "D-08", "D-09", "D-11", "D-16", "D-18"]
must_haves:
  truths:
    - "RootView composes its VisualIdentity from persisted selected mode and install seed."
    - "First launch creates a stable local install seed if the preference is empty."
    - "Command-Shift-M cycles modes through a native menu-visible command without sending input to the shell."
    - "Settings exposes a small visual mode picker using existing AppStorage patterns."
    - "Relaunch restores the selected mode from local preferences."
  artifacts:
    - path: "Sources/GridOSApp/RootView.swift"
      provides: "App composition of selected mode, install seed, and VisualIdentity"
      contains: "appearance.installSeed"
    - path: "Sources/GridOSApp/GridOSApp.swift"
      provides: "Native Appearance menu and Command-Shift-M switcher"
      contains: "keyboardShortcut(\"m\", modifiers: [.command, .shift])"
    - path: "Sources/GridOSApp/SettingsView.swift"
      provides: "Settings picker for exact Phase 5 modes"
      contains: "Picker(\"Visual mode\""
  key_links:
    - from: "Sources/GridOSKit/GridOSAppPreferences.swift"
      to: "Sources/GridOSApp/RootView.swift"
      via: "AppStorage keys for selected mode and install seed"
      pattern: "visualModeStorageKey"
    - from: "Sources/GridOSApp/GridOSApp.swift"
      to: "Sources/GridOSApp/RootView.swift"
      via: "Shared AppStorage selected mode raw value"
      pattern: "appearance.visualMode"
    - from: "Sources/GridOSApp/SettingsView.swift"
      to: "Sources/GridOSApp/GridOSApp.swift"
      via: "Both mutate the same selected mode preference"
      pattern: "defaultVisualModeRawValue"
---

<objective>
Wire Phase 5 mode selection into the app shell through local persistence, first-launch install seed creation, a native Command-Shift-M switcher, and a compact Settings picker.

Purpose: The mode system must be usable and discoverable without disturbing terminal focus or adding a broad theme editor.
Output: AppStorage-backed mode/seed composition, native Appearance command menu, and persisted Settings control.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-aesthetic-modes/05-CONTEXT.md
@.planning/phases/05-aesthetic-modes/05-RESEARCH.md
@.planning/phases/05-aesthetic-modes/05-VALIDATION.md
@.planning/phases/03-production-app-frame/03-01-SUMMARY.md
@docs/architecture.md
@Sources/GridOSApp/RootView.swift
@Sources/GridOSApp/GridOSApp.swift
@Sources/GridOSApp/SettingsView.swift
@Sources/GridOSKit/GridOSAppPreferences.swift
@Sources/RenderCore/VisualIdentity.swift

<interfaces>
Created by Plan 05-01:

```swift
public enum VisualMode: String, CaseIterable, Equatable, Sendable, Identifiable {
    case tron
    case severance
    case appleNative
    public static let defaultMode: VisualMode = .tron
    public var next: VisualMode { get }
}

public struct VisualIdentity: Equatable, Sendable {
    public init(mode: VisualMode = .defaultMode, installSeed: String)
}

public struct GridOSAppPreferences {
    public static let visualModeStorageKey = "appearance.visualMode"
    public static let installSeedStorageKey = "appearance.installSeed"
    public static let defaultVisualModeRawValue = "tron"
    public static let defaultInstallSeedRawValue = ""
    public static func normalizedVisualModeRawValue(_ rawValue: String) -> String
    public static func nextVisualModeRawValue(after rawValue: String) -> String
}
```
</interfaces>
</context>

<tasks>

<task id="05-02-01" type="auto">
  <name>Compose persisted mode and stable install seed in RootView</name>
  <files>Sources/GridOSApp/RootView.swift</files>
  <read_first>Sources/GridOSApp/RootView.swift, Sources/GridOSKit/GridOSAppPreferences.swift, Sources/RenderCore/VisualIdentity.swift, Sources/RenderCore/ProceduralSeed.swift, .planning/phases/05-aesthetic-modes/05-CONTEXT.md, .planning/phases/05-aesthetic-modes/05-VALIDATION.md</read_first>
  <action>Replace `private let visualIdentity = VisualIdentity.default` with AppStorage-driven composition. Add `@AppStorage(GridOSAppPreferences.visualModeStorageKey) private var visualModeRawValue = GridOSAppPreferences.defaultVisualModeRawValue` and `@AppStorage(GridOSAppPreferences.installSeedStorageKey) private var installSeedRawValue = GridOSAppPreferences.defaultInstallSeedRawValue`. Add computed `private var visualMode: VisualMode` that uses `GridOSAppPreferences.normalizedVisualModeRawValue(visualModeRawValue)` and `VisualMode(rawValue:) ?? .defaultMode`. Add computed `private var installSeed: String` that uses `GridOSAppPreferences.normalizedInstallSeedRawValue(installSeedRawValue)`. Add computed `private var visualIdentity: VisualIdentity` returning `VisualIdentity(mode: visualMode, installSeed: installSeed.isEmpty ? "gridOS.phase5.bootstrap" : installSeed)`. Add `@MainActor private func ensureInstallSeed()` that writes `UUID().uuidString.lowercased()` to `installSeedRawValue` only when the normalized stored seed is empty. Call `ensureInstallSeed()` before `await runMetricsLoop()` inside the existing root `.task`. Keep `TerminalWorkspaceView(configuration:onActivity:)` unchanged in layout and do not alter SwiftTerm rendering, terminal input, terminal focus, or terminal frame sizing.</action>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg "visualModeStorageKey|installSeedStorageKey|ensureInstallSeed|UUID\\(\\)\\.uuidString\\.lowercased\\(\\)|VisualIdentity\\(mode: visualMode, installSeed:" Sources/GridOSApp/RootView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `Sources/GridOSApp/RootView.swift` contains `@AppStorage(GridOSAppPreferences.visualModeStorageKey)`
    - `Sources/GridOSApp/RootView.swift` contains `@AppStorage(GridOSAppPreferences.installSeedStorageKey)`
    - `Sources/GridOSApp/RootView.swift` contains `private var visualMode: VisualMode`
    - `Sources/GridOSApp/RootView.swift` contains `private var installSeed: String`
    - `Sources/GridOSApp/RootView.swift` contains `ensureInstallSeed()`
    - `Sources/GridOSApp/RootView.swift` contains `UUID().uuidString.lowercased()`
    - `Sources/GridOSApp/RootView.swift` contains `VisualIdentity(mode: visualMode, installSeed:`
    - `Sources/GridOSApp/RootView.swift` still contains `TerminalWorkspaceView(`
    - `rg "GPU terminal text|custom terminal renderer|Cyberpunk|Matrix|marketplace" Sources/GridOSApp/RootView.swift` returns no matches
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>RootView restores the selected mode locally and guarantees a stable per-install seed after first launch.</done>
</task>

<task id="05-02-02" type="auto">
  <name>Add native mode command and Settings picker</name>
  <files>Sources/GridOSApp/GridOSApp.swift, Sources/GridOSApp/SettingsView.swift</files>
  <read_first>Sources/GridOSApp/GridOSApp.swift, Sources/GridOSApp/SettingsView.swift, Sources/GridOSKit/GridOSAppPreferences.swift, Sources/RenderCore/VisualIdentity.swift, .planning/phases/05-aesthetic-modes/05-CONTEXT.md, .planning/phases/05-aesthetic-modes/05-VALIDATION.md</read_first>
  <action>In `GridOSApp.swift`, import `GridOSKit` and keep `TerminalCommands` unchanged. Add a separate private `AppearanceCommands: Commands` with `@AppStorage(GridOSAppPreferences.visualModeStorageKey) private var visualModeRawValue = GridOSAppPreferences.defaultVisualModeRawValue`. Add `CommandMenu("Appearance")` containing `Button("Cycle Visual Mode") { visualModeRawValue = GridOSAppPreferences.nextVisualModeRawValue(after: visualModeRawValue) }` followed by `.keyboardShortcut("m", modifiers: [.command, .shift])`. Register both `TerminalCommands()` and `AppearanceCommands()` in `.commands`. Do not call `TerminalCommandCenter`, resign first responder, or send any shell bytes from the appearance command. In `SettingsView.swift`, import `RenderCore`, add `@AppStorage(GridOSAppPreferences.visualModeStorageKey) private var visualModeRawValue = GridOSAppPreferences.defaultVisualModeRawValue`, and add a small `Picker("Visual mode", selection: $visualModeRawValue)` in the existing `Appearance` section. The picker must list `VisualMode.allCases` with labels `mode.displayName` and tags `mode.rawValue`; it must include exactly `Tron`, `Severance`, and `Apple-native`. Update Reset to Defaults so it resets `visualModeRawValue` to `"tron"` while preserving the install seed. Do not add Cyberpunk, Matrix, sound themes, plugin/user theme controls, full light mode, marketplace/import controls, or custom terminal text options.</action>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg "Cycle Visual Mode|keyboardShortcut\\(\"m\", modifiers: \\[\\.command, \\.shift\\]\\)|CommandMenu\\(\"Appearance\"\\)|Picker\\(\"Visual mode\"|VisualMode\\.allCases" Sources/GridOSApp</automated>
  </verify>
  <acceptance_criteria>
    - `Sources/GridOSApp/GridOSApp.swift` contains `CommandMenu("Appearance")`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Button("Cycle Visual Mode")`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.keyboardShortcut("m", modifiers: [.command, .shift])`
    - `Sources/GridOSApp/GridOSApp.swift` contains `GridOSAppPreferences.nextVisualModeRawValue`
    - `Sources/GridOSApp/GridOSApp.swift` still contains `TerminalCommands()`
    - `Sources/GridOSApp/GridOSApp.swift` contains no `TerminalCommandCenter` reference inside `AppearanceCommands`
    - `Sources/GridOSApp/SettingsView.swift` contains `Picker("Visual mode"`
    - `Sources/GridOSApp/SettingsView.swift` contains `VisualMode.allCases`
    - `Sources/GridOSApp/SettingsView.swift` contains `.tag(mode.rawValue)`
    - `Sources/GridOSApp/SettingsView.swift` contains `visualModeRawValue = GridOSAppPreferences.defaultVisualModeRawValue`
    - `rg "Cyberpunk|Matrix|sound theme|plugin theme|marketplace|import theme|GPU terminal text" Sources/GridOSApp/GridOSApp.swift Sources/GridOSApp/SettingsView.swift` returns no matches
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>Mode switching is discoverable in native menus, uses exact Command-Shift-M, and Settings exposes only the three Phase 5 modes.</done>
</task>

</tasks>

<verification>
Run:

```bash
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
rg "appearance.visualMode|appearance.installSeed|@AppStorage" Sources/GridOSApp Sources/GridOSKit Tests/GridOSKitTests
rg "keyboardShortcut\\(\"m\", modifiers: \\[\\.command, \\.shift\\]\\)|Cycle Visual Mode" Sources/GridOSApp
git diff --check
```
</verification>

<success_criteria>
- The selected visual mode persists through `appearance.visualMode`.
- The install seed persists through `appearance.installSeed` and is generated once in the app layer.
- Command-Shift-M cycles `tron -> severance -> appleNative -> tron`.
- Settings picker shows exactly `Tron`, `Severance`, and `Apple-native`.
- Terminal focus and SwiftTerm rendering remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/05-aesthetic-modes/05-02-SUMMARY.md`.
</output>
