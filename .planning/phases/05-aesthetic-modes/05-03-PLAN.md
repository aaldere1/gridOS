---
phase: 05-aesthetic-modes
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - Sources/GridOSApp/RootView.swift
  - Sources/RenderCore/MetalBackgroundView.swift
  - Tests/RenderCoreTests/RenderCoreModelTests.swift
autonomous: true
requirements: ["PHASE-05"]
requirements_addressed: ["D-02", "D-03", "D-04", "D-05", "D-10", "D-11", "D-12", "D-13", "D-14", "D-15", "D-17", "D-18"]
must_haves:
  truths:
    - "The app frame looks visibly different across Tron, Severance, and Apple-native modes."
    - "Mode tokens affect header indicator, accents, separators, terminal chrome, metrics strip, and activity panel."
    - "Terminal text remains the foreground surface and usable terminal space is not reduced."
    - "Real metrics remain truthful and text-forward in every mode."
    - "Metal background behavior and motion profile differ by mode, while reduced motion suppresses animation."
  artifacts:
    - path: "Sources/GridOSApp/RootView.swift"
      provides: "SwiftUI app-frame token consumption"
      contains: "visualTheme"
    - path: "Sources/RenderCore/MetalBackgroundView.swift"
      provides: "Mode-aware Metal background and per-mode motion profile integration"
      contains: "uniforms.mode"
    - path: "Tests/RenderCoreTests/RenderCoreModelTests.swift"
      provides: "Shader compile and motion-profile regression coverage"
      contains: "testMetalShaderSourceCompilesWhenDeviceIsAvailable"
  key_links:
    - from: "Sources/RenderCore/VisualTheme.swift"
      to: "Sources/GridOSApp/RootView.swift"
      via: "VisualTheme tokens style app-frame surfaces"
      pattern: "visualTheme"
    - from: "Sources/RenderCore/VisualTheme.swift"
      to: "Sources/RenderCore/MetalBackgroundView.swift"
      via: "Motion and shader profile values drive renderer behavior"
      pattern: "motion"
    - from: "Sources/RenderCore/VisualEffectConfiguration.swift"
      to: "Sources/RenderCore/MetalBackgroundView.swift"
      via: "Reduced motion still suppresses event animation"
      pattern: "pulseMagnitude"
---

<objective>
Apply the Phase 5 token contract to the app frame and Metal renderer so the three modes are coherent visual systems rather than color swaps.

Purpose: The visual differences must be visible in screenshots while preserving terminal readability, metrics truthfulness, and reduced-motion behavior.
Output: Token-driven SwiftUI chrome, mode-aware Metal shader branches, per-mode pulse behavior, and regression checks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-aesthetic-modes/05-CONTEXT.md
@.planning/phases/05-aesthetic-modes/05-RESEARCH.md
@.planning/phases/05-aesthetic-modes/05-VALIDATION.md
@.planning/phases/04-real-system-metrics/04-01-SUMMARY.md
@.planning/phases/04-real-system-metrics/04-VERIFICATION.md
@docs/architecture.md
@Sources/GridOSApp/RootView.swift
@Sources/RenderCore/VisualIdentity.swift
@Sources/RenderCore/VisualTheme.swift
@Sources/RenderCore/VisualEffectConfiguration.swift
@Sources/RenderCore/MetalBackgroundView.swift
@Tests/RenderCoreTests/RenderCoreModelTests.swift
</context>

<tasks>

<task id="05-03-01" type="auto">
  <name>Apply visual tokens to app frame without reducing terminal space</name>
  <files>Sources/GridOSApp/RootView.swift</files>
  <read_first>Sources/GridOSApp/RootView.swift, Sources/RenderCore/VisualTheme.swift, Sources/RenderCore/VisualIdentity.swift, .planning/phases/05-aesthetic-modes/05-CONTEXT.md, .planning/phases/05-aesthetic-modes/05-VALIDATION.md, .planning/phases/04-real-system-metrics/04-VERIFICATION.md</read_first>
  <action>Add a private computed `visualTheme = visualMode.theme` in `RootView`. Add a private SwiftUI bridge extension in `RootView.swift`, such as `private extension Color { init(_ visualColor: VisualColor) { self.init(red: visualColor.red, green: visualColor.green, blue: visualColor.blue, opacity: visualColor.opacity) } }`. Thread `theme: visualTheme` into `AppFrameHeader`, `SystemStripView`, `ActivityContextPanel`, and `TerminalWorkspaceView`. Replace hard-coded `.cyan`, `.black.opacity`, and `.white.opacity` decorative chrome with token-derived values while keeping metrics and terminal text readable: labels and values must keep at least `0.56` text opacity, primary metric values at least `0.84`, and top process names at least `0.78`. Keep `ActivityContextPanel` width exactly `204`, keep `TerminalWorkspaceView` `.frame(maxWidth: .infinity, maxHeight: .infinity)`, and keep the existing root padding values `18` horizontal/top/bottom. Mode-specific app-frame effects must include header indicator color, shell/mode accent copy, system strip leading mark, separator opacity, activity panel divider, terminal border opacity, and terminal corner radius. Do not change Phase 4 metric labels `CPU`, `MEM`, `NET`, `BAT`, `THERM`; do not add fake metrics; do not add custom GPU terminal text rendering; do not shrink or overlay terminal text.</action>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg "visualTheme|Color\\(_ visualColor: VisualColor\\)|AppFrameHeader\\(|SystemStripView\\(snapshot: metricsSnapshot, theme: visualTheme\\)|ActivityContextPanel\\(snapshot: metricsSnapshot, theme: visualTheme\\)|TerminalWorkspaceView\\(" Sources/GridOSApp/RootView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `Sources/GridOSApp/RootView.swift` contains `private var visualTheme`
    - `Sources/GridOSApp/RootView.swift` contains `Color(_ visualColor: VisualColor)`
    - `Sources/GridOSApp/RootView.swift` contains `AppFrameHeader(`
    - `Sources/GridOSApp/RootView.swift` contains `theme: visualTheme`
    - `Sources/GridOSApp/RootView.swift` contains `SystemStripView(snapshot: metricsSnapshot, theme: visualTheme)`
    - `Sources/GridOSApp/RootView.swift` contains `ActivityContextPanel(snapshot: metricsSnapshot, theme: visualTheme)`
    - `Sources/GridOSApp/RootView.swift` contains `.frame(width: 204)`
    - `Sources/GridOSApp/RootView.swift` contains `.padding(.top, 18)`
    - `Sources/GridOSApp/RootView.swift` contains `.padding(.horizontal, 18)`
    - `Sources/GridOSApp/RootView.swift` contains `CPU`
    - `Sources/GridOSApp/RootView.swift` contains `THERM`
    - `rg "GPU terminal text|fake metric|Cyberpunk|Matrix|marketplace" Sources/GridOSApp/RootView.swift` returns no matches
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>The app frame consumes mode tokens broadly while preserving terminal-first geometry and truthful metric text.</done>
</task>

<task id="05-03-02" type="auto">
  <name>Integrate mode-aware Metal shader branches and motion profiles</name>
  <files>Sources/RenderCore/MetalBackgroundView.swift, Tests/RenderCoreTests/RenderCoreModelTests.swift</files>
  <read_first>Sources/RenderCore/MetalBackgroundView.swift, Sources/RenderCore/VisualIdentity.swift, Sources/RenderCore/VisualTheme.swift, Sources/RenderCore/VisualEffectConfiguration.swift, Tests/RenderCoreTests/RenderCoreModelTests.swift, .planning/phases/05-aesthetic-modes/05-RESEARCH.md, .planning/phases/05-aesthetic-modes/05-VALIDATION.md</read_first>
  <action>Update `MetalBackgroundRenderer` to use `identity.mode.theme` for clear/fallback colors, pulse duration, pulse decay, event gain, and shader profile values. Keep one uniform struct and add only conservative scalar fields if needed: `mode`, `gridScale`, `fieldScale`, `scanlineIntensity`, `noiseIntensity`, and `detailDensity`. In `record(event:configuration:)`, multiply `event.magnitude` by `identity.mode.theme.motion.eventGain` before passing it into `VisualEffectConfiguration.pulseMagnitude`; set `activeUntil = CACurrentMediaTime() + identity.mode.theme.motion.maxPulseDuration`. In `draw(in:)`, decay `pulse` using `identity.mode.theme.motion.pulseDecay`, and pass `identity.mode.shaderValue` to the shader. Update `shaderSource` so `uniforms.mode < 0.5` renders Tron with cyan/blue grid/circuit rhythm and restrained amber; `uniforms.mode >= 0.5 && uniforms.mode < 1.5` renders Severance with austere monochrome fluorescent structure, minimal scan, and very low pulse; `uniforms.mode >= 1.5` renders Apple-native with dark macOS-like depth, soft blue accent, and quiet drift. Keep reduced motion behavior from `VisualEffectConfiguration`: if reduced motion returns zero pulse, do not start an animation timer and draw only a static frame. Extend `RenderCoreModelTests` so the Metal shader source still compiles and every `VisualMode` shader value is covered by tests. Do not add a new renderer, new dependency, always-on timer, custom terminal text rendering, asset files, Cyberpunk, Matrix, sound themes, or theme import support.</action>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg "uniforms\\.mode|shaderValue|eventGain|maxPulseDuration|pulseDecay|scanlineIntensity|detailDensity|reducedMotion" Sources/RenderCore Tests/RenderCoreTests</automated>
  </verify>
  <acceptance_criteria>
    - `Sources/RenderCore/MetalBackgroundView.swift` contains `identity.mode.theme`
    - `Sources/RenderCore/MetalBackgroundView.swift` contains `eventGain`
    - `Sources/RenderCore/MetalBackgroundView.swift` contains `maxPulseDuration`
    - `Sources/RenderCore/MetalBackgroundView.swift` contains `pulseDecay`
    - `Sources/RenderCore/MetalBackgroundView.swift` contains `uniforms.mode < 0.5`
    - `Sources/RenderCore/MetalBackgroundView.swift` contains `uniforms.mode < 1.5`
    - `Sources/RenderCore/MetalBackgroundView.swift` contains `scanlineIntensity`
    - `Sources/RenderCore/MetalBackgroundView.swift` contains `detailDensity`
    - `Tests/RenderCoreTests/RenderCoreModelTests.swift` contains `testEveryVisualModeHasShaderCoverage`
    - `Tests/RenderCoreTests/RenderCoreModelTests.swift` contains `testMetalShaderSourceCompilesWhenDeviceIsAvailable`
    - `rg "Timer\\.scheduledTimer" Sources/RenderCore/MetalBackgroundView.swift` returns the existing burst timer only; no second/global timer is added
    - `rg "Cyberpunk|Matrix|sound theme|theme import|terminal glyph|GPU text" Sources/RenderCore/MetalBackgroundView.swift Tests/RenderCoreTests/RenderCoreModelTests.swift` returns no matches
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>The Metal layer visibly differentiates all three modes and keeps reduced motion as the final animation gate.</done>
</task>

</tasks>

<verification>
Run:

```bash
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
rg "visualTheme|SystemStripView\\(snapshot: metricsSnapshot, theme: visualTheme\\)|ActivityContextPanel\\(snapshot: metricsSnapshot, theme: visualTheme\\)" Sources/GridOSApp/RootView.swift
rg "uniforms.mode|shaderValue|eventGain|maxPulseDuration|pulseDecay|reducedMotion" Sources/RenderCore Tests/RenderCoreTests
git diff --check
```
</verification>

<success_criteria>
- Three screenshots from the same app will have visibly different frame and background treatments.
- Token application touches background, header indicator, accents, panel separators, metrics strip, activity panel, and terminal chrome.
- Terminal usable space and SwiftTerm rendering are preserved.
- Metrics text remains readable and truthful.
- Metal shader compiles and reduced motion suppresses animation across modes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-aesthetic-modes/05-03-SUMMARY.md`.
</output>
