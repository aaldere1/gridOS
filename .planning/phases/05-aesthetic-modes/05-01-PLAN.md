---
phase: 05-aesthetic-modes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gridOS.xcodeproj/project.pbxproj
  - Sources/RenderCore/VisualIdentity.swift
  - Sources/RenderCore/VisualTheme.swift
  - Sources/RenderCore/ProceduralSeed.swift
  - Sources/RenderCore/VisualEffectConfiguration.swift
  - Sources/GridOSKit/GridOSAppPreferences.swift
  - Tests/RenderCoreTests/RenderCoreModelTests.swift
  - Tests/GridOSKitTests/GridOSAppPreferencesTests.swift
autonomous: true
requirements: ["PHASE-05"]
requirements_addressed: ["D-01", "D-02", "D-03", "D-04", "D-05", "D-09", "D-14", "D-15", "D-16", "D-17", "D-18"]
must_haves:
  truths:
    - "RenderCore exposes exactly three public visual modes: Tron, Severance, and Apple-native."
    - "The default public mode is Tron."
    - "Each mode has a coherent token bundle covering palette, panel treatment, terminal chrome, motion profile, and shader profile."
    - "Per-install procedural seeds are deterministic for the same install seed and distinct across modes."
    - "GridOSKit exposes tested local preference keys for selected mode and install seed."
  artifacts:
    - path: "Sources/RenderCore/VisualIdentity.swift"
      provides: "VisualMode public registry and default VisualIdentity"
      contains: "case tron"
    - path: "Sources/RenderCore/VisualTheme.swift"
      provides: "Mode-specific palette, panel, terminal, motion, and shader tokens"
      contains: "public struct VisualTheme"
    - path: "Sources/RenderCore/ProceduralSeed.swift"
      provides: "Stable per-install, per-mode procedural seed derivation"
      contains: "gridOS.visual.v1"
    - path: "Sources/GridOSKit/GridOSAppPreferences.swift"
      provides: "Preference keys and raw-value validation for visual mode and install seed"
      contains: "appearance.visualMode"
    - path: "Tests/RenderCoreTests/RenderCoreModelTests.swift"
      provides: "Mode registry, theme, seed, and reduced-motion regression coverage"
      contains: "testVisualModesExposePhaseFiveCases"
    - path: "Tests/GridOSKitTests/GridOSAppPreferencesTests.swift"
      provides: "Preference key/default/fallback regression coverage"
      contains: "testVisualModeRawValueFallsBackToTron"
  key_links:
    - from: "Sources/RenderCore/VisualIdentity.swift"
      to: "Sources/RenderCore/VisualTheme.swift"
      via: "VisualMode resolves a VisualTheme"
      pattern: "theme"
    - from: "Sources/RenderCore/ProceduralSeed.swift"
      to: "Sources/RenderCore/VisualIdentity.swift"
      via: "Install-derived seed used by VisualIdentity"
      pattern: "installDerived"
    - from: "Sources/GridOSKit/GridOSAppPreferences.swift"
      to: "Tests/GridOSKitTests/GridOSAppPreferencesTests.swift"
      via: "Preference raw value validation is unit tested"
      pattern: "appearance.visualMode"
---

<objective>
Create the Phase 5 model foundation: public mode registry, shared visual tokens, per-mode motion/effect profiles, deterministic install-derived seeds, and local preference keys.

Purpose: Later app and renderer work should consume one tested contract instead of scattering theme literals across SwiftUI and Metal.
Output: `RenderCore` visual-mode/token models, `GridOSKit` preference helpers, and XCTest coverage from `.planning/phases/05-aesthetic-modes/05-VALIDATION.md` Wave 0 requirements.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-aesthetic-modes/05-CONTEXT.md
@.planning/phases/05-aesthetic-modes/05-RESEARCH.md
@.planning/phases/05-aesthetic-modes/05-VALIDATION.md
@docs/vision.md
@docs/architecture.md
@project.yml
@Sources/RenderCore/VisualIdentity.swift
@Sources/RenderCore/ProceduralSeed.swift
@Sources/RenderCore/VisualEffectConfiguration.swift
@Sources/GridOSKit/GridOSAppPreferences.swift
@Tests/RenderCoreTests/RenderCoreModelTests.swift
@Tests/GridOSKitTests/GridOSAppPreferencesTests.swift

<interfaces>
Current contracts to replace or extend:

```swift
public enum VisualMode: String, CaseIterable, Equatable, Sendable {
    case signalField
}

public struct VisualIdentity: Equatable, Sendable {
    public static let `default` = VisualIdentity(mode: .signalField, seed: .gridOSDefault)
    public let mode: VisualMode
    public let seed: ProceduralSeed
}

public struct GridOSAppPreferences: Equatable, Sendable {
    public static let defaultShellPath = "/bin/zsh"
    public static let defaultTerminalFontSize = 13.0
    public static let defaultVisualIntensity = 0.65
}
```
</interfaces>
</context>

<tasks>

<task id="05-01-01" type="auto" tdd="true">
  <name>Create RenderCore mode registry, tokens, seed derivation, and tests</name>
  <files>gridOS.xcodeproj/project.pbxproj, Sources/RenderCore/VisualIdentity.swift, Sources/RenderCore/VisualTheme.swift, Sources/RenderCore/ProceduralSeed.swift, Sources/RenderCore/VisualEffectConfiguration.swift, Tests/RenderCoreTests/RenderCoreModelTests.swift</files>
  <read_first>Sources/RenderCore/VisualIdentity.swift, Sources/RenderCore/ProceduralSeed.swift, Sources/RenderCore/VisualEffectConfiguration.swift, Sources/RenderCore/MetalBackgroundView.swift, Tests/RenderCoreTests/RenderCoreModelTests.swift, .planning/phases/05-aesthetic-modes/05-CONTEXT.md, .planning/phases/05-aesthetic-modes/05-VALIDATION.md</read_first>
  <behavior>
    - Test first for `VisualMode.allCases == [.tron, .severance, .appleNative]`.
    - Test exact raw values `tron`, `severance`, `appleNative` and exact display names `Tron`, `Severance`, `Apple-native`.
    - Test `VisualMode.defaultMode == .tron`, `VisualIdentity.default.mode == .tron`, and cycling order `tron -> severance -> appleNative -> tron`.
    - Test every mode has a unique `shaderValue`, `VisualTheme.signature`, and `VisualMotionProfile`.
    - Test `ProceduralSeed.installDerived(installSeed:mode:)` is stable for same seed/mode and distinct for same seed/different mode.
    - Test reduced motion still returns zero pulse magnitude for all mode motion profiles.
  </behavior>
  <action>Add the Wave 0 tests from `05-VALIDATION.md` before implementation. Replace the single `signalField` public mode with `public enum VisualMode: String, CaseIterable, Equatable, Sendable, Identifiable` cases `tron`, `severance`, and `appleNative`; add `id`, `static let defaultMode: VisualMode = .tron`, `next`, exact display names `Tron`, `Severance`, `Apple-native`, and shader values `0`, `1`, and `2`. Keep `signalField` only if needed as a private implementation detail; do not expose it in `allCases` or user-facing copy. Create `Sources/RenderCore/VisualTheme.swift` with `public struct VisualColor`, `VisualPalette`, `VisualPanelStyle`, `VisualTerminalChrome`, `VisualMotionProfile`, `VisualShaderProfile`, and `VisualTheme`, all `Equatable` and `Sendable`. Add exact mode token values: Tron background `(0.004, 0.007, 0.011, 1)`, primary accent `(0.10, 0.72, 0.78, 1)`, secondary accent `(0.12, 0.24, 0.46, 1)`, status accent `(0.95, 0.52, 0.20, 1)`, motion `idleDriftRate: 0.18`, `eventGain: 1.00`, `pulseDecay: 0.42`, `maxPulseDuration: 1.40`, `detailDensity: 0.92`. Severance background `(0.010, 0.011, 0.010, 1)`, primary accent `(0.78, 0.84, 0.76, 1)`, secondary accent `(0.30, 0.34, 0.30, 1)`, status accent `(0.62, 0.68, 0.56, 1)`, motion `idleDriftRate: 0.025`, `eventGain: 0.22`, `pulseDecay: 0.88`, `maxPulseDuration: 0.45`, `detailDensity: 0.28`. Apple-native background `(0.015, 0.017, 0.020, 1)`, primary accent `(0.38, 0.68, 1.00, 1)`, secondary accent `(0.26, 0.32, 0.42, 1)`, status accent `(0.97, 0.73, 0.30, 1)`, motion `idleDriftRate: 0.06`, `eventGain: 0.38`, `pulseDecay: 0.62`, `maxPulseDuration: 0.75`, `detailDensity: 0.48`. Add `public var theme: VisualTheme` on `VisualMode`. Update `VisualIdentity.default` to `.tron` and add `public init(mode: VisualMode = .defaultMode, installSeed: String)` using `ProceduralSeed.installDerived`. Add `public static func installDerived(installSeed: String, mode: VisualMode) -> ProceduralSeed` in `ProceduralSeed` with stable string `gridOS.visual.v1.\(installSeed).\(mode.rawValue)`. Do not add Cyberpunk, Matrix, sound themes, plugin/user themes, full light mode, GPU terminal text rendering, marketplace/import themes, or eDEX theme compatibility.</action>
  <verify>
    <automated>xcodegen generate --use-cache &amp;&amp; xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg "case tron|case severance|case appleNative|VisualTheme|VisualMotionProfile|gridOS\\.visual\\.v1" Sources/RenderCore Tests/RenderCoreTests</automated>
  </verify>
  <acceptance_criteria>
    - `Sources/RenderCore/VisualIdentity.swift` contains `case tron`
    - `Sources/RenderCore/VisualIdentity.swift` contains `case severance`
    - `Sources/RenderCore/VisualIdentity.swift` contains `case appleNative`
    - `Sources/RenderCore/VisualIdentity.swift` contains `static let defaultMode: VisualMode = .tron`
    - `Sources/RenderCore/VisualIdentity.swift` contains `Apple-native`
    - `Sources/RenderCore/VisualTheme.swift` contains `public struct VisualTheme`
    - `Sources/RenderCore/VisualTheme.swift` contains `public struct VisualMotionProfile`
    - `Sources/RenderCore/VisualTheme.swift` contains `idleDriftRate: 0.18`
    - `Sources/RenderCore/VisualTheme.swift` contains `eventGain: 0.22`
    - `Sources/RenderCore/VisualTheme.swift` contains `eventGain: 0.38`
    - `Sources/RenderCore/ProceduralSeed.swift` contains `gridOS.visual.v1`
    - `Tests/RenderCoreTests/RenderCoreModelTests.swift` contains `testVisualModesExposePhaseFiveCases`
    - `Tests/RenderCoreTests/RenderCoreModelTests.swift` contains `testInstallDerivedSeedIsStableAndModeSpecific`
    - `Tests/RenderCoreTests/RenderCoreModelTests.swift` contains `testReducedMotionSuppressesPulseMagnitudeForEveryMode`
    - `rg "Cyberpunk|Matrix|marketplace|sound theme|GPU terminal text" Sources/RenderCore Tests/RenderCoreTests` returns no matches
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>RenderCore exposes a tested, exact three-mode Phase 5 visual identity contract with deterministic install-derived seeds.</done>
</task>

<task id="05-01-02" type="auto" tdd="true">
  <name>Add visual mode and install seed preference helpers</name>
  <files>Sources/GridOSKit/GridOSAppPreferences.swift, Tests/GridOSKitTests/GridOSAppPreferencesTests.swift</files>
  <read_first>Sources/GridOSKit/GridOSAppPreferences.swift, Tests/GridOSKitTests/GridOSAppPreferencesTests.swift, .planning/phases/05-aesthetic-modes/05-CONTEXT.md, .planning/phases/05-aesthetic-modes/05-VALIDATION.md, .planning/phases/03-production-app-frame/03-01-SUMMARY.md</read_first>
  <behavior>
    - Test exact preference keys `appearance.visualMode` and `appearance.installSeed`.
    - Test `defaultVisualModeRawValue == "tron"` and `defaultInstallSeedRawValue == ""`.
    - Test raw mode fallback: empty, whitespace, unknown, `cyberpunk`, and `matrix` all normalize to `tron`.
    - Test `nextVisualModeRawValue(after:)` cycles `tron -> severance -> appleNative -> tron` and unknown values cycle from `tron` to `severance`.
    - Test install seed normalization trims whitespace and treats blank values as empty.
  </behavior>
  <action>Extend `GridOSAppPreferences` without importing `RenderCore`, because `RenderCore` depends on `GridOSKit` and dependency direction must not invert. Add constants `public static let visualModeStorageKey = "appearance.visualMode"`, `public static let installSeedStorageKey = "appearance.installSeed"`, `public static let defaultVisualModeRawValue = "tron"`, `public static let defaultInstallSeedRawValue = ""`, and `public static let supportedVisualModeRawValues = ["tron", "severance", "appleNative"]`. Add `public static func normalizedVisualModeRawValue(_ rawValue: String) -> String` that trims whitespace and returns `"tron"` unless the trimmed value is exactly one supported raw value. Add `public static func nextVisualModeRawValue(after rawValue: String) -> String` using order `["tron", "severance", "appleNative"]`. Add `public static func normalizedInstallSeedRawValue(_ rawValue: String) -> String` that trims whitespace and newlines. Do not store hardware identifiers, serial numbers, timestamps, or random values in `GridOSKit`; first-launch generation belongs in the app layer in Plan 02.</action>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg "appearance\\.visualMode|appearance\\.installSeed|defaultVisualModeRawValue|nextVisualModeRawValue|normalizedInstallSeedRawValue" Sources/GridOSKit Tests/GridOSKitTests</automated>
  </verify>
  <acceptance_criteria>
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `visualModeStorageKey = "appearance.visualMode"`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `installSeedStorageKey = "appearance.installSeed"`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `defaultVisualModeRawValue = "tron"`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `defaultInstallSeedRawValue = ""`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `"appleNative"`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `nextVisualModeRawValue`
    - `Tests/GridOSKitTests/GridOSAppPreferencesTests.swift` contains `testVisualModeRawValueFallsBackToTron`
    - `Tests/GridOSKitTests/GridOSAppPreferencesTests.swift` contains `testVisualModeRawValueCyclesInPhaseFiveOrder`
    - `Tests/GridOSKitTests/GridOSAppPreferencesTests.swift` contains `testInstallSeedRawValueIsTrimmed`
    - `rg "hardware|serial|timestamp" Sources/GridOSKit Tests/GridOSKitTests` returns no matches related to install seed generation
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>GridOSKit owns tested local persistence keys and raw-value normalization for Phase 5 without crossing module boundaries.</done>
</task>

</tasks>

<verification>
Run the Phase 5 validation source checks for foundation work:

```bash
xcodegen generate --use-cache
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
rg "case tron|case severance|case appleNative|VisualTheme|VisualMotionProfile" Sources/RenderCore Tests/RenderCoreTests
rg "appearance.visualMode|appearance.installSeed|@AppStorage" Sources/GridOSApp Sources/GridOSKit Tests/GridOSKitTests
git diff --check
```
</verification>

<success_criteria>
- Public user-facing modes are exactly `tron`, `severance`, and `appleNative`.
- Default public mode is `.tron`.
- Token, motion, and seed contracts are tested before app integration.
- Preference keys are exact: `appearance.visualMode` and `appearance.installSeed`.
- No deferred modes or theme systems are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/05-aesthetic-modes/05-01-SUMMARY.md`.
</output>
