---
phase: 08-macos-integrations
plan: 01
title: Integration models, preferences, and Keychain foundation
status: pending
type: execute
wave: 1
depends_on: []
files_modified:
  - project.yml
  - gridOS.xcodeproj/project.pbxproj
  - Sources/Integrations/Info.plist
  - Sources/Integrations/MacIntegrationModels.swift
  - Sources/GridOSKit/GridOSAppPreferences.swift
  - Sources/GridOSKit/KeychainCredentialStore.swift
  - Sources/CommandIntelligence/KeychainCommandCredentialStore.swift
  - Tests/IntegrationsTests/MacIntegrationModelsTests.swift
  - Tests/GridOSKitTests/GridOSAppPreferencesTests.swift
  - Tests/GridOSKitTests/KeychainCredentialStoreTests.swift
  - Tests/CommandIntelligenceTests/CommandCredentialStoreTests.swift
autonomous: true
requirements: ["PHASE-08"]
requirements_addressed: ["D-01", "D-02", "D-03", "D-07", "D-10", "D-11", "D-12", "D-17"]
must_haves:
  truths:
    - "Phase 8 has a separate Integrations module for testable macOS integration models."
    - "Integration preferences use GridOSKit storage keys with menu bar enabled by default and notifications/indexing disabled by default."
    - "Generic Keychain SecItem query construction lives in GridOSKit and CommandIntelligence reuses it without duplicating raw SecItem wrappers."
  artifacts:
    - path: "Sources/Integrations/MacIntegrationModels.swift"
      provides: "Pure menu bar, notification, and macOS integration model types"
      contains: "MacIntegrationPreferences"
    - path: "Sources/GridOSKit/KeychainCredentialStore.swift"
      provides: "Reusable Keychain descriptor, query, client, and generic secret store"
      contains: "KeychainCredentialDescriptor"
    - path: "Sources/CommandIntelligence/KeychainCommandCredentialStore.swift"
      provides: "Command Intelligence credential store backed by GridOSKit Keychain primitives"
      contains: "KeychainCredentialStore"
  key_links:
    - from: "Sources/CommandIntelligence/KeychainCommandCredentialStore.swift"
      to: "Sources/GridOSKit/KeychainCredentialStore.swift"
      via: "Command credentials call the shared KeychainCredentialStore"
      pattern: "KeychainCredentialDescriptor"
---

<objective>
Create the reusable Phase 8 foundation: a lightweight `Integrations` framework, persistent preference keys, and shared Keychain primitives.

Purpose: Menu bar, notifications, metadata indexing, and secret management need testable models before app UI wiring begins.
Output: new integration model target, preference defaults, reusable Keychain storage in `GridOSKit`, and tests proving no secrets are persisted in AppStorage/UserDefaults.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-macos-integrations/08-CONTEXT.md
@.planning/phases/08-macos-integrations/08-RESEARCH.md
@.planning/phases/08-macos-integrations/08-VALIDATION.md
@.planning/phases/08-macos-integrations/08-UI-SPEC.md
@project.yml
@Sources/GridOSKit/GridOSAppPreferences.swift
@Sources/CommandIntelligence/KeychainCommandCredentialStore.swift
@Tests/CommandIntelligenceTests/CommandCredentialStoreTests.swift
</context>

<tasks>

<task id="08-01-01" type="auto" tdd="true">
  <title>Add Integrations target, core models, and preference defaults</title>
  <name>Add Integrations target, core models, and preference defaults</name>
  <files>project.yml, gridOS.xcodeproj/project.pbxproj, Sources/Integrations/Info.plist, Sources/Integrations/MacIntegrationModels.swift, Sources/GridOSKit/GridOSAppPreferences.swift, Tests/IntegrationsTests/MacIntegrationModelsTests.swift, Tests/GridOSKitTests/GridOSAppPreferencesTests.swift</files>
  <read_first>project.yml, Sources/GridOSKit/GridOSAppPreferences.swift, Tests/GridOSKitTests/GridOSAppPreferencesTests.swift, .planning/phases/08-macos-integrations/08-CONTEXT.md, .planning/phases/08-macos-integrations/08-RESEARCH.md, .planning/phases/08-macos-integrations/08-UI-SPEC.md</read_first>
  <action>Add a new XcodeGen framework target named `Integrations` with source path `Sources/Integrations`, bundle identifier `com.aaldere1.gridos.Integrations`, and a target dependency on `GridOSKit`. Add a unit-test target named `IntegrationsTests` with source path `Tests/IntegrationsTests`, bundle identifier `com.aaldere1.gridos.IntegrationsTests`, and dependency on `Integrations`. Add `IntegrationsTests` to the `gridOS` scheme test targets. Create `Sources/Integrations/Info.plist` using the same minimal framework plist shape as the other framework modules. Create `Sources/Integrations/MacIntegrationModels.swift` with public Sendable/Equatable types named exactly `MacIntegrationPreferences`, `MenuBarStatusSnapshot`, `MenuBarRecentDirectory`, `MenuBarAction`, `NotificationAuthorizationState`, `NotificationDeliveryRequest`, and `NotificationDeliveryResult`. `MenuBarAction` must include exact cases `openGridOS`, `openSettings`, `quitGridOS`, and `openRecentDirectory`. `NotificationAuthorizationState` must include exact cases `notDetermined`, `denied`, `authorized`, `provisional`, and `ephemeral`. Extend `GridOSAppPreferences` with exact storage keys `showMenuBarExtraStorageKey = "integrations.showMenuBarExtra"`, `notificationsEnabledStorageKey = "integrations.notificationsEnabled"`, and `indexWorkspaceMetadataStorageKey = "integrations.indexWorkspaceMetadata"`, plus defaults `defaultShowMenuBarExtra = true`, `defaultNotificationsEnabled = false`, and `defaultIndexWorkspaceMetadata = false`. Add tests proving the target builds, `MacIntegrationPreferences.defaultValue` uses those defaults, notification content request models are equatable, and the integration storage keys do not contain `api`, `key`, `secret`, `prompt`, `output`, `history`, or `transcript`. Run `xcodegen generate --use-cache`.</action>
  <acceptance_criteria>
    - `project.yml` contains `Integrations:`
    - `project.yml` contains `IntegrationsTests:`
    - `project.yml` contains `com.aaldere1.gridos.Integrations`
    - `project.yml` contains `Tests/IntegrationsTests`
    - `Sources/Integrations/Info.plist` exists
    - `Sources/Integrations/MacIntegrationModels.swift` contains `public struct MacIntegrationPreferences`
    - `Sources/Integrations/MacIntegrationModels.swift` contains `public struct MenuBarStatusSnapshot`
    - `Sources/Integrations/MacIntegrationModels.swift` contains `public struct MenuBarRecentDirectory`
    - `Sources/Integrations/MacIntegrationModels.swift` contains `public enum MenuBarAction`
    - `Sources/Integrations/MacIntegrationModels.swift` contains `openGridOS`
    - `Sources/Integrations/MacIntegrationModels.swift` contains `public enum NotificationAuthorizationState`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `showMenuBarExtraStorageKey`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `integrations.showMenuBarExtra`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `defaultShowMenuBarExtra = true`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `defaultNotificationsEnabled = false`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `defaultIndexWorkspaceMetadata = false`
    - `Tests/IntegrationsTests/MacIntegrationModelsTests.swift` contains `testDefaultPreferencesEnableMenuBarOnly`
    - `Tests/GridOSKitTests/GridOSAppPreferencesTests.swift` contains `testMacIntegrationPreferencesDoNotStoreSecrets`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:IntegrationsTests -only-testing:GridOSKitTests/GridOSAppPreferencesTests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodegen generate --use-cache &amp;&amp; xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:IntegrationsTests -only-testing:GridOSKitTests/GridOSAppPreferencesTests &amp;&amp; rg 'Integrations:|IntegrationsTests:|MacIntegrationPreferences|MenuBarStatusSnapshot|NotificationAuthorizationState|integrations.showMenuBarExtra' project.yml Sources Tests</automated>
  </verify>
  <done>Phase 8 has a testable integration model target and privacy-safe persistent preference keys.</done>
</task>

<task id="08-01-02" type="auto" tdd="true">
  <title>Move reusable Keychain primitives into GridOSKit</title>
  <name>Move reusable Keychain primitives into GridOSKit</name>
  <files>Sources/GridOSKit/KeychainCredentialStore.swift, Sources/CommandIntelligence/KeychainCommandCredentialStore.swift, Tests/GridOSKitTests/KeychainCredentialStoreTests.swift, Tests/CommandIntelligenceTests/CommandCredentialStoreTests.swift</files>
  <read_first>Sources/CommandIntelligence/KeychainCommandCredentialStore.swift, Tests/CommandIntelligenceTests/CommandCredentialStoreTests.swift, Sources/GridOSKit/GridOSAppPreferences.swift, .planning/phases/08-macos-integrations/08-CONTEXT.md, .planning/phases/08-macos-integrations/08-RESEARCH.md</read_first>
  <action>Create `Sources/GridOSKit/KeychainCredentialStore.swift`. Move the generic types `KeychainSecItemValue`, `KeychainSecItemQuery`, `KeychainSecItemCopyResult`, `KeychainSecItemClient`, and `LiveKeychainSecItemClient` out of `Sources/CommandIntelligence/KeychainCommandCredentialStore.swift` into `GridOSKit` with public initializers and public query subscript access for tests. In the same file add `public struct KeychainCredentialDescriptor: Equatable, Sendable` with fields `service: String`, `account: String`, `accessible: String`, and `useDataProtectionKeychain: Bool`, defaulting `accessible` to `kSecAttrAccessibleWhenUnlockedThisDeviceOnly as String` and `useDataProtectionKeychain` to `true`. Add `public struct KeychainCredentialStore: Sendable` with methods `secret(for:) async throws -> String?`, `saveSecret(_:for:) async throws`, and `deleteSecret(for:) async throws`. It must build generic-password queries containing `kSecClassGenericPassword`, `kSecAttrService`, `kSecAttrAccount`, `kSecAttrAccessibleWhenUnlockedThisDeviceOnly`, and `kSecUseDataProtectionKeychain`. Update `KeychainCommandCredentialStore` so it imports `GridOSKit`, keeps `public static let service = "com.aaldere1.gridos.command-intelligence"`, wraps `KeychainCredentialStore`, and maps empty API keys or Keychain errors to the existing `CommandIntelligenceFailure` copy. Update `CommandCredentialStoreTests` to import `GridOSKit` for shared query types. Add `KeychainCredentialStoreTests` with fake `KeychainSecItemClient` coverage for save, update duplicate, read, delete, missing item, empty secret rejection, and exact query fields. Do not add AppStorage/UserDefaults storage for any secret.</action>
  <acceptance_criteria>
    - `Sources/GridOSKit/KeychainCredentialStore.swift` contains `public enum KeychainSecItemValue`
    - `Sources/GridOSKit/KeychainCredentialStore.swift` contains `public struct KeychainSecItemQuery`
    - `Sources/GridOSKit/KeychainCredentialStore.swift` contains `public protocol KeychainSecItemClient`
    - `Sources/GridOSKit/KeychainCredentialStore.swift` contains `public struct KeychainCredentialDescriptor`
    - `Sources/GridOSKit/KeychainCredentialStore.swift` contains `public struct KeychainCredentialStore`
    - `Sources/GridOSKit/KeychainCredentialStore.swift` contains `kSecClassGenericPassword`
    - `Sources/GridOSKit/KeychainCredentialStore.swift` contains `kSecAttrAccessibleWhenUnlockedThisDeviceOnly`
    - `Sources/CommandIntelligence/KeychainCommandCredentialStore.swift` contains `import GridOSKit`
    - `Sources/CommandIntelligence/KeychainCommandCredentialStore.swift` contains `KeychainCredentialStore`
    - `Sources/CommandIntelligence/KeychainCommandCredentialStore.swift` contains `com.aaldere1.gridos.command-intelligence`
    - `Tests/GridOSKitTests/KeychainCredentialStoreTests.swift` contains `testDescriptorBuildsGenericPasswordQuery`
    - `Tests/GridOSKitTests/KeychainCredentialStoreTests.swift` contains `testStoreUpdatesDuplicateItem`
    - `Tests/CommandIntelligenceTests/CommandCredentialStoreTests.swift` contains `import GridOSKit`
    - `! rg 'AppStorage|UserDefaults' Sources/GridOSKit/KeychainCredentialStore.swift Sources/CommandIntelligence/KeychainCommandCredentialStore.swift`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:GridOSKitTests/KeychainCredentialStoreTests -only-testing:CommandIntelligenceTests/CommandCredentialStoreTests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:GridOSKitTests/KeychainCredentialStoreTests -only-testing:CommandIntelligenceTests/CommandCredentialStoreTests &amp;&amp; rg 'KeychainCredentialDescriptor|KeychainCredentialStore|kSecClassGenericPassword|kSecAttrAccessibleWhenUnlockedThisDeviceOnly' Sources/GridOSKit Sources/CommandIntelligence Tests &amp;&amp; ! rg 'AppStorage|UserDefaults' Sources/GridOSKit/KeychainCredentialStore.swift Sources/CommandIntelligence/KeychainCommandCredentialStore.swift</automated>
  </verify>
  <done>Command Intelligence and future macOS integrations share one tested Keychain primitive layer without preference-backed secrets.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
xcodegen generate --use-cache
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
rg 'Integrations|MacIntegrationPreferences|MenuBarAction|NotificationAuthorizationState|KeychainCredentialDescriptor|KeychainCredentialStore' Sources Tests project.yml
! rg 'apiKey.*AppStorage|UserDefaults.*api|AppStorage.*secret|UserDefaults.*secret' Sources/GridOSApp Sources/GridOSKit Sources/CommandIntelligence Sources/Integrations
git diff --check
```
</verification>
