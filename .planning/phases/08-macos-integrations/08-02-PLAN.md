---
phase: 08-macos-integrations
plan: 02
title: Menu bar extra and macOS Integrations Settings
status: pending
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - Sources/GridOSApp/GridOSApp.swift
  - Sources/GridOSApp/MacIntegrationsController.swift
  - Sources/GridOSApp/MenuBarExtraView.swift
  - Sources/GridOSApp/MacIntegrationsSettingsView.swift
  - Sources/GridOSApp/SettingsView.swift
  - Sources/GridOSApp/RootView.swift
  - docs/architecture.md
autonomous: true
requirements: ["PHASE-08"]
requirements_addressed: ["D-01", "D-02", "D-04", "D-05", "D-06", "D-08", "D-09", "D-10", "D-15", "D-18"]
must_haves:
  truths:
    - "gridOS exposes a SwiftUI MenuBarExtra as a companion to the main app window, not a replacement app mode."
    - "The menu bar extra shows Open gridOS, active workspace, host status, recent directories, Settings, and Quit gridOS."
    - "Settings includes a compact macOS Integrations section with menu bar, notification, indexing, and secret-management controls."
    - "Menu bar actions activate the app or open Finder/Settings only; they do not insert or execute terminal text."
  artifacts:
    - path: "Sources/GridOSApp/GridOSApp.swift"
      provides: "App scene composition including MenuBarExtra"
      contains: "MenuBarExtra"
    - path: "Sources/GridOSApp/MenuBarExtraView.swift"
      provides: "Compact menu bar content and safe actions"
      contains: "Open gridOS"
    - path: "Sources/GridOSApp/MacIntegrationsSettingsView.swift"
      provides: "Settings section for Phase 8 integration controls"
      contains: "macOS Integrations"
  key_links:
    - from: "Sources/GridOSApp/MenuBarExtraView.swift"
      to: "Sources/TerminalCore/TerminalWorkspacePersistence.swift"
      via: "Recent directories are loaded from the existing Phase 7 snapshot store"
      pattern: "TerminalWorkspaceSnapshotStore"
---

<objective>
Wire the user-facing Mac-native surface: a lightweight menu bar extra and a compact Settings section.

Purpose: Phase 8 should make gridOS feel like a good Mac citizen without turning the menu bar into a second dashboard or compromising terminal focus.
Output: `MenuBarExtra`, menu content, app activation/settings actions, Settings toggles, and source/docs proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-macos-integrations/08-CONTEXT.md
@.planning/phases/08-macos-integrations/08-RESEARCH.md
@.planning/phases/08-macos-integrations/08-VALIDATION.md
@.planning/phases/08-macos-integrations/08-UI-SPEC.md
@Sources/GridOSApp/GridOSApp.swift
@Sources/GridOSApp/RootView.swift
@Sources/GridOSApp/SettingsView.swift
@Sources/SystemMetrics/SystemMetricsSnapshot.swift
@Sources/SystemMetrics/SystemMetricsSampler.swift
@Sources/TerminalCore/TerminalWorkspacePersistence.swift
</context>

<tasks>

<task id="08-02-01" type="auto">
  <title>Add SwiftUI menu bar extra with safe app and directory actions</title>
  <name>Add SwiftUI menu bar extra with safe app and directory actions</name>
  <files>Sources/GridOSApp/GridOSApp.swift, Sources/GridOSApp/MacIntegrationsController.swift, Sources/GridOSApp/MenuBarExtraView.swift, docs/architecture.md</files>
  <read_first>Sources/GridOSApp/GridOSApp.swift, Sources/GridOSApp/RootView.swift, Sources/SystemMetrics/SystemMetricsSnapshot.swift, Sources/SystemMetrics/SystemMetricsSampler.swift, Sources/TerminalCore/TerminalWorkspacePersistence.swift, Sources/Integrations/MacIntegrationModels.swift, .planning/phases/08-macos-integrations/08-UI-SPEC.md</read_first>
  <action>Import `Integrations`, `SystemMetrics`, and `AppKit` where needed. Add `@AppStorage(GridOSAppPreferences.showMenuBarExtraStorageKey) private var showMenuBarExtra = GridOSAppPreferences.defaultShowMenuBarExtra` to `GridOSApplication`. Add `MenuBarExtra("gridOS", systemImage: "terminal", isInserted: $showMenuBarExtra) { MenuBarExtraView(controller: macIntegrationsController) }` while keeping the existing `WindowGroup`, hidden-titlebar style, commands, and `Settings` scene. Start with `.menuBarExtraStyle(.menu)`. Create `MacIntegrationsController` as an `@MainActor ObservableObject` in `Sources/GridOSApp` that exposes `@Published var status: MenuBarStatusSnapshot`, `@Published var recentDirectories: [MenuBarRecentDirectory]`, and functions `refresh()`, `openGridOS()`, `openSettings()`, `openRecentDirectory(_:)`, and `quitGridOS()`. It must load recent directories from `TerminalWorkspaceSnapshotStore().loadRecentDirectories()` and map each path to a display basename without exposing full paths in the visible menu label. It must derive host status strings from `SystemMetricsSnapshot` using coarse labels for CPU, MEM, NET, BAT, and THERM. `openGridOS()` must activate the app and bring an existing gridOS window forward without inserting or running any terminal text. `openSettings()` must call the native settings window action and activate the app. `openRecentDirectory(_:)` may use `NSWorkspace.shared.open(URL(fileURLWithPath:))` or reveal the directory in Finder; it must not run shell commands. Create `MenuBarExtraView` with exact visible labels `Open gridOS`, `Active workspace`, `Host Status`, `Recent Directories`, `Settings`, and `Quit gridOS`. Add docs/architecture notes that menu bar actions are app/Finder/Settings actions only.</action>
  <acceptance_criteria>
    - `Sources/GridOSApp/GridOSApp.swift` contains `MenuBarExtra("gridOS"`
    - `Sources/GridOSApp/GridOSApp.swift` contains `systemImage: "terminal"`
    - `Sources/GridOSApp/GridOSApp.swift` contains `isInserted: $showMenuBarExtra`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.menuBarExtraStyle(.menu)`
    - `Sources/GridOSApp/GridOSApp.swift` contains `GridOSAppPreferences.showMenuBarExtraStorageKey`
    - `Sources/GridOSApp/MacIntegrationsController.swift` contains `final class MacIntegrationsController`
    - `Sources/GridOSApp/MacIntegrationsController.swift` contains `TerminalWorkspaceSnapshotStore`
    - `Sources/GridOSApp/MacIntegrationsController.swift` contains `openGridOS()`
    - `Sources/GridOSApp/MacIntegrationsController.swift` contains `openSettings()`
    - `Sources/GridOSApp/MacIntegrationsController.swift` contains `openRecentDirectory`
    - `Sources/GridOSApp/MenuBarExtraView.swift` contains `Open gridOS`
    - `Sources/GridOSApp/MenuBarExtraView.swift` contains `Active workspace`
    - `Sources/GridOSApp/MenuBarExtraView.swift` contains `Host Status`
    - `Sources/GridOSApp/MenuBarExtraView.swift` contains `Recent Directories`
    - `Sources/GridOSApp/MenuBarExtraView.swift` contains `Quit gridOS`
    - `! rg 'runInActivePane|insertInActivePane|TerminalInteractionController|send\\(' Sources/GridOSApp/MenuBarExtraView.swift Sources/GridOSApp/MacIntegrationsController.swift`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg 'MenuBarExtra|showMenuBarExtra|Open gridOS|Active workspace|Host Status|Recent Directories|Quit gridOS' Sources/GridOSApp Sources/Integrations docs/architecture.md &amp;&amp; ! rg 'runInActivePane|insertInActivePane|TerminalInteractionController|send\\(' Sources/GridOSApp/MenuBarExtraView.swift Sources/GridOSApp/MacIntegrationsController.swift</automated>
  </verify>
  <done>gridOS has a compact native menu bar companion with safe actions and Phase 7 recent-directory data.</done>
</task>

<task id="08-02-02" type="auto">
  <title>Add macOS Integrations Settings section</title>
  <name>Add macOS Integrations Settings section</name>
  <files>Sources/GridOSApp/MacIntegrationsSettingsView.swift, Sources/GridOSApp/SettingsView.swift, Sources/GridOSApp/GridOSApp.swift, docs/architecture.md</files>
  <read_first>Sources/GridOSApp/SettingsView.swift, Sources/GridOSApp/CommandIntelligenceSettingsView.swift, Sources/GridOSApp/CommandIntelligenceCommandCenter.swift, Sources/GridOSKit/GridOSAppPreferences.swift, .planning/phases/08-macos-integrations/08-UI-SPEC.md</read_first>
  <action>Create `Sources/GridOSApp/MacIntegrationsSettingsView.swift` with `struct MacIntegrationsSettingsView: View`. Add AppStorage-backed controls using `GridOSAppPreferences.showMenuBarExtraStorageKey`, `GridOSAppPreferences.notificationsEnabledStorageKey`, and `GridOSAppPreferences.indexWorkspaceMetadataStorageKey`. The section title must be exactly `macOS Integrations`. Add a toggle labeled `Show Menu Bar Extra`, a toggle labeled `Notify when long-running work finishes`, a button labeled `Enable Notifications` with disabled-state copy `Notifications are off. Terminal work continues normally.`, a toggle labeled `Index saved workspace metadata`, privacy copy exactly `Only saved workspace labels and directory names are indexed. Terminal output and command history are never indexed.`, and a button labeled `Manage Stored Secrets`. For this plan, `Enable Notifications` can route to the notification command center added in Plan 03 as a no-op placeholder if Plan 03 is not implemented yet, but the Settings view must compile. `Manage Stored Secrets` must scroll/focus the existing Command Intelligence settings section by posting `gridOS.commandIntelligence.openSettings`; it must not reveal or print stored secret values. Insert `MacIntegrationsSettingsView()` into `SettingsView` between Appearance and Command Intelligence. Update Settings reset behavior so menu bar returns to true, notifications false, and indexing false. Do not create an onboarding screen or landing page.</action>
  <acceptance_criteria>
    - `Sources/GridOSApp/MacIntegrationsSettingsView.swift` contains `struct MacIntegrationsSettingsView`
    - `Sources/GridOSApp/MacIntegrationsSettingsView.swift` contains `macOS Integrations`
    - `Sources/GridOSApp/MacIntegrationsSettingsView.swift` contains `Show Menu Bar Extra`
    - `Sources/GridOSApp/MacIntegrationsSettingsView.swift` contains `Notify when long-running work finishes`
    - `Sources/GridOSApp/MacIntegrationsSettingsView.swift` contains `Enable Notifications`
    - `Sources/GridOSApp/MacIntegrationsSettingsView.swift` contains `Notifications are off. Terminal work continues normally.`
    - `Sources/GridOSApp/MacIntegrationsSettingsView.swift` contains `Index saved workspace metadata`
    - `Sources/GridOSApp/MacIntegrationsSettingsView.swift` contains `Only saved workspace labels and directory names are indexed. Terminal output and command history are never indexed.`
    - `Sources/GridOSApp/MacIntegrationsSettingsView.swift` contains `Manage Stored Secrets`
    - `Sources/GridOSApp/SettingsView.swift` contains `MacIntegrationsSettingsView()`
    - `Sources/GridOSApp/SettingsView.swift` contains `GridOSAppPreferences.defaultShowMenuBarExtra`
    - `Sources/GridOSApp/SettingsView.swift` contains `GridOSAppPreferences.defaultNotificationsEnabled`
    - `Sources/GridOSApp/SettingsView.swift` contains `GridOSAppPreferences.defaultIndexWorkspaceMetadata`
    - `! rg 'secret|api key|API key' Sources/GridOSApp/MacIntegrationsSettingsView.swift | rg 'Text\\('`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg 'macOS Integrations|Show Menu Bar Extra|Notify when long-running work finishes|Enable Notifications|Manage Stored Secrets|Index saved workspace metadata' Sources/GridOSApp Sources/GridOSKit &amp;&amp; ! rg 'AppStorage.*api|UserDefaults.*api|Text\\(.*sk-' Sources/GridOSApp Sources/GridOSKit</automated>
  </verify>
  <done>Settings exposes the Phase 8 controls compactly, with privacy copy and no secret disclosure surface.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
rg 'MenuBarExtra|showMenuBarExtra|Open gridOS|Host Status|Recent Directories|macOS Integrations|Manage Stored Secrets' Sources/GridOSApp Sources/Integrations Sources/GridOSKit docs
! rg 'runInActivePane|insertInActivePane|TerminalInteractionController|send\\(' Sources/GridOSApp/MenuBarExtraView.swift Sources/GridOSApp/MacIntegrationsController.swift
git diff --check
```
</verification>
