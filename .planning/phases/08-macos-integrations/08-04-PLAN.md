---
phase: 08-macos-integrations
plan: 04
title: Metadata-only indexing foundation and final evidence
status: pending
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - Sources/Integrations/WorkspaceMetadataIndexer.swift
  - Sources/GridOSApp/MacIntegrationsSettingsView.swift
  - Tests/IntegrationsTests/WorkspaceMetadataIndexerTests.swift
  - docs/architecture.md
  - docs/release.md
  - .planning/phases/08-macos-integrations/evidence/README.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: ["PHASE-08"]
requirements_addressed: ["D-02", "D-03", "D-04", "D-06", "D-10", "D-15", "D-16", "D-18", "D-19", "D-20"]
must_haves:
  truths:
    - "Core Spotlight support is opt-in and indexes only workspace labels and directory basenames."
    - "Terminal output, command history, generated commands, prompts, secrets, environment variables, process args, and full paths are not indexed."
    - "Quick Look/Finder document extension work is explicitly deferred because gridOS has no saved workspace document type yet."
    - "Final Phase 8 evidence records automated gates, smoke commands, manual checkpoints, and privacy source gates."
  artifacts:
    - path: "Sources/Integrations/WorkspaceMetadataIndexer.swift"
      provides: "Metadata-only Core Spotlight indexing foundation"
      contains: "WorkspaceSearchMetadata"
    - path: "Tests/IntegrationsTests/WorkspaceMetadataIndexerTests.swift"
      provides: "Sanitization and Core Spotlight attribute mapping tests"
      contains: "testMetadataUsesDirectoryBasenameOnly"
    - path: ".planning/phases/08-macos-integrations/evidence/README.md"
      provides: "Phase 8 verification evidence log"
      contains: "Phase 8 final gate"
  key_links:
    - from: "Sources/GridOSApp/MacIntegrationsSettingsView.swift"
      to: "Sources/Integrations/WorkspaceMetadataIndexer.swift"
      via: "Settings toggle controls metadata-only indexing"
      pattern: "indexWorkspaceMetadata"
---

<objective>
Add the optional metadata-only indexing foundation, document Quick Look deferral, and record Phase 8 final evidence.

Purpose: Phase 8 should leave gridOS Mac-native without leaking terminal/private data into Spotlight or overbuilding a Finder extension before there is a document model.
Output: Core Spotlight metadata adapter, opt-in Settings link, sanitization tests, docs, evidence, and planning-state updates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-macos-integrations/08-CONTEXT.md
@.planning/phases/08-macos-integrations/08-RESEARCH.md
@.planning/phases/08-macos-integrations/08-VALIDATION.md
@.planning/phases/08-macos-integrations/08-UI-SPEC.md
@Sources/Integrations/MacIntegrationModels.swift
@Sources/GridOSApp/MacIntegrationsSettingsView.swift
@Sources/TerminalCore/TerminalWorkspacePersistence.swift
@docs/architecture.md
@docs/release.md
</context>

<tasks>

<task id="08-04-01" type="auto" tdd="true">
  <title>Add opt-in Core Spotlight metadata indexer</title>
  <name>Add opt-in Core Spotlight metadata indexer</name>
  <files>Sources/Integrations/WorkspaceMetadataIndexer.swift, Sources/GridOSApp/MacIntegrationsSettingsView.swift, Tests/IntegrationsTests/WorkspaceMetadataIndexerTests.swift, docs/architecture.md</files>
  <read_first>Sources/Integrations/MacIntegrationModels.swift, Sources/GridOSApp/MacIntegrationsSettingsView.swift, Sources/TerminalCore/TerminalWorkspacePersistence.swift, .planning/phases/08-macos-integrations/08-CONTEXT.md, .planning/phases/08-macos-integrations/08-RESEARCH.md, .planning/phases/08-macos-integrations/08-UI-SPEC.md</read_first>
  <action>Create `Sources/Integrations/WorkspaceMetadataIndexer.swift` importing `CoreSpotlight`, `CoreServices`, and `Foundation`. Add `public struct WorkspaceSearchMetadata: Equatable, Sendable, Identifiable` with fields `id: String`, `displayName: String`, and `directoryBasename: String`. Add initializer `init(id:displayName:directoryPath:)` that trims displayName, uses `URL(fileURLWithPath: directoryPath).lastPathComponent` for `directoryBasename`, never stores the full directory path, and falls back to `Workspace` and `Home` for empty values. Add `public protocol WorkspaceMetadataIndexClient: Sendable` with `index(_ items: [CSSearchableItem]) async throws` and `deleteAll(domainIdentifier:) async throws`. Add `public struct CoreSpotlightWorkspaceMetadataIndexClient` wrapping `CSSearchableIndex.default()`. Add `public struct WorkspaceMetadataIndexer: Sendable` with domain identifier `com.aaldere1.gridos.workspace-metadata`, methods `searchableItem(for:)`, `index(_:) async throws`, and `deleteAll() async throws`. `searchableItem(for:)` must set `CSSearchableItem.uniqueIdentifier` from metadata id, `domainIdentifier` from the domain constant, `attributeSet.title` from displayName, `attributeSet.displayName` from directoryBasename, and no contentDescription/body containing terminal text. Add tests proving full paths are stripped, blank values fall back, Core Spotlight items use the exact domain identifier, and no strings named `shellHistory`, `commandOutput`, `terminalTranscript`, `environmentVariables`, `processArgs`, `prompt`, or `generatedCommand` appear in the metadata source. Keep the Settings toggle `Index saved workspace metadata` off by default and leave live indexing opt-in only.</action>
  <acceptance_criteria>
    - `Sources/Integrations/WorkspaceMetadataIndexer.swift` contains `import CoreSpotlight`
    - `Sources/Integrations/WorkspaceMetadataIndexer.swift` contains `public struct WorkspaceSearchMetadata`
    - `Sources/Integrations/WorkspaceMetadataIndexer.swift` contains `directoryBasename`
    - `Sources/Integrations/WorkspaceMetadataIndexer.swift` contains `public protocol WorkspaceMetadataIndexClient`
    - `Sources/Integrations/WorkspaceMetadataIndexer.swift` contains `CSSearchableItem`
    - `Sources/Integrations/WorkspaceMetadataIndexer.swift` contains `com.aaldere1.gridos.workspace-metadata`
    - `Sources/Integrations/WorkspaceMetadataIndexer.swift` contains `deleteAll`
    - `Tests/IntegrationsTests/WorkspaceMetadataIndexerTests.swift` contains `testMetadataUsesDirectoryBasenameOnly`
    - `Tests/IntegrationsTests/WorkspaceMetadataIndexerTests.swift` contains `testSearchableItemUsesWorkspaceMetadataDomain`
    - `Sources/GridOSApp/MacIntegrationsSettingsView.swift` contains `Index saved workspace metadata`
    - `Sources/GridOSApp/MacIntegrationsSettingsView.swift` contains `Terminal output and command history are never indexed.`
    - `docs/architecture.md` contains `Quick Look`
    - `docs/architecture.md` contains `deferred`
    - `! rg 'shellHistory|commandOutput|terminalTranscript|environmentVariables|processArgs|generatedCommand' Sources/Integrations/WorkspaceMetadataIndexer.swift Tests/IntegrationsTests/WorkspaceMetadataIndexerTests.swift`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:IntegrationsTests/WorkspaceMetadataIndexerTests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:IntegrationsTests/WorkspaceMetadataIndexerTests &amp;&amp; rg 'CSSearchableItem|WorkspaceSearchMetadata|Index saved workspace metadata|Terminal output and command history are never indexed' Sources Tests docs .planning &amp;&amp; ! rg 'shellHistory|commandOutput|terminalTranscript|environmentVariables|processArgs|generatedCommand' Sources/Integrations/WorkspaceMetadataIndexer.swift Tests/IntegrationsTests/WorkspaceMetadataIndexerTests.swift</automated>
  </verify>
  <done>gridOS has an opt-in, metadata-only Spotlight foundation and an explicit Quick Look deferral.</done>
</task>

<task id="08-04-02" type="auto">
  <title>Record Phase 8 evidence and update planning state</title>
  <name>Record Phase 8 evidence and update planning state</name>
  <files>.planning/phases/08-macos-integrations/evidence/README.md, docs/release.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/phases/08-macos-integrations/08-CONTEXT.md, .planning/phases/08-macos-integrations/08-VALIDATION.md, docs/release.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Create `.planning/phases/08-macos-integrations/evidence/README.md` with sections `Phase 8 final gate`, `Menu bar smoke`, `Notification smoke`, `Keychain privacy proof`, `Metadata indexing proof`, `Manual checkpoints`, and `Known limitations`. Record the exact pass/fail output or concise observations from the final verification run. Include these commands exactly in the evidence file: `xcodegen generate --use-cache`; `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test`; `git diff --check`; `rg 'MenuBarExtra|showMenuBarExtra|Open gridOS|Host Status|Recent Directories' Sources Tests docs .planning`; `rg 'UNUserNotificationCenter|UNNotificationRequest|NotificationAuthorizationState|Enable Notifications|gridOS work finished' Sources Tests docs .planning`; `rg 'Keychain|kSecClassGenericPassword|kSecAttrAccessibleWhenUnlockedThisDeviceOnly|Manage Stored Secrets' Sources Tests docs .planning`; `rg 'CSSearchableItem|WorkspaceSearchMetadata|Index saved workspace metadata|Terminal output and command history are never indexed' Sources Tests docs .planning`; and `! rg 'shellHistory|commandOutput|terminalTranscript|environmentVariables|apiKey.*AppStorage|UserDefaults.*api|import SwiftTerm' Sources/GridOSApp Sources/GridOSKit Sources/CommandIntelligence Sources/TerminalCore Sources/SystemMetrics Sources/Integrations`. Update `docs/release.md` with Phase 8 manual smoke steps for menu bar, notification, and metadata-indexing privacy checks. Update `.planning/ROADMAP.md` Phase 8 status to complete only after gates pass and list four checked plan files. Update `.planning/STATE.md` current status, decisions, progress log, performance metrics if available, and next target Phase 9. Do not commit screenshots that reveal terminal content.</action>
  <acceptance_criteria>
    - `.planning/phases/08-macos-integrations/evidence/README.md` contains `Phase 8 final gate`
    - `.planning/phases/08-macos-integrations/evidence/README.md` contains `Menu bar smoke`
    - `.planning/phases/08-macos-integrations/evidence/README.md` contains `Notification smoke`
    - `.planning/phases/08-macos-integrations/evidence/README.md` contains `Keychain privacy proof`
    - `.planning/phases/08-macos-integrations/evidence/README.md` contains `Metadata indexing proof`
    - `.planning/phases/08-macos-integrations/evidence/README.md` contains `Known limitations`
    - `.planning/phases/08-macos-integrations/evidence/README.md` contains `xcodegen generate --use-cache`
    - `.planning/phases/08-macos-integrations/evidence/README.md` contains `MenuBarExtra|showMenuBarExtra|Open gridOS|Host Status|Recent Directories`
    - `docs/release.md` contains `Phase 8`
    - `docs/release.md` contains `--phase8-notification-smoke`
    - `.planning/ROADMAP.md` contains `Phase 8 - macOS integrations`
    - `.planning/ROADMAP.md` contains `Status: complete`
    - `.planning/ROADMAP.md` contains `08-04-PLAN.md`
    - `.planning/STATE.md` contains `Phase 08 verification passed`
    - `.planning/STATE.md` contains `Plan Phase 09: Performance hardening`
    - `git diff --check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodegen generate --use-cache &amp;&amp; xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; git diff --check &amp;&amp; rg 'MenuBarExtra|showMenuBarExtra|Open gridOS|Host Status|Recent Directories' Sources Tests docs .planning &amp;&amp; rg 'UNUserNotificationCenter|UNNotificationRequest|NotificationAuthorizationState|Enable Notifications|gridOS work finished' Sources Tests docs .planning &amp;&amp; rg 'Keychain|kSecClassGenericPassword|kSecAttrAccessibleWhenUnlockedThisDeviceOnly|Manage Stored Secrets' Sources Tests docs .planning &amp;&amp; rg 'CSSearchableItem|WorkspaceSearchMetadata|Index saved workspace metadata|Terminal output and command history are never indexed' Sources Tests docs .planning &amp;&amp; ! rg 'shellHistory|commandOutput|terminalTranscript|environmentVariables|apiKey.*AppStorage|UserDefaults.*api|import SwiftTerm' Sources/GridOSApp Sources/GridOSKit Sources/CommandIntelligence Sources/TerminalCore Sources/SystemMetrics Sources/Integrations</automated>
  </verify>
  <done>Phase 8 has complete evidence, privacy gates, release docs, and planning-state handoff to Phase 9.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
xcodegen generate --use-cache
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
git diff --check
rg 'MenuBarExtra|showMenuBarExtra|Open gridOS|Host Status|Recent Directories' Sources Tests docs .planning
rg 'UNUserNotificationCenter|UNNotificationRequest|NotificationAuthorizationState|Enable Notifications|gridOS work finished' Sources Tests docs .planning
rg 'Keychain|kSecClassGenericPassword|kSecAttrAccessibleWhenUnlockedThisDeviceOnly|Manage Stored Secrets' Sources Tests docs .planning
rg 'CSSearchableItem|WorkspaceSearchMetadata|Index saved workspace metadata|Terminal output and command history are never indexed' Sources Tests docs .planning
! rg 'shellHistory|commandOutput|terminalTranscript|environmentVariables|apiKey.*AppStorage|UserDefaults.*api|import SwiftTerm' Sources/GridOSApp Sources/GridOSKit Sources/CommandIntelligence Sources/TerminalCore Sources/SystemMetrics Sources/Integrations
```
</verification>
