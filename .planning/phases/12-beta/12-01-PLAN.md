---
phase: 12-beta
plan: 01
title: Beta notarization preflight and evidence policy
status: complete
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/beta-notarization-preflight.sh
  - docs/release.md
  - .planning/phases/12-beta/evidence/README.md
autonomous: true
requirements: ["PHASE-12"]
requirements_addressed: ["BETA-01", "BETA-02", "BETA-03"]
must_haves:
  truths:
    - "Phase 12 starts with presence-only Beta signing and notary preflight evidence before attempting notarization."
    - "Missing Apple signing or notary configuration is recorded as BETA_NOTARIZATION_BLOCKED with missing input names only."
    - "Beta evidence rules prohibit committing build artifacts, private terminal data, raw logs, shell history, environment dumps, API keys, prompts, generated commands, and user-specific paths."
  artifacts:
    - path: "scripts/beta-notarization-preflight.sh"
      provides: "Beta signing and notary preflight command"
      contains: "GRIDOS_BETA_PREFLIGHT"
    - path: ".planning/phases/12-beta/evidence/README.md"
      provides: "Beta evidence and blocker policy"
      contains: "BETA_NOTARIZATION_BLOCKED"
  key_links:
    - from: "docs/release.md"
      to: ".planning/phases/12-beta/evidence/README.md"
      via: "Release docs reference the Phase 12 beta evidence lane"
      pattern: "Phase 12 beta"
---

<objective>
Create the Beta notarization preflight and evidence policy.

Purpose: Beta should fail early and legibly when signing or notary prerequisites are missing, while preserving Alpha privacy guarantees in every committed evidence artifact.
Output: a local preflight script, release-doc Beta section, and evidence README.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-beta/12-CONTEXT.md
@.planning/phases/12-beta/12-RESEARCH.md
@.planning/phases/12-beta/12-VALIDATION.md
@.planning/phases/11-alpha/11-VERIFICATION.md
@docs/release.md
@docs/production-roadmap.md
@docs/security-privacy.md
@docs/privacy-data-inventory.md
@project.yml
</context>

<tasks>

<task id="12-01-01" type="auto">
  <title>Add Beta notarization preflight script</title>
  <name>Add Beta notarization preflight script</name>
  <files>scripts/beta-notarization-preflight.sh</files>
  <read_first>scripts/alpha-signing-preflight.sh, project.yml, docs/release.md, .planning/phases/12-beta/12-RESEARCH.md</read_first>
  <action>Create executable Bash script `scripts/beta-notarization-preflight.sh` with `set -euo pipefail`, `--dry-run`, and sanitized text output headed `GRIDOS_BETA_PREFLIGHT`. The script must check required tools (`xcodegen`, `xcodebuild`, `security`, `ditto`, `hdiutil`, `spctl`, `xcrun`) and verify `xcrun notarytool --version` plus `xcrun stapler --help` can run. It must read `PRODUCT_BUNDLE_IDENTIFIER`, `DEVELOPMENT_TEAM`, `CODE_SIGN_STYLE`, and `ENABLE_HARDENED_RUNTIME` from `project.yml`, check presence of `GRIDOS_DEVELOPMENT_TEAM`, `GRIDOS_SIGNING_IDENTITY`, `GRIDOS_NOTARY_PROFILE`, `GRIDOS_NOTARY_APPLE_ID`, `GRIDOS_NOTARY_PASSWORD`, `GRIDOS_NOTARY_TEAM_ID`, `GRIDOS_NOTARY_KEY_ID`, `GRIDOS_NOTARY_ISSUER_ID`, and `GRIDOS_NOTARY_KEY_PATH` without echoing values, and summarize code signing identities with `security find-identity -v -p codesigning` as present/missing only. If no signing identity/team or no notary credential mode is present, print `BETA_NOTARIZATION_BLOCKED` and missing input names only. Unless `--dry-run` is used, write the same sanitized report to `.planning/phases/12-beta/evidence/beta-notarization-preflight.txt`. Do not export private keys, inspect keychain item contents, print certificates in full, dump environment variables, or write raw command output.</action>
  <acceptance_criteria>
    - `scripts/beta-notarization-preflight.sh` contains `set -euo pipefail`
    - `scripts/beta-notarization-preflight.sh` contains `GRIDOS_BETA_PREFLIGHT`
    - `scripts/beta-notarization-preflight.sh` contains `BETA_NOTARIZATION_BLOCKED`
    - `scripts/beta-notarization-preflight.sh` contains `GRIDOS_NOTARY_PROFILE`
    - `scripts/beta-notarization-preflight.sh` contains `GRIDOS_NOTARY_APPLE_ID`
    - `scripts/beta-notarization-preflight.sh` contains `GRIDOS_NOTARY_KEY_ID`
    - `scripts/beta-notarization-preflight.sh` contains `xcrun notarytool`
    - `scripts/beta-notarization-preflight.sh` contains `xcrun stapler`
    - `scripts/beta-notarization-preflight.sh` contains `ENABLE_HARDENED_RUNTIME`
    - `bash -n scripts/beta-notarization-preflight.sh` exits 0
    - `scripts/beta-notarization-preflight.sh --dry-run` exits 0 on a normal developer machine without requiring notary credentials
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/beta-notarization-preflight.sh && scripts/beta-notarization-preflight.sh --dry-run && rg 'GRIDOS_BETA_PREFLIGHT|BETA_NOTARIZATION_BLOCKED|GRIDOS_NOTARY_PROFILE|GRIDOS_NOTARY_APPLE_ID|GRIDOS_NOTARY_KEY_ID|xcrun notarytool|xcrun stapler|ENABLE_HARDENED_RUNTIME' scripts/beta-notarization-preflight.sh && git diff --check</automated>
  </verify>
  <done>Beta preflight exists, is dry-run safe, and records notary absence as a first-class blocker.</done>
</task>

<task id="12-01-02" type="auto">
  <title>Create Beta evidence policy</title>
  <name>Create Beta evidence policy</name>
  <files>.planning/phases/12-beta/evidence/README.md, docs/release.md</files>
  <read_first>.planning/phases/12-beta/12-CONTEXT.md, .planning/phases/12-beta/12-RESEARCH.md, docs/release.md, docs/privacy-data-inventory.md</read_first>
  <action>Create `.planning/phases/12-beta/evidence/README.md` with heading `# Phase 12 beta evidence` and exact sections `## Notarization preflight`, `## Artifact build`, `## Notarization and stapling`, `## Gatekeeper UAT`, `## Update flow`, `## Feedback and diagnostics`, `## Blocker policy`, and `## Privacy boundaries`. State that committed evidence must be text-only and sanitized; build products stay outside git; signing/notary absence is recorded as `BETA_NOTARIZATION_BLOCKED`; Beta cannot be marked complete without notarized artifact evidence or an explicit user-approved blocker; and diagnostics/evidence must exclude shell history, terminal transcripts, environment variables, API keys, prompts, generated commands, raw terminal output, screenshots, traces, and user-specific paths. Update `docs/release.md` with a `## Phase 12 beta` section linking this evidence README, the Beta preflight script, future Beta build/notarize/verify scripts, Beta UAT, update manifest, feedback template, and known-issues workflow. Include the exact sentence `No artifacts committed: .app, .xcarchive, .dmg, .zip, .pkg, .trace, and screenshots stay out of source control.`</action>
  <acceptance_criteria>
    - `.planning/phases/12-beta/evidence/README.md` contains `# Phase 12 beta evidence`
    - `.planning/phases/12-beta/evidence/README.md` contains `## Notarization preflight`
    - `.planning/phases/12-beta/evidence/README.md` contains `## Gatekeeper UAT`
    - `.planning/phases/12-beta/evidence/README.md` contains `## Blocker policy`
    - `.planning/phases/12-beta/evidence/README.md` contains `BETA_NOTARIZATION_BLOCKED`
    - `.planning/phases/12-beta/evidence/README.md` contains `No artifacts committed`
    - `docs/release.md` contains `## Phase 12 beta`
    - `docs/release.md` contains `.planning/phases/12-beta/evidence/README.md`
    - `docs/release.md` contains `scripts/beta-notarization-preflight.sh`
  </acceptance_criteria>
  <verify>
    <automated>rg 'Phase 12 beta evidence|## Notarization preflight|## Gatekeeper UAT|## Blocker policy|BETA_NOTARIZATION_BLOCKED|No artifacts committed' .planning/phases/12-beta/evidence/README.md && rg '## Phase 12 beta|scripts/beta-notarization-preflight.sh|.planning/phases/12-beta/evidence/README.md' docs/release.md && git diff --check</automated>
  </verify>
  <done>Beta evidence rules and release-doc links are durable and privacy-safe.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
bash -n scripts/beta-notarization-preflight.sh
scripts/beta-notarization-preflight.sh --dry-run
rg 'GRIDOS_BETA_PREFLIGHT|BETA_NOTARIZATION_BLOCKED|GRIDOS_NOTARY_PROFILE|GRIDOS_NOTARY_APPLE_ID|GRIDOS_NOTARY_KEY_ID|xcrun notarytool|xcrun stapler|ENABLE_HARDENED_RUNTIME' scripts/beta-notarization-preflight.sh
rg 'Phase 12 beta evidence|Notarization preflight|Gatekeeper UAT|Blocker policy|No artifacts committed' .planning/phases/12-beta/evidence/README.md docs/release.md
git diff --check
```
</verification>
