---
phase: 12-beta
plan: 04
title: First-run privacy, support, and feedback loop
status: complete
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - Sources/GridOSKit/GridOSAppPreferences.swift
  - Sources/GridOSApp/BetaPrivacyDisclosureView.swift
  - Sources/GridOSApp/RootView.swift
  - Sources/GridOSApp/SettingsView.swift
  - Tests/GridOSKitTests/GridOSAppPreferencesTests.swift
  - .planning/phases/12-beta/BETA-FEEDBACK.md
  - .planning/phases/12-beta/KNOWN-ISSUES.md
  - docs/security-privacy.md
autonomous: true
requirements: ["PHASE-12"]
requirements_addressed: ["BETA-11", "BETA-12", "BETA-13"]
must_haves:
  truths:
    - "Beta users see first-run privacy facts before normal use without telemetry or automatic diagnostics upload."
    - "The support/feedback loop requests only sanitized diagnostics."
    - "The app remains useful without a provider key and privacy-sensitive features remain opt-in."
  artifacts:
    - path: "Sources/GridOSApp/BetaPrivacyDisclosureView.swift"
      provides: "First-run Beta privacy disclosure"
      contains: "Terminal sessions stay local to this Mac."
    - path: ".planning/phases/12-beta/BETA-FEEDBACK.md"
      provides: "Beta feedback template"
      contains: "Do not include"
    - path: ".planning/phases/12-beta/KNOWN-ISSUES.md"
      provides: "Beta known-issues tracker"
      contains: "Beta blocker"
  key_links:
    - from: "docs/security-privacy.md"
      to: ".planning/phases/12-beta/BETA-FEEDBACK.md"
      via: "Privacy docs link to Beta feedback boundaries"
      pattern: "BETA-FEEDBACK.md"
---

<objective>
Add Beta first-run trust UI and a sanitized support/feedback loop.

Purpose: External testers need clear privacy expectations and a safe way to report issues without exposing terminal data.
Output: first-run privacy disclosure, preference/tests, feedback template, known-issues tracker, and privacy-doc links.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-beta/12-CONTEXT.md
@.planning/phases/12-beta/12-RESEARCH.md
@.planning/phases/12-beta/12-VALIDATION.md
@.planning/phases/12-beta/12-UI-SPEC.md
@Sources/GridOSKit/GridOSAppPreferences.swift
@Sources/GridOSApp/RootView.swift
@Sources/GridOSApp/SettingsView.swift
@Sources/GridOSApp/CommandIntelligenceSettingsView.swift
@Sources/GridOSApp/MacIntegrationsSettingsView.swift
@Tests/GridOSKitTests/GridOSAppPreferencesTests.swift
@docs/security-privacy.md
</context>

<tasks>

<task id="12-04-01" type="auto">
  <title>Add first-run Beta privacy disclosure</title>
  <name>Add first-run Beta privacy disclosure</name>
  <files>Sources/GridOSKit/GridOSAppPreferences.swift, Sources/GridOSApp/BetaPrivacyDisclosureView.swift, Sources/GridOSApp/RootView.swift, Sources/GridOSApp/SettingsView.swift, Tests/GridOSKitTests/GridOSAppPreferencesTests.swift</files>
  <read_first>Sources/GridOSKit/GridOSAppPreferences.swift, Sources/GridOSApp/RootView.swift, Sources/GridOSApp/SettingsView.swift, Tests/GridOSKitTests/GridOSAppPreferencesTests.swift, .planning/phases/12-beta/12-UI-SPEC.md</read_first>
  <action>Add a persistent preference key named exactly `betaPrivacyDisclosureAccepted` to `GridOSAppPreferences`, defaulting to `false` and included in reset/default tests. Create `Sources/GridOSApp/BetaPrivacyDisclosureView.swift` containing a SwiftUI view named `BetaPrivacyDisclosureView` with the exact strings `Terminal sessions stay local to this Mac.`, `Command Intelligence is opt-in and sends context only after preview approval.`, `API keys are stored in Keychain.`, `Risky commands are inserted for review instead of run automatically.`, `Notifications and workspace indexing are off until you enable them.`, and `Diagnostics are local, sanitized, and user-reviewed.` Wire `RootView` so the disclosure appears once when `betaPrivacyDisclosureAccepted` is false, with actions `Continue` and `Open Privacy Settings`. Extend `SettingsView` with a privacy/support section that can reopen the disclosure. Do not add telemetry, diagnostics upload, crash upload, or network calls.</action>
  <acceptance_criteria>
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `betaPrivacyDisclosureAccepted`
    - `Sources/GridOSApp/BetaPrivacyDisclosureView.swift` contains `struct BetaPrivacyDisclosureView`
    - `Sources/GridOSApp/BetaPrivacyDisclosureView.swift` contains `Terminal sessions stay local to this Mac.`
    - `Sources/GridOSApp/BetaPrivacyDisclosureView.swift` contains `Command Intelligence is opt-in and sends context only after preview approval.`
    - `Sources/GridOSApp/BetaPrivacyDisclosureView.swift` contains `API keys are stored in Keychain.`
    - `Sources/GridOSApp/BetaPrivacyDisclosureView.swift` contains `Risky commands are inserted for review instead of run automatically.`
    - `Sources/GridOSApp/BetaPrivacyDisclosureView.swift` contains `Diagnostics are local, sanitized, and user-reviewed.`
    - `Tests/GridOSKitTests/GridOSAppPreferencesTests.swift` contains `betaPrivacyDisclosureAccepted`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test && rg 'betaPrivacyDisclosureAccepted|struct BetaPrivacyDisclosureView|Terminal sessions stay local to this Mac.|Command Intelligence is opt-in|API keys are stored in Keychain.|Risky commands are inserted|Diagnostics are local, sanitized, and user-reviewed.' Sources Tests && git diff --check</automated>
  </verify>
  <done>Beta first-run privacy disclosure is visible, persistent, test-covered, and local-only.</done>
</task>

<task id="12-04-02" type="auto">
  <title>Create Beta feedback and known-issues loop</title>
  <name>Create Beta feedback and known-issues loop</name>
  <files>.planning/phases/12-beta/BETA-FEEDBACK.md, .planning/phases/12-beta/KNOWN-ISSUES.md, docs/security-privacy.md, docs/beta-distribution.md</files>
  <read_first>.planning/phases/11-alpha/ALPHA-FEEDBACK.md, .planning/phases/11-alpha/KNOWN-ISSUES.md, docs/security-privacy.md, docs/beta-distribution.md</read_first>
  <action>Create `.planning/phases/12-beta/BETA-FEEDBACK.md` with sections `## Report template`, `## Include`, `## Do not include`, `## Sanitized diagnostics`, and `## Triage`. The template must ask for app version/build, artifact basename, macOS version, hardware class, install path category, sanitized steps, expected result, actual result, and whether the issue blocks install, launch, terminal correctness, Command Intelligence, update flow, or diagnostics. The `Do not include` section must list shell history, terminal transcripts, environment variables, API keys, prompts, generated commands, provider responses, screenshots with secrets, and private file paths. Create `.planning/phases/12-beta/KNOWN-ISSUES.md` with severity definitions, `Beta blocker`, `Production blocker`, owner, target phase, status, and evidence columns. Update `docs/security-privacy.md` and `docs/beta-distribution.md` to link the feedback template and state `No telemetry, crash upload, or automatic diagnostics upload is added in Phase 12.`</action>
  <acceptance_criteria>
    - `.planning/phases/12-beta/BETA-FEEDBACK.md` contains `## Report template`
    - `.planning/phases/12-beta/BETA-FEEDBACK.md` contains `## Do not include`
    - `.planning/phases/12-beta/BETA-FEEDBACK.md` contains `shell history`
    - `.planning/phases/12-beta/BETA-FEEDBACK.md` contains `terminal transcripts`
    - `.planning/phases/12-beta/KNOWN-ISSUES.md` contains `Beta blocker`
    - `.planning/phases/12-beta/KNOWN-ISSUES.md` contains `Production blocker`
    - `docs/security-privacy.md` contains `BETA-FEEDBACK.md`
    - `docs/beta-distribution.md` contains `No telemetry, crash upload, or automatic diagnostics upload is added in Phase 12.`
  </acceptance_criteria>
  <verify>
    <automated>rg '## Report template|## Do not include|shell history|terminal transcripts' .planning/phases/12-beta/BETA-FEEDBACK.md && rg 'Beta blocker|Production blocker' .planning/phases/12-beta/KNOWN-ISSUES.md && rg 'BETA-FEEDBACK.md|No telemetry, crash upload, or automatic diagnostics upload is added in Phase 12.' docs/security-privacy.md docs/beta-distribution.md && git diff --check</automated>
  </verify>
  <done>Beta testers have a safe feedback path and durable known-issues tracker.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test
rg 'betaPrivacyDisclosureAccepted|struct BetaPrivacyDisclosureView|Terminal sessions stay local to this Mac.|Command Intelligence is opt-in|API keys are stored in Keychain.|Diagnostics are local, sanitized, and user-reviewed.' Sources Tests docs
rg 'BETA-FEEDBACK.md|Beta blocker|No telemetry, crash upload, or automatic diagnostics upload is added in Phase 12.' .planning/phases/12-beta docs
git diff --check
```
</verification>
