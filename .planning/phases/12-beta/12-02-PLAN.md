---
phase: 12-beta
plan: 02
title: Notarized Beta artifact build, staple, and verification
status: complete
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - scripts/build-beta.sh
  - scripts/notarize-beta-artifact.sh
  - scripts/verify-beta-artifact.sh
  - docs/release.md
  - .planning/phases/12-beta/evidence/README.md
autonomous: true
requirements: ["PHASE-12"]
requirements_addressed: ["BETA-04", "BETA-05", "BETA-06", "BETA-07"]
must_haves:
  truths:
    - "The unsigned build/test gate remains the always-runnable baseline."
    - "The signed Beta build path is parameterized through local signing and notary variables and never commits build products."
    - "Artifact verification records code-signature status, notarization/stapling status, Gatekeeper assessment, app metadata, checksum, and blocker state."
  artifacts:
    - path: "scripts/build-beta.sh"
      provides: "Beta archive and DMG/ZIP package command"
      contains: "hdiutil create"
    - path: "scripts/notarize-beta-artifact.sh"
      provides: "Notary submission and stapling command"
      contains: "notarytool submit"
    - path: "scripts/verify-beta-artifact.sh"
      provides: "Beta artifact verification command"
      contains: "spctl --assess"
  key_links:
    - from: ".planning/phases/12-beta/evidence/README.md"
      to: "scripts/verify-beta-artifact.sh"
      via: "Evidence policy points to artifact verification"
      pattern: "scripts/verify-beta-artifact.sh"
---

<objective>
Create the Beta build, notarization, stapling, and artifact verification lane.

Purpose: Beta needs a repeatable path from clean source to a signed and notarized external artifact when local credentials exist, plus honest blocker evidence when they do not.
Output: build script, notarization script, verification script, release docs, and evidence-manifest hooks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-beta/12-CONTEXT.md
@.planning/phases/12-beta/12-RESEARCH.md
@.planning/phases/12-beta/12-VALIDATION.md
@.planning/phases/12-beta/evidence/README.md
@scripts/beta-notarization-preflight.sh
@scripts/build-alpha.sh
@scripts/verify-alpha-artifact.sh
@docs/release.md
@project.yml
</context>

<tasks>

<task id="12-02-01" type="auto">
  <title>Add signed Beta build and package script</title>
  <name>Add signed Beta build and package script</name>
  <files>scripts/build-beta.sh, docs/release.md, .planning/phases/12-beta/evidence/README.md</files>
  <read_first>scripts/beta-notarization-preflight.sh, scripts/build-alpha.sh, project.yml, docs/release.md, .planning/phases/12-beta/evidence/README.md</read_first>
  <action>Create executable Bash script `scripts/build-beta.sh` with `set -euo pipefail`. The script must run `scripts/beta-notarization-preflight.sh` first, run `xcodegen generate --use-cache`, archive the macOS app using `xcodebuild -project gridOS.xcodeproj -scheme gridOS -configuration Release -destination 'generic/platform=macOS' -archivePath "$OUTPUT_DIR/gridOS.xcarchive" DEVELOPMENT_TEAM="$GRIDOS_DEVELOPMENT_TEAM" CODE_SIGN_IDENTITY="$GRIDOS_SIGNING_IDENTITY" CODE_SIGN_STYLE=Manual archive`, and place artifacts only under `GRIDOS_BETA_OUTPUT_DIR` or default `build/beta`. It must create `gridOS-<version>-<build>-<commit>.zip` with `ditto -c -k --keepParent` and `gridOS-<version>-<build>-<commit>.dmg` with `hdiutil create -volname gridOS -srcfolder "$APP_PATH" -ov -format UDZO`. It must write `.planning/phases/12-beta/evidence/beta-artifact-manifest.md` with timestamp, source commit, bundle ID, version, build, ZIP basename, DMG basename, SHA-256 values, signing identity presence, hardened runtime setting, artifact path policy, and verification/notarization commands. It must reject output paths under `.planning`. Update docs with the build command and local-output-only rule.</action>
  <acceptance_criteria>
    - `scripts/build-beta.sh` contains `set -euo pipefail`
    - `scripts/build-beta.sh` contains `scripts/beta-notarization-preflight.sh`
    - `scripts/build-beta.sh` contains `xcodegen generate --use-cache`
    - `scripts/build-beta.sh` contains `xcodebuild`
    - `scripts/build-beta.sh` contains `archive`
    - `scripts/build-beta.sh` contains `GRIDOS_BETA_OUTPUT_DIR`
    - `scripts/build-beta.sh` contains `ditto -c -k --keepParent`
    - `scripts/build-beta.sh` contains `hdiutil create`
    - `scripts/build-beta.sh` contains `beta-artifact-manifest.md`
    - `bash -n scripts/build-beta.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/build-beta.sh && rg 'scripts/beta-notarization-preflight.sh|xcodegen generate --use-cache|xcodebuild|archive|GRIDOS_BETA_OUTPUT_DIR|ditto -c -k --keepParent|hdiutil create|beta-artifact-manifest.md' scripts/build-beta.sh docs/release.md .planning/phases/12-beta/evidence/README.md && git diff --check</automated>
  </verify>
  <done>Beta builds are scripted and artifacts stay outside source control.</done>
</task>

<task id="12-02-02" type="auto">
  <title>Add notarization and verification scripts</title>
  <name>Add notarization and verification scripts</name>
  <files>scripts/notarize-beta-artifact.sh, scripts/verify-beta-artifact.sh, docs/release.md, .planning/phases/12-beta/evidence/README.md</files>
  <read_first>scripts/beta-notarization-preflight.sh, scripts/build-beta.sh, scripts/verify-alpha-artifact.sh, docs/release.md, .planning/phases/12-beta/evidence/README.md</read_first>
  <action>Create executable Bash script `scripts/notarize-beta-artifact.sh` with `set -euo pipefail` and usage `scripts/notarize-beta-artifact.sh path/to/gridOS.dmg-or-zip-or-app`. It must reject artifacts under `.planning`, require one credential mode (`GRIDOS_NOTARY_PROFILE` preferred, or Apple ID/password/team, or API key/issuer/key path), run `xcrun notarytool submit "$ARTIFACT" --wait` with the selected credential arguments, parse the submission status, write `.planning/phases/12-beta/evidence/beta-notarization.md`, run `xcrun stapler staple "$ARTIFACT"` for `.dmg`, `.pkg`, and `.app` inputs, run `xcrun stapler validate "$ARTIFACT"` when stapling is attempted, and fetch a notary log to `.planning/phases/12-beta/evidence/beta-notarization-log.json` only on failure with secrets removed. Create executable Bash script `scripts/verify-beta-artifact.sh` with usage `scripts/verify-beta-artifact.sh path/to/gridOS.dmg-or-zip-or-app`; it must extract/mount artifacts safely, verify a single `gridOS.app`, run `codesign --verify --deep --strict --verbose=2`, capture sanitized `codesign -dv` metadata, read Info.plist version/build/bundle ID, compute SHA-256, run `xcrun stapler validate` where applicable, run `spctl --assess --type execute --verbose=4 "$APP_PATH"` for the app, write `.planning/phases/12-beta/evidence/beta-artifact-verification.md`, and print `BETA_ARTIFACT_VERIFICATION PASS|FAIL|BLOCKED`. Update docs with notarization and verification usage.</action>
  <acceptance_criteria>
    - `scripts/notarize-beta-artifact.sh` contains `set -euo pipefail`
    - `scripts/notarize-beta-artifact.sh` contains `notarytool submit`
    - `scripts/notarize-beta-artifact.sh` contains `--wait`
    - `scripts/notarize-beta-artifact.sh` contains `stapler staple`
    - `scripts/notarize-beta-artifact.sh` contains `stapler validate`
    - `scripts/verify-beta-artifact.sh` contains `set -euo pipefail`
    - `scripts/verify-beta-artifact.sh` contains `codesign --verify --deep --strict --verbose=2`
    - `scripts/verify-beta-artifact.sh` contains `spctl --assess`
    - `scripts/verify-beta-artifact.sh` contains `BETA_ARTIFACT_VERIFICATION`
    - `bash -n scripts/notarize-beta-artifact.sh` exits 0
    - `bash -n scripts/verify-beta-artifact.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/notarize-beta-artifact.sh && bash -n scripts/verify-beta-artifact.sh && rg 'notarytool submit|--wait|stapler staple|stapler validate|codesign --verify --deep --strict --verbose=2|spctl --assess|BETA_ARTIFACT_VERIFICATION' scripts/notarize-beta-artifact.sh scripts/verify-beta-artifact.sh docs/release.md .planning/phases/12-beta/evidence/README.md && git diff --check</automated>
  </verify>
  <done>Beta artifacts can be notarized, stapled, assessed, and verified with sanitized evidence.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
bash -n scripts/build-beta.sh
bash -n scripts/notarize-beta-artifact.sh
bash -n scripts/verify-beta-artifact.sh
rg 'GRIDOS_BETA_OUTPUT_DIR|hdiutil create|beta-artifact-manifest.md' scripts/build-beta.sh docs/release.md .planning/phases/12-beta/evidence/README.md
rg 'notarytool submit|stapler staple|stapler validate|spctl --assess|BETA_ARTIFACT_VERIFICATION' scripts/notarize-beta-artifact.sh scripts/verify-beta-artifact.sh docs/release.md .planning/phases/12-beta/evidence/README.md
git diff --check
```
</verification>
