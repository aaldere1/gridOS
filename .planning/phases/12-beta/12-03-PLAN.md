---
phase: 12-beta
plan: 03
title: Beta update channel and distribution manifest
status: pending
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - scripts/write-beta-release-manifest.sh
  - docs/beta-distribution.md
  - docs/release.md
  - .planning/phases/12-beta/BETA-UAT.md
  - .planning/phases/12-beta/beta-release-manifest.json
autonomous: true
requirements: ["PHASE-12"]
requirements_addressed: ["BETA-08", "BETA-09", "BETA-10"]
must_haves:
  truths:
    - "The minimum Phase 12 update mechanism is a documented manual Beta N to Beta N+1 flow backed by a versioned release manifest and checksum verification."
    - "The manifest contains artifact metadata only and no private paths, credentials, terminal transcripts, or raw logs."
    - "The update mechanism remains compatible with a future Sparkle migration."
  artifacts:
    - path: "scripts/write-beta-release-manifest.sh"
      provides: "Sanitized Beta release manifest writer"
      contains: "beta-release-manifest.json"
    - path: "docs/beta-distribution.md"
      provides: "Beta install, update, rollback, and feedback instructions"
      contains: "Update from Beta N to Beta N+1"
  key_links:
    - from: "docs/release.md"
      to: "docs/beta-distribution.md"
      via: "Release docs link to Beta distribution instructions"
      pattern: "docs/beta-distribution.md"
---

<objective>
Create the Beta release manifest and documented update flow.

Purpose: External Beta users need a deterministic install/update/rollback path before automatic update infrastructure exists.
Output: manifest writer, distribution guide, initial manifest template, and UAT update checklist.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-beta/12-CONTEXT.md
@.planning/phases/12-beta/12-RESEARCH.md
@.planning/phases/12-beta/12-VALIDATION.md
@scripts/build-beta.sh
@scripts/verify-beta-artifact.sh
@docs/release.md
@docs/production-roadmap.md
</context>

<tasks>

<task id="12-03-01" type="auto">
  <title>Add Beta release manifest writer</title>
  <name>Add Beta release manifest writer</name>
  <files>scripts/write-beta-release-manifest.sh, .planning/phases/12-beta/beta-release-manifest.json</files>
  <read_first>scripts/verify-beta-artifact.sh, .planning/phases/12-beta/evidence/beta-artifact-manifest.md, docs/release.md</read_first>
  <action>Create executable Bash script `scripts/write-beta-release-manifest.sh` with `set -euo pipefail` and usage `scripts/write-beta-release-manifest.sh path/to/gridOS.dmg-or-zip-or-app`. It must reject artifacts under `.planning`, compute `shasum -a 256`, extract or mount enough to read `CFBundleIdentifier`, `CFBundleShortVersionString`, and `CFBundleVersion`, include source commit from `git rev-parse --short HEAD`, and write `.planning/phases/12-beta/beta-release-manifest.json` with keys `channel`, `artifact`, `sha256`, `bundleIdentifier`, `version`, `build`, `sourceCommit`, `notarization`, `gatekeeper`, `releaseNotes`, `updateInstructions`, and `rollback`. The default manifest values for credential-dependent proof must be `notarization.status: "pending"` and `gatekeeper.status: "pending"` until `scripts/verify-beta-artifact.sh` evidence is available. The JSON must not contain absolute local paths. Add an initial placeholder manifest with `channel` equal to `beta` and `artifact.name` equal to `pending-local-artifact`.</action>
  <acceptance_criteria>
    - `scripts/write-beta-release-manifest.sh` contains `set -euo pipefail`
    - `scripts/write-beta-release-manifest.sh` contains `beta-release-manifest.json`
    - `scripts/write-beta-release-manifest.sh` contains `shasum -a 256`
    - `scripts/write-beta-release-manifest.sh` contains `CFBundleShortVersionString`
    - `scripts/write-beta-release-manifest.sh` contains `notarization`
    - `.planning/phases/12-beta/beta-release-manifest.json` contains `"channel": "beta"`
    - `.planning/phases/12-beta/beta-release-manifest.json` contains `"pending-local-artifact"`
    - `bash -n scripts/write-beta-release-manifest.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/write-beta-release-manifest.sh && rg 'beta-release-manifest.json|shasum -a 256|CFBundleShortVersionString|notarization' scripts/write-beta-release-manifest.sh && rg '\"channel\": \"beta\"|pending-local-artifact' .planning/phases/12-beta/beta-release-manifest.json && git diff --check</automated>
  </verify>
  <done>A sanitized release manifest can be created from a verified local Beta artifact.</done>
</task>

<task id="12-03-02" type="auto">
  <title>Document install, update, and rollback flow</title>
  <name>Document install, update, and rollback flow</name>
  <files>docs/beta-distribution.md, docs/release.md, .planning/phases/12-beta/BETA-UAT.md</files>
  <read_first>docs/release.md, docs/production-roadmap.md, .planning/phases/12-beta/12-CONTEXT.md, .planning/phases/12-beta/12-RESEARCH.md</read_first>
  <action>Create `docs/beta-distribution.md` with heading `# Beta distribution` and sections `## Scope`, `## Download`, `## Verify checksum`, `## Install`, `## First launch`, `## Update from Beta N to Beta N+1`, `## Rollback`, `## Feedback`, and `## Privacy boundaries`. State that Phase 12 uses a manual Beta update flow backed by `.planning/phases/12-beta/beta-release-manifest.json`, that future Sparkle/appcast work remains compatible but not required for this manual Beta lane, and that testers must not send shell history, terminal transcripts, environment variables, API keys, prompts, generated commands, provider responses, screenshots with secrets, or private file paths. Create `.planning/phases/12-beta/BETA-UAT.md` with rows for checksum verification, DMG open/copy, Finder launch, first-run privacy, Command Intelligence no-key state, diagnostics review, manual Beta N to Beta N+1 update, rollback, and feedback submission. Update `docs/release.md` to link `docs/beta-distribution.md`, `.planning/phases/12-beta/beta-release-manifest.json`, and `.planning/phases/12-beta/BETA-UAT.md`.</action>
  <acceptance_criteria>
    - `docs/beta-distribution.md` contains `# Beta distribution`
    - `docs/beta-distribution.md` contains `## Update from Beta N to Beta N+1`
    - `docs/beta-distribution.md` contains `## Rollback`
    - `docs/beta-distribution.md` contains `manual Beta update flow`
    - `.planning/phases/12-beta/BETA-UAT.md` contains `Clean Mac Gatekeeper UAT`
    - `.planning/phases/12-beta/BETA-UAT.md` contains `Update from Beta N to Beta N+1`
    - `.planning/phases/12-beta/BETA-UAT.md` contains `spctl --assess`
    - `docs/release.md` contains `docs/beta-distribution.md`
    - `docs/release.md` contains `beta-release-manifest.json`
  </acceptance_criteria>
  <verify>
    <automated>rg '# Beta distribution|## Update from Beta N to Beta N\\+1|## Rollback|manual Beta update flow' docs/beta-distribution.md && rg 'Clean Mac Gatekeeper UAT|Update from Beta N to Beta N\\+1|spctl --assess' .planning/phases/12-beta/BETA-UAT.md && rg 'docs/beta-distribution.md|beta-release-manifest.json|BETA-UAT.md' docs/release.md && git diff --check</automated>
  </verify>
  <done>Beta testers have a documented install, manual update, rollback, and feedback path.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
bash -n scripts/write-beta-release-manifest.sh
rg 'beta-release-manifest.json|shasum -a 256|CFBundleShortVersionString|notarization' scripts/write-beta-release-manifest.sh
rg '"channel": "beta"|pending-local-artifact' .planning/phases/12-beta/beta-release-manifest.json
rg '# Beta distribution|Update from Beta N to Beta N\+1|Rollback|manual Beta update flow' docs/beta-distribution.md .planning/phases/12-beta/BETA-UAT.md docs/release.md
git diff --check
```
</verification>
