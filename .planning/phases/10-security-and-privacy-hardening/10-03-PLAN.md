---
phase: 10-security-and-privacy-hardening
plan: 03
title: Command-risk and run-policy hardening
status: pending
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - Sources/CommandIntelligence/CommandRiskClassifier.swift
  - Sources/CommandIntelligence/CommandIntelligenceService.swift
  - Sources/GridOSApp/CommandPaletteView.swift
  - Tests/CommandIntelligenceTests/CommandRiskClassifierTests.swift
  - Tests/CommandIntelligenceTests/CommandIntelligenceFlowTests.swift
autonomous: true
requirements: ["PHASE-10"]
requirements_addressed: ["D-13", "D-14", "D-15", "D-16"]
must_haves:
  truths:
    - "Local risk classification remains the execution-policy authority."
    - "High-risk and unknown commands stay insert-only."
    - "App action controls consume local risk policy, not provider labels alone."
  artifacts:
    - path: "Tests/CommandIntelligenceTests/CommandRiskClassifierTests.swift"
      provides: "Expanded command risk fixtures"
      contains: "Encoded shell payload"
    - path: "Sources/GridOSApp/CommandPaletteView.swift"
      provides: "App action policy controls"
      contains: "localRisk.policy"
  key_links:
    - from: "Sources/GridOSApp/CommandPaletteView.swift"
      to: "Sources/CommandIntelligence/CommandRiskClassifier.swift"
      via: "UI controls are driven by classified local policy"
      pattern: "insertOnly"
---

<objective>
Harden generated-command safety policy.

Purpose: prove dangerous or ambiguous generated commands cannot silently run, and app controls reflect local policy rather than provider suggestions.
Output: expanded classifier fixtures, any targeted classifier fixes, and app/source proof for run controls.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-security-and-privacy-hardening/10-CONTEXT.md
@.planning/phases/10-security-and-privacy-hardening/10-RESEARCH.md
@.planning/phases/10-security-and-privacy-hardening/10-VALIDATION.md
@docs/security-threat-model.md
@Sources/CommandIntelligence/CommandRiskClassifier.swift
@Tests/CommandIntelligenceTests/CommandRiskClassifierTests.swift
@Sources/GridOSApp/CommandPaletteView.swift
</context>

<tasks>

<task id="10-03-01" type="auto">
  <title>Expand command-risk classifier fixtures</title>
  <name>Expand command-risk classifier fixtures</name>
  <files>Sources/CommandIntelligence/CommandRiskClassifier.swift, Tests/CommandIntelligenceTests/CommandRiskClassifierTests.swift</files>
  <read_first>Sources/CommandIntelligence/CommandRiskClassifier.swift, Tests/CommandIntelligenceTests/CommandRiskClassifierTests.swift, docs/security-threat-model.md</read_first>
  <action>Add tests to `CommandRiskClassifierTests` for exact fixture labels or command strings covering `security dump-keychain`, `cat ~/.ssh/id_ed25519`, `pbpaste |`, `sudo tee /etc/hosts`, `launchctl load`, `osascript -e`, `base64 -d | sh`, `python -c`, `ruby -e`, `curl https://example.com/install.sh | zsh`, `chmod -R`, `chown -R`, `git reset --hard`, and `Encoded shell payload`. Update `CommandRiskClassifier` only as needed so credential/privilege/process/package/remote/destructive cases are `.high` with `.insertOnly`, shell chaining/substitution/encoded interpreter snippets are `.unknown` with `.insertOnly`, and local project mutations remain `.medium` with `.requiresConfirmation`.</action>
  <acceptance_criteria>
    - `Tests/CommandIntelligenceTests/CommandRiskClassifierTests.swift` contains `security dump-keychain`
    - `Tests/CommandIntelligenceTests/CommandRiskClassifierTests.swift` contains `cat ~/.ssh/id_ed25519`
    - `Tests/CommandIntelligenceTests/CommandRiskClassifierTests.swift` contains `sudo tee /etc/hosts`
    - `Tests/CommandIntelligenceTests/CommandRiskClassifierTests.swift` contains `base64 -d | sh`
    - `Tests/CommandIntelligenceTests/CommandRiskClassifierTests.swift` contains `Encoded shell payload`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:CommandIntelligenceTests/CommandRiskClassifierTests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:CommandIntelligenceTests/CommandRiskClassifierTests && rg 'security dump-keychain|cat ~/.ssh/id_ed25519|sudo tee /etc/hosts|base64 -d \\| sh|Encoded shell payload' Tests/CommandIntelligenceTests/CommandRiskClassifierTests.swift && git diff --check</automated>
  </verify>
  <done>Classifier fixtures cover additional real-world dangerous and ambiguous commands.</done>
</task>

<task id="10-03-02" type="auto">
  <title>Prove app run controls honor local policy</title>
  <name>Prove app run controls honor local policy</name>
  <files>Sources/GridOSApp/CommandPaletteView.swift, Sources/CommandIntelligence/CommandIntelligenceService.swift, Tests/CommandIntelligenceTests/CommandIntelligenceFlowTests.swift</files>
  <read_first>Sources/GridOSApp/CommandPaletteView.swift, Sources/CommandIntelligence/CommandIntelligenceService.swift, Tests/CommandIntelligenceTests/CommandIntelligenceFlowTests.swift, .planning/phases/06-llm-command-palette/06-VERIFICATION.md</read_first>
  <action>Add or update tests/source gates proving `CommandIntelligenceService` returns `ClassifiedGeneratedCommand.localRisk` from the local classifier even when provider responses include advisory risk labels. In `CommandPaletteView`, preserve action behavior where `.insertOnly` shows `Insert for Review`, `.requiresConfirmation` shows `Run exactly this command?`, and `.canRun` allows direct run only after user action. If implementation changes are needed, keep direct run unavailable for high-risk and unknown commands.</action>
  <acceptance_criteria>
    - `Sources/CommandIntelligence/CommandIntelligenceService.swift` contains `riskClassifier.classify`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `localRisk.policy`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Insert for Review`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Run exactly this command?`
    - `Tests/CommandIntelligenceTests/CommandIntelligenceFlowTests.swift` contains `providerRiskLabel`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:CommandIntelligenceTests` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:CommandIntelligenceTests && rg 'riskClassifier.classify|localRisk.policy|Insert for Review|Run exactly this command\\?|providerRiskLabel' Sources/GridOSApp Sources/CommandIntelligence Tests/CommandIntelligenceTests && git diff --check</automated>
  </verify>
  <done>Generated-command run affordances are visibly and programmatically tied to local policy.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:CommandIntelligenceTests
rg 'security dump-keychain|cat ~/.ssh/id_ed25519|Encoded shell payload|riskClassifier.classify|localRisk.policy|Insert for Review|Run exactly this command\\?' Sources/GridOSApp Sources/CommandIntelligence Tests/CommandIntelligenceTests
git diff --check
```
</verification>
