---
phase: 10-security-and-privacy-hardening
plan: 01
title: Threat model and privacy inventory
status: pending
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/security-threat-model.md
  - docs/privacy-data-inventory.md
  - docs/security-privacy.md
  - docs/release.md
autonomous: true
requirements: ["PHASE-10"]
requirements_addressed: ["D-01", "D-02", "D-03", "D-04", "D-05", "D-06", "D-07", "D-08"]
must_haves:
  truths:
    - "Phase 10 starts from a concrete threat model for the current terminal-plus-LLM app."
    - "Phase 10 has a local data inventory covering stored, sent, indexed, notified, displayed, and evidence-captured data."
    - "Security and release docs link to the new threat model and privacy inventory."
  artifacts:
    - path: "docs/security-threat-model.md"
      provides: "Threat model"
      contains: "## Trust Boundaries"
    - path: "docs/privacy-data-inventory.md"
      provides: "Privacy data inventory"
      contains: "## Data Inventory"
  key_links:
    - from: "docs/security-privacy.md"
      to: "docs/security-threat-model.md"
      via: "Security posture references the Phase 10 threat model"
      pattern: "docs/security-threat-model.md"
---

<objective>
Create the Phase 10 source-of-truth docs for what gridOS must protect.

Purpose: downstream hardening needs concrete assets, trust boundaries, data classes, mitigations, and gaps before tests can be meaningfully expanded.
Output: threat model, privacy data inventory, and release/security doc links.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-security-and-privacy-hardening/10-CONTEXT.md
@.planning/phases/10-security-and-privacy-hardening/10-RESEARCH.md
@.planning/phases/10-security-and-privacy-hardening/10-VALIDATION.md
@docs/security-privacy.md
@docs/release.md
</context>

<tasks>

<task id="10-01-01" type="auto">
  <title>Create current-app threat model</title>
  <name>Create current-app threat model</name>
  <files>docs/security-threat-model.md</files>
  <read_first>.planning/phases/10-security-and-privacy-hardening/10-CONTEXT.md, docs/security-privacy.md, docs/architecture.md, docs/production-roadmap.md</read_first>
  <action>Create `docs/security-threat-model.md` with heading `# gridOS Threat Model`. Include exact sections `## Scope`, `## Assets`, `## Trust Boundaries`, `## Entry Points`, `## Abuse Cases`, `## Existing Mitigations`, `## Open Gaps`, and `## Verification Gates`. Cover these current app surfaces by name: `TerminalCore`, `CommandIntelligence`, `GridOSKit.KeychainCredentialStore`, `TerminalWorkspaceSnapshotStore`, `WorkspaceMetadataIndexer`, `LocalNotificationClient`, `MenuBarExtra`, `SystemMetrics`, `RenderCore`, `project.yml`, and `SwiftTerm`. Include at least these abuse-case rows: `Accidental LLM context leak`, `Prompt injection from terminal output`, `Dangerous provider command`, `API key stored outside Keychain`, `Workspace persistence captures private shell data`, `Spotlight indexes private terminal data`, `Notification exposes command output`, and `Hardened runtime incompatibility`.</action>
  <acceptance_criteria>
    - `docs/security-threat-model.md` contains `# gridOS Threat Model`
    - `docs/security-threat-model.md` contains `## Trust Boundaries`
    - `docs/security-threat-model.md` contains `Accidental LLM context leak`
    - `docs/security-threat-model.md` contains `Prompt injection from terminal output`
    - `docs/security-threat-model.md` contains `Dangerous provider command`
    - `docs/security-threat-model.md` contains `WorkspaceMetadataIndexer`
    - `docs/security-threat-model.md` contains `Hardened runtime incompatibility`
  </acceptance_criteria>
  <verify>
    <automated>rg '# gridOS Threat Model|## Trust Boundaries|Accidental LLM context leak|Prompt injection from terminal output|Dangerous provider command|WorkspaceMetadataIndexer|Hardened runtime incompatibility' docs/security-threat-model.md && git diff --check</automated>
  </verify>
  <done>Threat model exists with concrete assets, boundaries, abuse cases, mitigations, and gates.</done>
</task>

<task id="10-01-02" type="auto">
  <title>Create privacy data inventory and link docs</title>
  <name>Create privacy data inventory and link docs</name>
  <files>docs/privacy-data-inventory.md, docs/security-privacy.md, docs/release.md</files>
  <read_first>docs/security-threat-model.md, docs/security-privacy.md, docs/release.md, Sources/GridOSKit/GridOSAppPreferences.swift, Sources/TerminalCore/TerminalWorkspacePersistence.swift, Sources/Integrations/WorkspaceMetadataIndexer.swift, Sources/Integrations/LocalNotificationClient.swift</read_first>
  <action>Create `docs/privacy-data-inventory.md` with heading `# gridOS Privacy Data Inventory`. Include exact sections `## Data Inventory`, `## Data That Must Not Be Persisted`, `## Device Exit Points`, `## User Controls`, and `## Verification Gates`. The inventory table must include rows for `Provider API key`, `Provider/model preference`, `LLM approved preview payload`, `Generated command`, `Workspace session layout`, `Recent directories`, `Spotlight workspace metadata`, `Notification content`, `Menu bar status`, `System metrics snapshot`, `Visual install seed`, and `Performance evidence`. Update `docs/security-privacy.md` to link `docs/security-threat-model.md` and `docs/privacy-data-inventory.md`. Update `docs/release.md` with a `## Phase 10 security and privacy hardening` section listing the threat model, inventory, dependency review, hardened runtime check, and privacy gates.</action>
  <acceptance_criteria>
    - `docs/privacy-data-inventory.md` contains `# gridOS Privacy Data Inventory`
    - `docs/privacy-data-inventory.md` contains `Provider API key`
    - `docs/privacy-data-inventory.md` contains `LLM approved preview payload`
    - `docs/privacy-data-inventory.md` contains `Data That Must Not Be Persisted`
    - `docs/security-privacy.md` contains `docs/security-threat-model.md`
    - `docs/security-privacy.md` contains `docs/privacy-data-inventory.md`
    - `docs/release.md` contains `Phase 10 security and privacy hardening`
    - `docs/release.md` contains `privacy gates`
  </acceptance_criteria>
  <verify>
    <automated>rg 'gridOS Privacy Data Inventory|Provider API key|LLM approved preview payload|Data That Must Not Be Persisted' docs/privacy-data-inventory.md && rg 'docs/security-threat-model.md|docs/privacy-data-inventory.md' docs/security-privacy.md && rg 'Phase 10 security and privacy hardening|privacy gates' docs/release.md && git diff --check</automated>
  </verify>
  <done>Privacy inventory exists and release/security docs point to Phase 10 trust artifacts.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
rg 'gridOS Threat Model|Trust Boundaries|Abuse Cases|gridOS Privacy Data Inventory|Data Inventory|Phase 10 security and privacy hardening' docs/security-threat-model.md docs/privacy-data-inventory.md docs/security-privacy.md docs/release.md
git diff --check
```
</verification>
