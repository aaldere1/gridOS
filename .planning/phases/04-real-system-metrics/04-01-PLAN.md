---
phase: 04-real-system-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - project.yml
  - gridOS.xcodeproj/project.pbxproj
  - Sources/SystemMetrics/SystemMetricAvailability.swift
  - Sources/SystemMetrics/SystemMetricsSnapshot.swift
  - Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift
  - Sources/SystemMetrics/NativeSystemMetricsProvider.swift
  - Sources/SystemMetrics/SystemMetricsSampler.swift
  - Sources/GridOSApp/RootView.swift
  - Tests/SystemMetricsTests/SystemMetricsModelTests.swift
  - Tests/SystemMetricsTests/SystemMetricsSamplingPolicyTests.swift
  - Tests/SystemMetricsTests/SystemMetricsDeltaTests.swift
  - docs/architecture.md
  - docs/release.md
  - .planning/STATE.md
autonomous: true
requirements: ["PHASE-04"]
must_haves:
  truths:
    - "The app replaces Phase 3's placeholder system/activity panels with truthful local metrics."
    - "SystemMetrics exposes a sampler service for CPU, memory, disk, network, battery, thermal, and top-process data."
    - "Metric snapshots model available, stale, and unavailable states explicitly."
    - "Sampling cadence and backpressure policy live in SystemMetrics, not SwiftUI views."
    - "Normal metrics require no root permissions and do not send data to a network, LLM provider, telemetry sink, or persistent log."
    - "Terminal launch/input smoke checks still pass after metrics integration."
  artifacts:
    - path: "Sources/SystemMetrics/SystemMetricsSnapshot.swift"
      provides: "Typed snapshot model for all Phase 4 metrics"
      contains: "struct SystemMetricsSnapshot"
    - path: "Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift"
      provides: "Cadence, stale threshold, and backpressure contract"
      contains: "struct SystemMetricsSamplingPolicy"
    - path: "Sources/SystemMetrics/NativeSystemMetricsProvider.swift"
      provides: "Native macOS metric sampling implementation"
      contains: "host_processor_info"
    - path: "Sources/SystemMetrics/SystemMetricsSampler.swift"
      provides: "App-consumable sampler service boundary"
      contains: "protocol SystemMetricsSampler"
    - path: "Tests/SystemMetricsTests/SystemMetricsModelTests.swift"
      provides: "Snapshot/availability regression tests"
      contains: "SystemMetricsModelTests"
  key_links:
    - from: "Sources/SystemMetrics/SystemMetricsSampler.swift"
      to: "Sources/GridOSApp/RootView.swift"
      via: "SystemMetricsSnapshot consumed by app frame"
      pattern: "SystemMetricsSnapshot"
    - from: "Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift"
      to: "Sources/SystemMetrics/SystemMetricsSampler.swift"
      via: "Sampler owns cadence and backpressure"
      pattern: "SystemMetricsSamplingPolicy"
    - from: "Sources/SystemMetrics/NativeSystemMetricsProvider.swift"
      to: "Tests/SystemMetricsTests/SystemMetricsDeltaTests.swift"
      via: "Synthetic delta calculations are unit tested"
      pattern: "delta"
---

<objective>
Replace Phase 3's decorative system/activity placeholders with truthful, local, no-root system metrics while preserving terminal dominance and idle-friendly behavior.

Purpose: Phase 4 makes the system strip and activity panel useful without turning gridOS into a dashboard or compromising terminal correctness.
Output: `SystemMetrics` snapshot models, sampling/backpressure policy, native macOS metric providers, app-frame integration, tests, documentation, and launch-smoke evidence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-system-metrics/04-CONTEXT.md
@.planning/phases/04-real-system-metrics/04-RESEARCH.md
@.planning/phases/04-real-system-metrics/04-VALIDATION.md
@.planning/phases/04-real-system-metrics/04-UI-SPEC.md
@.planning/phases/03-production-app-frame/03-CONTEXT.md
@.planning/phases/03-production-app-frame/03-VERIFICATION.md
@docs/architecture.md
@docs/release.md
@project.yml
@Sources/SystemMetrics/SystemMetricsStatus.swift
@Sources/GridOSApp/RootView.swift
</context>

<tasks>

<task id="04-01-01" type="auto">
  <name>Create SystemMetrics model and test target foundation</name>
  <files>project.yml, gridOS.xcodeproj/project.pbxproj, Sources/SystemMetrics/SystemMetricAvailability.swift, Sources/SystemMetrics/SystemMetricsSnapshot.swift, Tests/SystemMetricsTests/SystemMetricsModelTests.swift</files>
  <read_first>project.yml, Sources/SystemMetrics/SystemMetricsStatus.swift, Sources/GridOSKit/FoundationModuleStatus.swift, Tests/GridOSKitTests/GridOSProductTests.swift, .planning/phases/04-real-system-metrics/04-CONTEXT.md, .planning/phases/04-real-system-metrics/04-VALIDATION.md</read_first>
  <action>Create a `SystemMetricsTests` target in `project.yml` with source path `Tests/SystemMetricsTests`, dependency `SystemMetrics`, `GENERATE_INFOPLIST_FILE: YES`, and product bundle identifier `com.aaldere1.gridos.SystemMetricsTests`. Add `SystemMetricsTests` to the `gridOS` scheme test targets. Create `Sources/SystemMetrics/SystemMetricAvailability.swift` with `public enum SystemMetricAvailability<Value: Equatable & Sendable>: Equatable, Sendable` cases `available(Value)`, `stale(Value, age: TimeInterval)`, and `unavailable(reason: String)`, plus public computed properties `isAvailable`, `isStale`, `unavailableReason`, and `displayState`. Create `Sources/SystemMetrics/SystemMetricsSnapshot.swift` with public Sendable/Equatable models: `SystemMetricsSnapshot`, `CPUMetrics`, `MemoryMetrics`, `DiskMetrics`, `NetworkMetrics`, `BatteryMetrics`, `ThermalMetrics`, `TopProcessMetrics`, and `SamplingState`. `SystemMetricsSnapshot` must contain `timestamp: Date`, `cpu`, `memory`, `disk`, `network`, `battery`, `thermal`, `topProcesses`, and `samplingState`. Add `Tests/SystemMetricsTests/SystemMetricsModelTests.swift` covering available/stale/unavailable state, explicit unavailable copy strings `Battery unavailable`, `Thermal unavailable`, `Network idle`, and `No process data`, and snapshot construction with all metric fields.</action>
  <verify>xcodegen generate --use-cache &amp;&amp; xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test</verify>
  <acceptance_criteria>
    - `project.yml` contains `SystemMetricsTests`
    - `project.yml` contains `Tests/SystemMetricsTests`
    - `project.yml` contains `com.aaldere1.gridos.SystemMetricsTests`
    - `Sources/SystemMetrics/SystemMetricAvailability.swift` contains `public enum SystemMetricAvailability`
    - `Sources/SystemMetrics/SystemMetricAvailability.swift` contains `case stale(Value, age: TimeInterval)`
    - `Sources/SystemMetrics/SystemMetricsSnapshot.swift` contains `public struct SystemMetricsSnapshot`
    - `Sources/SystemMetrics/SystemMetricsSnapshot.swift` contains `public struct TopProcessMetrics`
    - `Tests/SystemMetricsTests/SystemMetricsModelTests.swift` contains `SystemMetricsModelTests`
    - `Tests/SystemMetricsTests/SystemMetricsModelTests.swift` contains `Battery unavailable`
    - `Tests/SystemMetricsTests/SystemMetricsModelTests.swift` contains `Thermal unavailable`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>Phase 4 has a compiled metrics model foundation and XCTest target.</done>
</task>

<task id="04-01-02" type="auto">
  <name>Add sampling cadence and delta policy</name>
  <files>Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift, Sources/SystemMetrics/SystemMetricsSnapshot.swift, Tests/SystemMetricsTests/SystemMetricsSamplingPolicyTests.swift, Tests/SystemMetricsTests/SystemMetricsDeltaTests.swift</files>
  <read_first>Sources/SystemMetrics/SystemMetricsSnapshot.swift, .planning/phases/04-real-system-metrics/04-CONTEXT.md, .planning/phases/04-real-system-metrics/04-RESEARCH.md, .planning/phases/04-real-system-metrics/04-VALIDATION.md</read_first>
  <action>Create `Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift` with `public struct SystemMetricsSamplingPolicy: Equatable, Sendable` containing `fastCadence: TimeInterval = 1.0`, `slowCadence: TimeInterval = 10.0`, `backgroundCadence: TimeInterval = 5.0`, `fastStaleThreshold: TimeInterval = 3.0`, `slowStaleThreshold: TimeInterval = 30.0`, `static let defaultValue`, and methods `cadence(isActive: Bool, includesFastMetrics: Bool) -> TimeInterval` and `staleThreshold(includesFastMetrics: Bool) -> TimeInterval`. Add pure delta helpers in `SystemMetricsSnapshot.swift` or `SystemMetricsSamplingPolicy.swift`: `CPUMetrics.percent(user:system:idle:nice:)`, `NetworkMetrics.throughput(previousBytes:currentBytes:elapsed:)`, and `TopProcessMetrics.cpuPercent(previousTotalTime:currentTotalTime:elapsed:processorCount:)`. The helpers must clamp impossible negative values to 0 and cap percentages at 100 for host CPU and 100 * processorCount for processes. Add `SystemMetricsSamplingPolicyTests` and `SystemMetricsDeltaTests` covering the exact defaults, active/background cadence, fast/slow stale thresholds, CPU percent calculation, network byte-per-second calculation, top-process delta calculation, and negative-delta clamping.</action>
  <verify>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test</verify>
  <acceptance_criteria>
    - `Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift` contains `public struct SystemMetricsSamplingPolicy`
    - `Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift` contains `fastCadence: TimeInterval = 1.0`
    - `Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift` contains `slowCadence: TimeInterval = 10.0`
    - `Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift` contains `backgroundCadence: TimeInterval = 5.0`
    - `Tests/SystemMetricsTests/SystemMetricsSamplingPolicyTests.swift` contains `testDefaultCadences`
    - `Tests/SystemMetricsTests/SystemMetricsDeltaTests.swift` contains `testCPUPercentUsesDelta`
    - `Tests/SystemMetricsTests/SystemMetricsDeltaTests.swift` contains `testNetworkThroughputUsesDelta`
    - `Tests/SystemMetricsTests/SystemMetricsDeltaTests.swift` contains `testTopProcessCPUPercentUsesDelta`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>Sampling cadence, stale thresholds, and synthetic delta math are deterministic and tested.</done>
</task>

<task id="04-01-03" type="auto">
  <name>Implement native no-root metrics provider</name>
  <files>Sources/SystemMetrics/NativeSystemMetricsProvider.swift, Sources/SystemMetrics/SystemMetricsSnapshot.swift, Tests/SystemMetricsTests/SystemMetricsDeltaTests.swift</files>
  <read_first>Sources/SystemMetrics/SystemMetricsSnapshot.swift, Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift, .planning/phases/04-real-system-metrics/04-RESEARCH.md, .planning/phases/04-real-system-metrics/04-CONTEXT.md</read_first>
  <action>Create `Sources/SystemMetrics/NativeSystemMetricsProvider.swift` with `import Foundation`, `import Darwin`, and `import IOKit.ps`. Define `public struct NativeSystemMetricsProvider: Sendable` with public initializer and methods `readCPU(previous:)`, `readMemory()`, `readDisk(url:)`, `readNetwork(previous:elapsed:)`, `readBattery()`, `readThermal()`, and `readTopProcesses(previous:elapsed:limit:)`. Use native APIs as the primary path: `host_processor_info` with `PROCESSOR_CPU_LOAD_INFO` for CPU ticks, `host_statistics64` with `HOST_VM_INFO64` for memory, `URLResourceKey.volumeTotalCapacityKey`, `volumeAvailableCapacityKey`, and `volumeAvailableCapacityForImportantUsageKey` for disk, `getifaddrs` with AF_LINK `if_data` byte counters for network, `IOPSCopyPowerSourcesInfo`, `IOPSCopyPowerSourcesList`, and `IOPSGetPowerSourceDescription` for battery, `ProcessInfo.processInfo.thermalState` for thermal, and `proc_listpids`, `proc_pidinfo`, `proc_name`, and `PROC_PIDTASKINFO` for top processes. Do not shell out to `top`, `ps`, `vm_stat`, `netstat`, or Activity Monitor commands. Model battery and thermal absence as `.unavailable(reason: "Battery unavailable")` and `.unavailable(reason: "Thermal unavailable")`. Model zero network throughput as `.available(NetworkMetrics(... stateText: "Network idle" ...))`, not unavailable. Use process name, PID, CPU percent, and resident memory only; do not expose full command lines, arguments, environment, or paths.</action>
  <verify>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg "host_processor_info|host_statistics64|IOPSCopyPowerSourcesInfo|getifaddrs|proc_pidinfo|thermalState" Sources/SystemMetrics/NativeSystemMetricsProvider.swift</verify>
  <acceptance_criteria>
    - `Sources/SystemMetrics/NativeSystemMetricsProvider.swift` contains `public struct NativeSystemMetricsProvider`
    - `Sources/SystemMetrics/NativeSystemMetricsProvider.swift` contains `import IOKit.ps`
    - `Sources/SystemMetrics/NativeSystemMetricsProvider.swift` contains `host_processor_info`
    - `Sources/SystemMetrics/NativeSystemMetricsProvider.swift` contains `host_statistics64`
    - `Sources/SystemMetrics/NativeSystemMetricsProvider.swift` contains `IOPSCopyPowerSourcesInfo`
    - `Sources/SystemMetrics/NativeSystemMetricsProvider.swift` contains `getifaddrs`
    - `Sources/SystemMetrics/NativeSystemMetricsProvider.swift` contains `proc_pidinfo`
    - `Sources/SystemMetrics/NativeSystemMetricsProvider.swift` contains `thermalState`
    - `Sources/SystemMetrics/NativeSystemMetricsProvider.swift` does not contain `Process(`
    - `Sources/SystemMetrics/NativeSystemMetricsProvider.swift` does not contain `/bin/ps`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>SystemMetrics can collect local no-root metrics through native APIs with graceful unavailable states.</done>
</task>

<task id="04-01-04" type="auto">
  <name>Add app-consumable sampler service</name>
  <files>Sources/SystemMetrics/SystemMetricsSampler.swift, Sources/SystemMetrics/NativeSystemMetricsProvider.swift, Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift, Tests/SystemMetricsTests/SystemMetricsSamplingPolicyTests.swift</files>
  <read_first>Sources/SystemMetrics/NativeSystemMetricsProvider.swift, Sources/SystemMetrics/SystemMetricsSamplingPolicy.swift, Sources/SystemMetrics/SystemMetricsSnapshot.swift, .planning/phases/04-real-system-metrics/04-CONTEXT.md</read_first>
  <action>Create `Sources/SystemMetrics/SystemMetricsSampler.swift` with `public protocol SystemMetricsSampler: Sendable` exposing `func snapshot(isActive: Bool) async -> SystemMetricsSnapshot`. Add `public actor LiveSystemMetricsSampler: SystemMetricsSampler` that stores `NativeSystemMetricsProvider`, `SystemMetricsSamplingPolicy`, previous CPU/network/top-process samples, and last snapshot timestamp. Its `snapshot(isActive:)` must apply `SystemMetricsSamplingPolicy.cadence(isActive:includesFastMetrics:)` and return the previous snapshot marked stale when called before the cadence interval instead of resampling immediately. Add `public struct PreviewSystemMetricsSampler: SystemMetricsSampler` or `public enum SystemMetricsPreviewData` with a deterministic sample snapshot for SwiftUI previews and tests. The sampler must not persist snapshots to disk, call network APIs, or import `CommandIntelligence`.</action>
  <verify>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg "protocol SystemMetricsSampler|actor LiveSystemMetricsSampler|PreviewSystemMetrics" Sources/SystemMetrics</verify>
  <acceptance_criteria>
    - `Sources/SystemMetrics/SystemMetricsSampler.swift` contains `public protocol SystemMetricsSampler`
    - `Sources/SystemMetrics/SystemMetricsSampler.swift` contains `func snapshot(isActive: Bool) async -> SystemMetricsSnapshot`
    - `Sources/SystemMetrics/SystemMetricsSampler.swift` contains `public actor LiveSystemMetricsSampler`
    - `Sources/SystemMetrics/SystemMetricsSampler.swift` contains `SystemMetricsSamplingPolicy`
    - `Sources/SystemMetrics/SystemMetricsSampler.swift` contains `PreviewSystemMetrics`
    - `Sources/SystemMetrics/SystemMetricsSampler.swift` does not contain `URLSession`
    - `Sources/SystemMetrics/SystemMetricsSampler.swift` does not contain `CommandIntelligence`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>GridOSApp has a narrow async metrics sampler boundary with built-in cadence/backpressure.</done>
</task>

<task id="04-01-05" type="auto">
  <name>Wire real metrics into the app frame</name>
  <files>Sources/GridOSApp/RootView.swift, Sources/SystemMetrics/SystemMetricsSampler.swift, .planning/phases/04-real-system-metrics/04-UI-SPEC.md</files>
  <read_first>Sources/GridOSApp/RootView.swift, Sources/SystemMetrics/SystemMetricsSnapshot.swift, Sources/SystemMetrics/SystemMetricsSampler.swift, .planning/phases/04-real-system-metrics/04-UI-SPEC.md, .planning/phases/03-production-app-frame/03-VERIFICATION.md</read_first>
  <action>Update `Sources/GridOSApp/RootView.swift` to `import SystemMetrics`. Add `@State private var metricsSnapshot = SystemMetricsPreviewData.snapshot` and a private `let metricsSampler: any SystemMetricsSampler = LiveSystemMetricsSampler()`. Add an async sampling task on the root view using `.task` that repeatedly calls `await metricsSampler.snapshot(isActive: true)`, updates `metricsSnapshot` on the main actor, and sleeps for `metricsSnapshot.samplingState.nextRefreshAfter` seconds. Replace `SystemStripView()` with `SystemStripView(snapshot: metricsSnapshot)` showing compact labels `CPU`, `MEM`, `NET`, `BAT`, and `THERM`. Replace `ActivityContextPanel()` with `ActivityContextPanel(snapshot: metricsSnapshot)` showing a read-only top-process list. Include exact graceful copy strings from the UI spec: `Battery unavailable`, `Thermal unavailable`, `Network idle`, `No process data`, and `Stale`. Preserve `TerminalWorkspaceView` as the visually dominant region and do not add process action buttons, command intelligence affordances, or fake metric values. Add accessibility labels/values for the system strip summary and top-process panel, and hide decorative separators from accessibility.</action>
  <verify>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg "import SystemMetrics|SystemMetricsSnapshot|SystemStripView\\(snapshot:|ActivityContextPanel\\(snapshot:|Battery unavailable|Thermal unavailable|Network idle|No process data|Stale" Sources/GridOSApp/RootView.swift</verify>
  <acceptance_criteria>
    - `Sources/GridOSApp/RootView.swift` contains `import SystemMetrics`
    - `Sources/GridOSApp/RootView.swift` contains `SystemMetricsSnapshot`
    - `Sources/GridOSApp/RootView.swift` contains `SystemStripView(snapshot:`
    - `Sources/GridOSApp/RootView.swift` contains `ActivityContextPanel(snapshot:`
    - `Sources/GridOSApp/RootView.swift` contains `CPU`
    - `Sources/GridOSApp/RootView.swift` contains `MEM`
    - `Sources/GridOSApp/RootView.swift` contains `NET`
    - `Sources/GridOSApp/RootView.swift` contains `Battery unavailable`
    - `Sources/GridOSApp/RootView.swift` contains `Thermal unavailable`
    - `Sources/GridOSApp/RootView.swift` contains `No process data`
    - `Sources/GridOSApp/RootView.swift` contains `accessibilityLabel("System metrics")`
    - `Sources/GridOSApp/RootView.swift` contains `accessibilityLabel("Top processes")`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>The app frame displays real snapshot-driven metrics while preserving the terminal-first layout.</done>
</task>

<task id="04-01-06" type="auto">
  <name>Update docs and run final Phase 4 verification</name>
  <files>docs/architecture.md, docs/release.md, .planning/STATE.md, project.yml, gridOS.xcodeproj/project.pbxproj</files>
  <read_first>docs/architecture.md, docs/release.md, .planning/STATE.md, .planning/phases/04-real-system-metrics/04-VALIDATION.md, .planning/phases/04-real-system-metrics/04-RESEARCH.md</read_first>
  <action>Run `xcodegen generate --use-cache` after all source/test changes. Update `docs/architecture.md` with a `Phase 4 architecture target` section describing `SystemMetricsSnapshot`, `SystemMetricsSamplingPolicy`, `NativeSystemMetricsProvider`, `SystemMetricsSampler`, app-frame snapshot consumption, no-root native sampling, local-only behavior, and graceful unavailable states. Update `docs/release.md` with `Phase 4 metrics smoke` documenting launch with `--cmd "printf 'GRIDOS_PHASE4_SMOKE\n' > /tmp/gridos-phase4-smoke.txt; exit"`, build/test command, source checks, and idle CPU sampling after startup. Update `.planning/STATE.md` progress log with Phase 4 execution evidence. Run final verification: `xcodegen generate --use-cache`, `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test`, `git diff --check`, `rg "SystemMetricsSnapshot|SystemMetricsSampler|SamplingPolicy|Battery unavailable|Thermal unavailable|GRIDOS_PHASE4_SMOKE" Sources Tests docs .planning`, and launch smoke using the Debug app binary with the Phase 4 startup command. Verify `/tmp/gridos-phase4-smoke.txt` contains `GRIDOS_PHASE4_SMOKE` and the app quits cleanly.</action>
  <verify>xcodegen generate --use-cache &amp;&amp; xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; git diff --check</verify>
  <acceptance_criteria>
    - `docs/architecture.md` contains `Phase 4 architecture target`
    - `docs/architecture.md` contains `SystemMetricsSnapshot`
    - `docs/release.md` contains `Phase 4 metrics smoke`
    - `.planning/STATE.md` contains `Phase 4`
    - `project.yml` contains `SystemMetricsTests`
    - `/tmp/gridos-phase4-smoke.txt` contains `GRIDOS_PHASE4_SMOKE` after launch smoke
    - `git diff --check` exits 0
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>Phase 4 has generated project state, documentation, automated verification, and launch-smoke evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodegen generate --use-cache`
- [ ] `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test`
- [ ] `git diff --check`
- [ ] `rg "SystemMetricsSnapshot|SystemMetricsSampler|SystemMetricsSamplingPolicy|NativeSystemMetricsProvider|Battery unavailable|Thermal unavailable|Network idle|No process data" Sources Tests`
- [ ] Launch smoke writes `GRIDOS_PHASE4_SMOKE` to `/tmp/gridos-phase4-smoke.txt`
</verification>

<success_criteria>
- `SystemMetrics` sampler service exists and is consumed by `GridOSApp`.
- CPU, memory, disk, network, battery, thermal, and top-process data are represented in typed snapshots.
- Native no-root APIs are the primary sampling path.
- Sampling cadence/backpressure policy is implemented in `SystemMetrics`.
- The system strip and activity panel display real or gracefully unavailable data.
- Terminal focus and startup command smoke still work.
- Tests cover model availability states, cadence policy, and synthetic delta math.
- Documentation records Phase 4 architecture and smoke expectations.
</success_criteria>

<output>
After execution, create `.planning/phases/04-real-system-metrics/04-01-SUMMARY.md`.
</output>
