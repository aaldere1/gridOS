---
phase: 03-production-app-frame
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/GridOSKit/GridOSAppPreferences.swift
  - Tests/GridOSKitTests/GridOSAppPreferencesTests.swift
  - Sources/RenderCore/VisualEffectConfiguration.swift
  - Sources/RenderCore/MetalBackgroundView.swift
  - Tests/RenderCoreTests/RenderCoreModelTests.swift
  - Sources/GridOSApp/GridOSApp.swift
  - Sources/GridOSApp/RootView.swift
  - Sources/GridOSApp/SettingsView.swift
  - Sources/GridOSApp/WindowFrameController.swift
  - docs/architecture.md
  - docs/release.md
  - .planning/STATE.md
autonomous: true
requirements: ["PHASE-03"]
user_setup: []
must_haves:
  truths:
    - "The app opens into a terminal-first cockpit where terminal work remains visually dominant."
    - "Settings persist shell path, terminal font size, reduced motion, and visual intensity."
    - "Reduced motion changes RenderCore behavior by reducing or disabling Metal pulse animation."
    - "The main window uses AppKit frame autosave for crash-safe size and position restoration."
    - "Terminal copy, paste, clear, reset, launch, and keyboard input smoke checks still pass."
  artifacts:
    - path: "Sources/GridOSKit/GridOSAppPreferences.swift"
      provides: "Testable preference defaults and clamping"
      contains: "struct GridOSAppPreferences"
    - path: "Sources/RenderCore/VisualEffectConfiguration.swift"
      provides: "Reduced motion and visual intensity contract"
      contains: "struct VisualEffectConfiguration"
    - path: "Sources/GridOSApp/WindowFrameController.swift"
      provides: "NSWindow frame autosave bridge"
      contains: "setFrameAutosaveName"
    - path: "Sources/GridOSApp/SettingsView.swift"
      provides: "Persisted settings UI"
      contains: "@AppStorage"
    - path: "Tests/GridOSKitTests/GridOSAppPreferencesTests.swift"
      provides: "Preference model regression tests"
      contains: "GridOSAppPreferencesTests"
  key_links:
    - from: "Sources/GridOSApp/SettingsView.swift"
      to: "Sources/GridOSApp/RootView.swift"
      via: "Shared @AppStorage keys"
      pattern: "terminal[.]shellPath|terminal[.]fontSize|appearance[.]reducedMotion|appearance[.]visualIntensity"
    - from: "Sources/GridOSApp/RootView.swift"
      to: "Sources/TerminalCore/TerminalSessionConfiguration.swift"
      via: "Persisted terminal profile mapping"
      pattern: "TerminalSessionConfiguration[(]"
    - from: "Sources/GridOSApp/RootView.swift"
      to: "Sources/RenderCore/MetalBackgroundView.swift"
      via: "VisualEffectConfiguration"
      pattern: "VisualEffectConfiguration"
---

<objective>
Make gridOS feel like a coherent Mac app frame around the existing terminal and Metal renderer.

Purpose: Phase 3 converts the prototype shell into a production-shaped Mac app surface without adding later-phase metrics, LLM, or session-management capabilities.
Output: persisted app preferences, window frame restoration, terminal-first layout, reduced-motion-aware rendering, hardened menu/focus behavior, accessibility coverage, tests, and smoke documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-app-frame/03-CONTEXT.md
@.planning/phases/03-production-app-frame/03-RESEARCH.md
@.planning/phases/03-production-app-frame/03-UI-SPEC.md
@.planning/phases/03-production-app-frame/03-VALIDATION.md
@docs/architecture.md
@docs/release.md
@project.yml
@Sources/GridOSApp/GridOSApp.swift
@Sources/GridOSApp/RootView.swift
@Sources/GridOSApp/SettingsView.swift
@Sources/TerminalCore/TerminalSessionConfiguration.swift
@Sources/TerminalCore/TerminalCommandCenter.swift
@Sources/TerminalCore/TerminalSurface.swift
@Sources/RenderCore/MetalBackgroundView.swift
</context>

<tasks>

<task id="03-01-01" type="auto">
  <name>Create testable app preference model</name>
  <files>Sources/GridOSKit/GridOSAppPreferences.swift, Tests/GridOSKitTests/GridOSAppPreferencesTests.swift</files>
  <read_first>Sources/GridOSKit/FoundationModuleStatus.swift, Tests/GridOSKitTests/GridOSProductTests.swift, .planning/phases/03-production-app-frame/03-CONTEXT.md</read_first>
  <action>Create `Sources/GridOSKit/GridOSAppPreferences.swift` with `public struct GridOSAppPreferences: Equatable, Sendable`. Add these exact public members: `defaultShellPath = "/bin/zsh"`, `defaultTerminalFontSize = 13.0`, `defaultVisualIntensity = 0.65`, `fontSizeRange = 10.0...24.0`, `visualIntensityRange = 0.0...1.0`, stored properties `shellPath`, `terminalFontSize`, `visualIntensity`, `reducedMotion`, initializer `init(shellPath:terminalFontSize:visualIntensity:reducedMotion:)`, static functions `clampedFontSize(_:)` and `clampedVisualIntensity(_:)`, and `static let defaultValue`. The initializer must replace an empty shell path with `defaultShellPath` and clamp font size and visual intensity. Add `Tests/GridOSKitTests/GridOSAppPreferencesTests.swift` covering defaults, empty shell fallback, font-size clamping at 10 and 24, visual-intensity clamping at 0 and 1, and reduced-motion storage.</action>
  <verify>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test</verify>
  <acceptance_criteria>
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `public struct GridOSAppPreferences`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `defaultShellPath = "/bin/zsh"`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `fontSizeRange`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `visualIntensityRange`
    - `Tests/GridOSKitTests/GridOSAppPreferencesTests.swift` contains `GridOSAppPreferencesTests`
    - `Tests/GridOSKitTests/GridOSAppPreferencesTests.swift` contains `testFontSizeIsClamped`
    - `Tests/GridOSKitTests/GridOSAppPreferencesTests.swift` contains `testVisualIntensityIsClamped`
  </acceptance_criteria>
  <done>Preference defaults and clamping are testable outside the app target.</done>
</task>

<task id="03-01-02" type="auto">
  <name>Add reduced-motion and intensity contract to RenderCore</name>
  <files>Sources/RenderCore/VisualEffectConfiguration.swift, Sources/RenderCore/MetalBackgroundView.swift, Tests/RenderCoreTests/RenderCoreModelTests.swift</files>
  <read_first>Sources/RenderCore/MetalBackgroundView.swift, Sources/RenderCore/RenderEvent.swift, Tests/RenderCoreTests/RenderCoreModelTests.swift, .planning/phases/03-production-app-frame/03-UI-SPEC.md</read_first>
  <action>Create `Sources/RenderCore/VisualEffectConfiguration.swift` with `public struct VisualEffectConfiguration: Equatable, Sendable` containing `public static let defaultValue`, stored properties `intensity: Double` and `reducedMotion: Bool`, an initializer that clamps intensity to `0...1`, and `public func pulseMagnitude(for eventMagnitude: Double) -> Double` that returns `0` when `reducedMotion == true` and otherwise returns `clampedIntensity * clampedEventMagnitude`. Update `MetalBackgroundView` to accept `effectConfiguration: VisualEffectConfiguration = .defaultValue`. Store it in `MetalBackgroundView`, pass it to `Coordinator.update`, and have `MetalBackgroundRenderer.record(event:configuration:)` use `configuration.pulseMagnitude(for:)`. When reduced motion is true and the pulse magnitude is 0, draw a static frame but do not start a repeating animation timer. Add tests in `RenderCoreModelTests` for intensity clamping and reduced-motion pulse suppression.</action>
  <verify>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test</verify>
  <acceptance_criteria>
    - `Sources/RenderCore/VisualEffectConfiguration.swift` contains `public struct VisualEffectConfiguration`
    - `Sources/RenderCore/VisualEffectConfiguration.swift` contains `pulseMagnitude`
    - `Sources/RenderCore/MetalBackgroundView.swift` contains `effectConfiguration`
    - `Sources/RenderCore/MetalBackgroundView.swift` contains `record(event:configuration:)`
    - `Tests/RenderCoreTests/RenderCoreModelTests.swift` contains `testVisualEffectConfigurationClampsIntensity`
    - `Tests/RenderCoreTests/RenderCoreModelTests.swift` contains `testReducedMotionSuppressesPulseMagnitude`
  </acceptance_criteria>
  <done>RenderCore can be driven by app/user reduced-motion and visual-intensity settings.</done>
</task>

<task id="03-01-03" type="auto">
  <name>Add AppKit window frame restoration bridge</name>
  <files>Sources/GridOSApp/WindowFrameController.swift, Sources/GridOSApp/RootView.swift</files>
  <read_first>Sources/GridOSApp/GridOSApp.swift, Sources/GridOSApp/RootView.swift, .planning/phases/03-production-app-frame/03-RESEARCH.md</read_first>
  <action>Create `Sources/GridOSApp/WindowFrameController.swift` as an `NSViewRepresentable` that installs an invisible `NSView` and configures the containing `NSWindow` on the main actor. The controller must set `window.titleVisibility = .hidden`, `window.titlebarAppearsTransparent = true`, `window.minSize = NSSize(width: 960, height: 640)`, and `window.setFrameAutosaveName("gridOS.main")`. It must not call `makeFirstResponder` or otherwise steal terminal focus. Attach `WindowFrameController(autosaveName: "gridOS.main")` to the root frame through `.background(...)` or an equivalent invisible host in `RootView`.</action>
  <verify>rg "setFrameAutosaveName\\(\"gridOS.main\"\\)|titlebarAppearsTransparent|NSSize\\(width: 960, height: 640\\)" Sources/GridOSApp</verify>
  <acceptance_criteria>
    - `Sources/GridOSApp/WindowFrameController.swift` contains `struct WindowFrameController`
    - `Sources/GridOSApp/WindowFrameController.swift` contains `setFrameAutosaveName("gridOS.main")`
    - `Sources/GridOSApp/WindowFrameController.swift` contains `titlebarAppearsTransparent = true`
    - `Sources/GridOSApp/WindowFrameController.swift` contains `NSSize(width: 960, height: 640)`
    - `Sources/GridOSApp/RootView.swift` contains `WindowFrameController(autosaveName: "gridOS.main")`
  </acceptance_criteria>
  <done>Main window size and position have an AppKit autosave path without disrupting terminal focus.</done>
</task>

<task id="03-01-04" type="auto">
  <name>Refactor RootView into terminal-first production frame</name>
  <files>Sources/GridOSApp/RootView.swift</files>
  <read_first>Sources/GridOSApp/RootView.swift, Sources/TerminalCore/TerminalSessionConfiguration.swift, Sources/RenderCore/MetalBackgroundView.swift, Sources/GridOSKit/GridOSAppPreferences.swift, .planning/phases/03-production-app-frame/03-UI-SPEC.md</read_first>
  <action>Refactor `RootView` into a terminal-first cockpit while keeping all code local to `Sources/GridOSApp/RootView.swift` unless a helper file is clearly cleaner. Add `@AppStorage` values using these exact keys: `terminal.shellPath`, `terminal.fontSize`, `appearance.reducedMotion`, `appearance.visualIntensity`. Add `@Environment(\\.accessibilityReduceMotion)` and compute an effective reduced-motion value where either the system environment or app setting enables reduced motion. Build a `TerminalSessionConfiguration` from the process-argument configuration while overriding `shellPath` and `fontSize` from persisted preferences and preserving `startupCommand`. Pass `VisualEffectConfiguration(intensity: visualIntensity, reducedMotion: effectiveReducedMotion)` into `MetalBackgroundView`. Introduce private SwiftUI subviews named `AppFrameHeader`, `SystemStripView`, `ActivityContextPanel`, and `TerminalWorkspaceView`. Keep `TerminalWorkspaceView` visually dominant. System/activity panels must use placeholder copy from the UI-SPEC: `Systems ready` and `Activity`. Do not add fake CPU, memory, network, LLM, or command intelligence content.</action>
  <verify>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test</verify>
  <acceptance_criteria>
    - `Sources/GridOSApp/RootView.swift` contains `@AppStorage("terminal.shellPath")`
    - `Sources/GridOSApp/RootView.swift` contains `@AppStorage("terminal.fontSize")`
    - `Sources/GridOSApp/RootView.swift` contains `@AppStorage("appearance.reducedMotion")`
    - `Sources/GridOSApp/RootView.swift` contains `@AppStorage("appearance.visualIntensity")`
    - `Sources/GridOSApp/RootView.swift` contains `@Environment(\\.accessibilityReduceMotion)`
    - `Sources/GridOSApp/RootView.swift` contains `VisualEffectConfiguration(`
    - `Sources/GridOSApp/RootView.swift` contains `struct AppFrameHeader`
    - `Sources/GridOSApp/RootView.swift` contains `struct SystemStripView`
    - `Sources/GridOSApp/RootView.swift` contains `struct ActivityContextPanel`
    - `Sources/GridOSApp/RootView.swift` contains `struct TerminalWorkspaceView`
    - `Sources/GridOSApp/RootView.swift` contains `Systems ready`
    - `Sources/GridOSApp/RootView.swift` contains `Activity`
  </acceptance_criteria>
  <done>RootView has a production app-frame composition that preserves terminal dominance and routes persisted settings into terminal/render modules.</done>
</task>

<task id="03-01-05" type="auto">
  <name>Replace Settings placeholders with persisted controls</name>
  <files>Sources/GridOSApp/SettingsView.swift</files>
  <read_first>Sources/GridOSApp/SettingsView.swift, Sources/GridOSKit/GridOSAppPreferences.swift, .planning/phases/03-production-app-frame/03-UI-SPEC.md</read_first>
  <action>Replace placeholder `@State` values in `SettingsView` with persisted controls using the same exact `@AppStorage` keys as `RootView`: `terminal.shellPath`, `terminal.fontSize`, `appearance.reducedMotion`, and `appearance.visualIntensity`. Use grouped `Form` sections titled exactly `Terminal`, `Appearance`, and `Recovery`. The Terminal section must contain a `TextField("Shell", text: ...)` and a font-size `Stepper` or `Slider` constrained by `GridOSAppPreferences.fontSizeRange`. The Appearance section must contain a `Toggle("Reduce motion", isOn: ...)` and `Slider` for visual intensity constrained by `GridOSAppPreferences.visualIntensityRange`. The Recovery section must include explanatory copy that running shell processes are not restored and a `Button("Reset to Defaults")` that resets all four persisted values to `GridOSAppPreferences.defaultValue`. Add accessibility labels or values for shell, font size, reduced motion, visual intensity, and reset action.</action>
  <verify>rg "@AppStorage|Reset to Defaults|Reduce motion|Visual intensity|Recovery" Sources/GridOSApp/SettingsView.swift</verify>
  <acceptance_criteria>
    - `Sources/GridOSApp/SettingsView.swift` contains `@AppStorage("terminal.shellPath")`
    - `Sources/GridOSApp/SettingsView.swift` contains `@AppStorage("terminal.fontSize")`
    - `Sources/GridOSApp/SettingsView.swift` contains `@AppStorage("appearance.reducedMotion")`
    - `Sources/GridOSApp/SettingsView.swift` contains `@AppStorage("appearance.visualIntensity")`
    - `Sources/GridOSApp/SettingsView.swift` contains `Section("Terminal")`
    - `Sources/GridOSApp/SettingsView.swift` contains `Section("Appearance")`
    - `Sources/GridOSApp/SettingsView.swift` contains `Section("Recovery")`
    - `Sources/GridOSApp/SettingsView.swift` contains `Button("Reset to Defaults")`
    - `Sources/GridOSApp/SettingsView.swift` contains `accessibilityLabel`
    - `Sources/GridOSApp/SettingsView.swift` does not contain `@State private var shellPath`
  </acceptance_criteria>
  <done>Settings persist real values and expose the Phase 3 recovery/reduced-motion controls.</done>
</task>

<task id="03-01-06" type="auto">
  <name>Harden terminal command menu and focus policy</name>
  <files>Sources/GridOSApp/GridOSApp.swift, Sources/TerminalCore/TerminalCommandCenter.swift, Sources/TerminalCore/TerminalSurface.swift</files>
  <read_first>Sources/GridOSApp/GridOSApp.swift, Sources/TerminalCore/TerminalCommandCenter.swift, Sources/TerminalCore/TerminalSurface.swift, .planning/phases/03-production-app-frame/03-CONTEXT.md</read_first>
  <action>Keep terminal commands explicit and menu-driven. Refactor the Terminal command menu into a private `TerminalCommands: Commands` type in `GridOSApp.swift` if it improves clarity, but preserve these exact command labels and shortcuts: `Copy` with Command-C, `Paste` with Command-V, `Clear` with Command-K, and `Reset` with Option-Command-R. Do not add any single-key global shortcuts. Review `TerminalSurface` focus behavior after the RootView/window changes and preserve the existing first-responder behavior: the terminal should request first responder on move-to-window and after shell start. If any frame/panel additions interfere, adjust only app-shell focus plumbing; do not bypass SwiftTerm input routing.</action>
  <verify>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test</verify>
  <acceptance_criteria>
    - `Sources/GridOSApp/GridOSApp.swift` contains `CommandMenu("Terminal")` or `struct TerminalCommands`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Button("Copy")`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Button("Paste")`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Button("Clear")`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Button("Reset")`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.keyboardShortcut("c", modifiers: [.command])`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.keyboardShortcut("v", modifiers: [.command])`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.keyboardShortcut("k", modifiers: [.command])`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.keyboardShortcut("r", modifiers: [.command, .option])`
    - `Sources/TerminalCore/TerminalSurface.swift` contains `makeFirstResponder`
  </acceptance_criteria>
  <done>Menu command behavior remains terminal-safe after app-frame changes.</done>
</task>

<task id="03-01-07" type="auto">
  <name>Add accessibility labels and reduced-motion verification points</name>
  <files>Sources/GridOSApp/RootView.swift, Sources/GridOSApp/SettingsView.swift, Sources/RenderCore/MetalBackgroundView.swift, Sources/RenderCore/VisualEffectConfiguration.swift</files>
  <read_first>Sources/GridOSApp/RootView.swift, Sources/GridOSApp/SettingsView.swift, Sources/RenderCore/MetalBackgroundView.swift, .planning/phases/03-production-app-frame/03-UI-SPEC.md</read_first>
  <action>Add explicit accessibility coverage for non-textual app-frame controls and indicators. In `RootView`, add accessibility labels for the terminal workspace, system strip placeholder, activity panel placeholder, visual mode indicator, and any icon-only/symbol-only controls. Hide purely decorative shapes from accessibility with `.accessibilityHidden(true)`. In `SettingsView`, add accessibility labels and values for shell path, font size, reduced motion, visual intensity, and reset defaults. Ensure reduced-motion source paths are grep-verifiable through `accessibilityReduceMotion`, `reducedMotion`, and `VisualEffectConfiguration`. Do not add explanatory feature/tutorial copy to the app frame.</action>
  <verify>rg "accessibilityLabel|accessibilityValue|accessibilityHidden|accessibilityReduceMotion|VisualEffectConfiguration" Sources/GridOSApp Sources/RenderCore</verify>
  <acceptance_criteria>
    - `Sources/GridOSApp/RootView.swift` contains `accessibilityLabel("Terminal workspace")`
    - `Sources/GridOSApp/RootView.swift` contains `accessibilityLabel("System strip")`
    - `Sources/GridOSApp/RootView.swift` contains `accessibilityLabel("Activity panel")`
    - `Sources/GridOSApp/SettingsView.swift` contains `accessibilityLabel("Shell")`
    - `Sources/GridOSApp/SettingsView.swift` contains `accessibilityLabel("Font size")`
    - `Sources/GridOSApp/SettingsView.swift` contains `accessibilityLabel("Reduce motion")`
    - `Sources/GridOSApp/SettingsView.swift` contains `accessibilityLabel("Visual intensity")`
    - `Sources/GridOSApp/RootView.swift` contains `accessibilityReduceMotion`
    - `Sources/RenderCore/VisualEffectConfiguration.swift` contains `reducedMotion`
  </acceptance_criteria>
  <done>Phase 3 has concrete accessibility and reduced-motion implementation points.</done>
</task>

<task id="03-01-08" type="auto">
  <name>Regenerate project, update docs, and run final verification</name>
  <files>project.yml, gridOS.xcodeproj/project.pbxproj, gridOS.xcodeproj/xcshareddata/xcschemes/gridOS.xcscheme, docs/architecture.md, docs/release.md, .planning/STATE.md</files>
  <read_first>project.yml, docs/architecture.md, docs/release.md, .planning/STATE.md, .planning/phases/03-production-app-frame/03-VALIDATION.md</read_first>
  <action>If new test files require project changes, update `project.yml` first and run `xcodegen generate --use-cache`. Update `docs/architecture.md` with the Phase 3 app-frame architecture: persisted preferences, window autosave bridge, terminal-first layout, and reduced-motion render configuration. Update `docs/release.md` with Phase 3 smoke checks for settings persistence, window autosave, terminal launch/input, and reduced-motion/idle renderer behavior. Update `.planning/STATE.md` progress log with Phase 3 execution evidence after verification. Run final automated verification with `xcodegen generate --use-cache`, `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test`, and `git diff --check`. Run launch smoke using the Debug app binary and `--cmd "printf 'GRIDOS_PHASE3_SMOKE\n' > /tmp/gridos-phase3-smoke.txt; exit"`, then verify the file contains `GRIDOS_PHASE3_SMOKE` and the app quits cleanly.</action>
  <verify>xcodegen generate --use-cache &amp;&amp; xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; git diff --check</verify>
  <acceptance_criteria>
    - `docs/architecture.md` contains `Phase 3 architecture target`
    - `docs/architecture.md` contains `GridOSAppPreferences`
    - `docs/release.md` contains `Phase 3 app-frame smoke`
    - `.planning/STATE.md` contains `Phase 3`
    - `/tmp/gridos-phase3-smoke.txt` contains `GRIDOS_PHASE3_SMOKE` after launch smoke
    - `git diff --check` exits 0
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <done>Phase 3 has documentation, generated project state, automated verification, and launch-smoke evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodegen generate --use-cache`
- [ ] `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test`
- [ ] `git diff --check`
- [ ] `rg "setFrameAutosaveName|@AppStorage|VisualEffectConfiguration|accessibilityReduceMotion" Sources`
- [ ] Launch smoke writes `GRIDOS_PHASE3_SMOKE` to `/tmp/gridos-phase3-smoke.txt`
</verification>

<success_criteria>
- Persisted settings control shell path, terminal font size, reduced motion, and visual intensity.
- Root app frame is terminal-first and includes restrained system/activity placeholders without fake metrics or LLM UI.
- Window size and position restoration uses AppKit autosave.
- Reduced motion materially changes Metal pulse behavior.
- App commands remain terminal-safe and menu-visible.
- Accessibility labels/values exist for custom app-frame and settings controls.
- Automated build/tests and launch smoke pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-production-app-frame/03-01-SUMMARY.md`.
</output>
