---
phase: 11-alpha
plan: 05
title: Alpha final verification and Phase 12 handoff
status: pending
type: execute
wave: 4
depends_on: ["11-02", "11-03", "11-04"]
files_modified:
  - .planning/phases/11-alpha/11-VERIFICATION.md
  - .planning/phases/11-alpha/evidence/README.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - docs/release.md
autonomous: true
requirements: ["PHASE-11"]
requirements_addressed: ["D-24", "D-25", "D-26", "D-27", "D-28", "D-29", "D-30"]
must_haves:
  truths:
    - "Phase 11 is complete only with build/test, signing preflight, artifact verification or explicit signing blocker, UAT, known-issues, diagnostics, and privacy gates."
    - "The roadmap and state move to Phase 12 only after Alpha evidence is internally consistent."
    - "A signed internal build is preferred, but missing signing identity can only produce a documented blocker, not a false pass."
  artifacts:
    - path: ".planning/phases/11-alpha/11-VERIFICATION.md"
      provides: "Alpha verification report"
      contains: "Phase 11: Alpha Verification Report"
    - path: ".planning/STATE.md"
      provides: "Phase 12 handoff"
      contains: "Phase 12 - Beta"
  key_links:
    - from: ".planning/phases/11-alpha/11-VERIFICATION.md"
      to: ".planning/phases/11-alpha/KNOWN-ISSUES.md"
      via: "Verification report summarizes known blockers"
      pattern: "KNOWN-ISSUES.md"
---

<objective>
Finish Alpha with final verification and handoff.

Purpose: Alpha signoff should be a coherent evidence decision, not a collection of disconnected scripts and notes.
Output: verification report, final source/privacy gates, roadmap update, state update, and Phase 12 handoff.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-alpha/11-CONTEXT.md
@.planning/phases/11-alpha/11-RESEARCH.md
@.planning/phases/11-alpha/11-VALIDATION.md
@.planning/phases/11-alpha/evidence/README.md
@.planning/phases/11-alpha/ALPHA-UAT.md
@.planning/phases/11-alpha/KNOWN-ISSUES.md
@.planning/phases/11-alpha/DIAGNOSTICS.md
@docs/release.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="11-05-01" type="auto">
  <title>Create Alpha verification report</title>
  <name>Create Alpha verification report</name>
  <files>.planning/phases/11-alpha/11-VERIFICATION.md, .planning/phases/11-alpha/evidence/README.md, docs/release.md</files>
  <read_first>.planning/phases/11-alpha/evidence/README.md, .planning/phases/11-alpha/ALPHA-UAT.md, .planning/phases/11-alpha/KNOWN-ISSUES.md, .planning/phases/11-alpha/DIAGNOSTICS.md, docs/release.md</read_first>
  <action>Create `.planning/phases/11-alpha/11-VERIFICATION.md` with heading `# Phase 11: Alpha Verification Report`. Include a must-have checklist for unsigned build/test, signing preflight, signed internal build or explicit `SIGNING_BLOCKED`, artifact verification, DEBUG alpha smoke, daily-driver UAT, known-issues triage, diagnostics policy, privacy gates, and Phase 12 handoff. Include final commands exactly: `xcodegen generate --use-cache`; `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test`; `scripts/alpha-signing-preflight.sh`; `scripts/verify-alpha-artifact.sh path/to/gridOS.app`; `.planning/phases/11-alpha/run-alpha-uat.sh`; `git diff --check`; and `! rg 'shellHistory|terminalTranscript|environmentVariables|apiKey.*AppStorage|UserDefaults.*api|commandOutput|selectedOutput.*write|prompt.*write|\\.app|\\.xcarchive|\\.dmg|\\.zip|\\.pkg|\\.trace|\\.png' Sources Tests docs scripts .planning/phases/11-alpha/evidence`. The report must say whether Alpha is `PASS`, `BLOCKED`, or `FAIL`; if signing or UAT has a blocker, use `BLOCKED` and do not mark the roadmap complete. Update release docs and evidence README to point to the verification report.</action>
  <acceptance_criteria>
    - `.planning/phases/11-alpha/11-VERIFICATION.md` contains `Phase 11: Alpha Verification Report`
    - `.planning/phases/11-alpha/11-VERIFICATION.md` contains `Signed internal build`
    - `.planning/phases/11-alpha/11-VERIFICATION.md` contains `SIGNING_BLOCKED`
    - `.planning/phases/11-alpha/11-VERIFICATION.md` contains `Terminal correctness`
    - `.planning/phases/11-alpha/11-VERIFICATION.md` contains `KNOWN-ISSUES.md`
    - `.planning/phases/11-alpha/11-VERIFICATION.md` contains `scripts/alpha-signing-preflight.sh`
    - `.planning/phases/11-alpha/11-VERIFICATION.md` contains `scripts/verify-alpha-artifact.sh path/to/gridOS.app`
    - `.planning/phases/11-alpha/11-VERIFICATION.md` contains `run-alpha-uat.sh`
    - `.planning/phases/11-alpha/evidence/README.md` contains `11-VERIFICATION.md`
  </acceptance_criteria>
  <verify>
    <automated>rg 'Phase 11: Alpha Verification Report|Signed internal build|SIGNING_BLOCKED|Terminal correctness|KNOWN-ISSUES.md|scripts/alpha-signing-preflight.sh|scripts/verify-alpha-artifact.sh path/to/gridOS.app|run-alpha-uat.sh' .planning/phases/11-alpha/11-VERIFICATION.md && rg '11-VERIFICATION.md' .planning/phases/11-alpha/evidence/README.md docs/release.md && git diff --check</automated>
  </verify>
  <done>Alpha verification report exists and distinguishes pass, blocked, and fail states.</done>
</task>

<task id="11-05-02" type="auto">
  <title>Run final gates and update roadmap/state handoff</title>
  <name>Run final gates and update roadmap/state handoff</name>
  <files>.planning/phases/11-alpha/11-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/phases/11-alpha/11-VERIFICATION.md, .planning/phases/11-alpha/KNOWN-ISSUES.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Run final source gates. If and only if build/test, signing preflight, signed artifact verification or explicit accepted signing blocker, UAT, known-issues triage, diagnostics, and privacy gates are coherent, update `.planning/ROADMAP.md` Phase 11 status to `complete` and leave Phase 12 as pending. If signing or UAT is blocked, update Phase 11 status to `blocked` with a concise blocker note instead. Update `.planning/STATE.md` current status to either `Phase 11 verification passed` or `Phase 11 blocked`, add a progress-log entry, add Phase 11 performance metrics row after actual execution, add session handoff links for `11-VERIFICATION.md`, `ALPHA-UAT.md`, `KNOWN-ISSUES.md`, and `evidence/README.md`, and set next target to `Phase 12 - Beta` only on pass. Do not inflate `completed_phases` or `completed_plans` unless Phase 11 is genuinely passed.</action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains `Phase 11 - Alpha`
    - `.planning/ROADMAP.md` contains either `Status: complete` or `Status: blocked`
    - `.planning/STATE.md` contains either `Phase 11 verification passed` or `Phase 11 blocked`
    - `.planning/STATE.md` contains `11-VERIFICATION.md`
    - `.planning/STATE.md` contains `ALPHA-UAT.md`
    - `.planning/STATE.md` contains `KNOWN-ISSUES.md`
    - If `.planning/STATE.md` contains `Phase 12 - Beta`, `.planning/phases/11-alpha/11-VERIFICATION.md` must contain `PASS`
    - `git diff --check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg 'Phase 11 - Alpha|Status: complete|Status: blocked' .planning/ROADMAP.md && rg 'Phase 11 verification passed|Phase 11 blocked|11-VERIFICATION.md|ALPHA-UAT.md|KNOWN-ISSUES.md' .planning/STATE.md && git diff --check</automated>
  </verify>
  <done>Alpha is either signed off with Phase 12 handoff or honestly blocked with durable evidence.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
xcodegen generate --use-cache
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
scripts/alpha-signing-preflight.sh
scripts/verify-alpha-artifact.sh path/to/gridOS.app
.planning/phases/11-alpha/run-alpha-uat.sh
git diff --check
rg 'Phase 11: Alpha Verification Report|Signed internal build|Terminal correctness|KNOWN-ISSUES.md|Phase 12 - Beta' .planning/phases/11-alpha/11-VERIFICATION.md .planning/STATE.md
! rg 'shellHistory|terminalTranscript|environmentVariables|apiKey.*AppStorage|UserDefaults.*api|commandOutput|selectedOutput.*write|prompt.*write|\\.app|\\.xcarchive|\\.dmg|\\.zip|\\.pkg|\\.trace|\\.png' Sources Tests docs scripts .planning/phases/11-alpha/evidence
```
</verification>
