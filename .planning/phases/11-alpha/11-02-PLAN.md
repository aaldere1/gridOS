---
phase: 11-alpha
plan: 02
title: Internal alpha artifact build and verification
status: pending
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - scripts/build-alpha.sh
  - scripts/verify-alpha-artifact.sh
  - docs/release.md
  - .planning/phases/11-alpha/evidence/README.md
autonomous: true
requirements: ["PHASE-11"]
requirements_addressed: ["D-06", "D-07", "D-08", "D-09", "D-10"]
must_haves:
  truths:
    - "The unsigned build/test gate remains the always-runnable baseline."
    - "The signed alpha build path is parameterized through local signing variables and never commits build products."
    - "Artifact verification records code-signature status, app metadata, checksum, and whether notarization is intentionally deferred."
  artifacts:
    - path: "scripts/build-alpha.sh"
      provides: "Internal alpha archive/package command"
      contains: "xcodebuild archive"
    - path: "scripts/verify-alpha-artifact.sh"
      provides: "Alpha artifact verification command"
      contains: "codesign --verify --deep --strict --verbose=2"
  key_links:
    - from: ".planning/phases/11-alpha/evidence/README.md"
      to: "scripts/verify-alpha-artifact.sh"
      via: "Evidence policy points to artifact verification"
      pattern: "scripts/verify-alpha-artifact.sh"
---

<objective>
Create the internal alpha build and artifact verification lane.

Purpose: Alpha needs a repeatable path from clean source to a signed internal build when local credentials exist, plus honest verification evidence when they do not.
Output: build script, verification script, release docs, and evidence-manifest hooks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-alpha/11-CONTEXT.md
@.planning/phases/11-alpha/11-RESEARCH.md
@.planning/phases/11-alpha/11-VALIDATION.md
@.planning/phases/11-alpha/evidence/README.md
@docs/release.md
@docs/dependency-security-review.md
@project.yml
</context>

<tasks>

<task id="11-02-01" type="auto">
  <title>Add signed alpha build script</title>
  <name>Add signed alpha build script</name>
  <files>scripts/build-alpha.sh, docs/release.md, .planning/phases/11-alpha/evidence/README.md</files>
  <read_first>scripts/alpha-signing-preflight.sh, project.yml, docs/release.md, .planning/phases/11-alpha/evidence/README.md</read_first>
  <action>Create executable Bash script `scripts/build-alpha.sh` with `set -euo pipefail`. The script must run `scripts/alpha-signing-preflight.sh` first, run `xcodegen generate --use-cache`, then archive the macOS app using `xcodebuild -project gridOS.xcodeproj -scheme gridOS -configuration Release -destination 'generic/platform=macOS' -archivePath "$OUTPUT_DIR/gridOS.xcarchive" DEVELOPMENT_TEAM="$GRIDOS_DEVELOPMENT_TEAM" CODE_SIGN_IDENTITY="$GRIDOS_SIGNING_IDENTITY" CODE_SIGN_STYLE=Manual archive`. Use `GRIDOS_ALPHA_OUTPUT_DIR` when provided and default to an ignored local build output such as `build/alpha`. Copy or reference the built `.app` without placing it in `.planning`, create a zipped local artifact only under the output directory, and write a sanitized manifest to `.planning/phases/11-alpha/evidence/alpha-artifact-manifest.md` containing timestamp, source commit, version/build, artifact basename, checksum, signing identity presence, hardened runtime setting, and the verification command to run next. If signing variables are absent, stop before archive and preserve the preflight blocker evidence. Update `docs/release.md` and the evidence README with the build command and the rule that artifact paths must be local-output paths only.</action>
  <acceptance_criteria>
    - `scripts/build-alpha.sh` contains `set -euo pipefail`
    - `scripts/build-alpha.sh` contains `scripts/alpha-signing-preflight.sh`
    - `scripts/build-alpha.sh` contains `xcodegen generate --use-cache`
    - `scripts/build-alpha.sh` contains `xcodebuild`
    - `scripts/build-alpha.sh` contains `archive`
    - `scripts/build-alpha.sh` contains `GRIDOS_DEVELOPMENT_TEAM`
    - `scripts/build-alpha.sh` contains `GRIDOS_SIGNING_IDENTITY`
    - `scripts/build-alpha.sh` contains `alpha-artifact-manifest.md`
    - `bash -n scripts/build-alpha.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/build-alpha.sh && rg 'scripts/alpha-signing-preflight.sh|xcodegen generate --use-cache|xcodebuild|archive|GRIDOS_DEVELOPMENT_TEAM|GRIDOS_SIGNING_IDENTITY|alpha-artifact-manifest.md' scripts/build-alpha.sh docs/release.md .planning/phases/11-alpha/evidence/README.md && git diff --check</automated>
  </verify>
  <done>Internal alpha builds are scripted and signing blockers stop before ambiguous archives.</done>
</task>

<task id="11-02-02" type="auto">
  <title>Add artifact verification script</title>
  <name>Add artifact verification script</name>
  <files>scripts/verify-alpha-artifact.sh, docs/release.md, .planning/phases/11-alpha/evidence/README.md</files>
  <read_first>scripts/build-alpha.sh, docs/release.md, .planning/phases/11-alpha/evidence/README.md</read_first>
  <action>Create executable Bash script `scripts/verify-alpha-artifact.sh` with `set -euo pipefail` and usage `scripts/verify-alpha-artifact.sh path/to/gridOS.app-or-zip`. For an `.app`, run `codesign --verify --deep --strict --verbose=2 "$APP_PATH"`, capture sanitized `codesign -dv "$APP_PATH"` metadata, read `CFBundleShortVersionString`, `CFBundleVersion`, and `CFBundleIdentifier` from `Contents/Info.plist`, compute `shasum -a 256`, confirm the app bundle is not under `.planning`, and write `.planning/phases/11-alpha/evidence/alpha-artifact-verification.md`. For a `.zip`, compute checksum and verify it contains a single `gridOS.app` entry or require the caller to pass the extracted `.app`. The report must contain `Alpha artifact manifest`, `codesign --verify --deep --strict --verbose=2`, `Notarization: deferred to Phase 12`, checksum, bundle ID, version, build, and pass/fail. Update `docs/release.md` and the evidence README with verification usage.</action>
  <acceptance_criteria>
    - `scripts/verify-alpha-artifact.sh` contains `set -euo pipefail`
    - `scripts/verify-alpha-artifact.sh` contains `codesign --verify --deep --strict --verbose=2`
    - `scripts/verify-alpha-artifact.sh` contains `codesign -dv`
    - `scripts/verify-alpha-artifact.sh` contains `CFBundleShortVersionString`
    - `scripts/verify-alpha-artifact.sh` contains `CFBundleVersion`
    - `scripts/verify-alpha-artifact.sh` contains `shasum -a 256`
    - `scripts/verify-alpha-artifact.sh` contains `Notarization: deferred to Phase 12`
    - `bash -n scripts/verify-alpha-artifact.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/verify-alpha-artifact.sh && rg 'codesign --verify --deep --strict --verbose=2|codesign -dv|CFBundleShortVersionString|CFBundleVersion|shasum -a 256|Notarization: deferred to Phase 12|Alpha artifact manifest' scripts/verify-alpha-artifact.sh docs/release.md .planning/phases/11-alpha/evidence/README.md && git diff --check</automated>
  </verify>
  <done>Alpha artifact verification is repeatable and produces sanitized manifest evidence.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
bash -n scripts/build-alpha.sh
bash -n scripts/verify-alpha-artifact.sh
rg 'xcodebuild archive|GRIDOS_DEVELOPMENT_TEAM|GRIDOS_SIGNING_IDENTITY|alpha-artifact-manifest.md' scripts/build-alpha.sh docs/release.md .planning/phases/11-alpha/evidence/README.md
rg 'codesign --verify --deep --strict --verbose=2|shasum -a 256|Notarization: deferred to Phase 12|Alpha artifact manifest' scripts/verify-alpha-artifact.sh docs/release.md .planning/phases/11-alpha/evidence/README.md
git diff --check
```
</verification>
