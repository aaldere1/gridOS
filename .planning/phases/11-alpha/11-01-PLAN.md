---
phase: 11-alpha
plan: 01
title: Signing preflight and alpha evidence policy
status: pending
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/alpha-signing-preflight.sh
  - docs/release.md
  - .planning/phases/11-alpha/evidence/README.md
autonomous: true
requirements: ["PHASE-11"]
requirements_addressed: ["D-01", "D-02", "D-03", "D-04", "D-05"]
must_haves:
  truths:
    - "Phase 11 starts with presence-only signing preflight evidence before attempting signed artifacts."
    - "Missing local Apple signing configuration is recorded as an explicit alpha blocker, not hidden as a generic build failure."
    - "Alpha evidence rules prohibit committing build artifacts, private terminal data, raw logs, shell history, environment dumps, API keys, prompts, generated commands, and user-specific paths."
  artifacts:
    - path: "scripts/alpha-signing-preflight.sh"
      provides: "Signing preflight command"
      contains: "GRIDOS_ALPHA_PREFLIGHT"
    - path: ".planning/phases/11-alpha/evidence/README.md"
      provides: "Alpha evidence policy"
      contains: "Blocker policy"
  key_links:
    - from: "docs/release.md"
      to: ".planning/phases/11-alpha/evidence/README.md"
      via: "Release docs reference the Phase 11 alpha evidence lane"
      pattern: "Phase 11 alpha"
---

<objective>
Create the alpha signing preflight and evidence policy.

Purpose: Alpha should fail early and legibly when signing prerequisites are missing, while preserving Phase 10 privacy guarantees in every committed evidence artifact.
Output: a local preflight script, release-doc alpha section, and evidence README.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-alpha/11-CONTEXT.md
@.planning/phases/11-alpha/11-RESEARCH.md
@.planning/phases/11-alpha/11-VALIDATION.md
@.planning/phases/10-security-and-privacy-hardening/10-VERIFICATION.md
@docs/release.md
@docs/dependency-security-review.md
@docs/security-threat-model.md
@docs/privacy-data-inventory.md
@project.yml
</context>

<tasks>

<task id="11-01-01" type="auto">
  <title>Add signing preflight script</title>
  <name>Add signing preflight script</name>
  <files>scripts/alpha-signing-preflight.sh</files>
  <read_first>project.yml, docs/release.md, docs/dependency-security-review.md, .planning/phases/11-alpha/11-RESEARCH.md</read_first>
  <action>Create executable Bash script `scripts/alpha-signing-preflight.sh` with `set -euo pipefail`, `--dry-run`, and sanitized text output headed `GRIDOS_ALPHA_PREFLIGHT`. The script must check required tools (`xcodegen`, `xcodebuild`, `security`), print Xcode version, read `PRODUCT_BUNDLE_IDENTIFIER`, `DEVELOPMENT_TEAM`, `CODE_SIGN_STYLE`, and `ENABLE_HARDENED_RUNTIME` from `project.yml` or build settings, and check presence of `GRIDOS_DEVELOPMENT_TEAM`, `GRIDOS_SIGNING_IDENTITY`, and optional `GRIDOS_EXPORT_METHOD` without echoing their values. It must run `security find-identity -v -p codesigning`, summarize only whether candidate identities are present, and write a sanitized report to `.planning/phases/11-alpha/evidence/signing-preflight.txt` unless `--dry-run` is used. If signing inputs are missing, exit nonzero with a clear `SIGNING_BLOCKED` line and the missing variable names only. Do not export private keys, inspect keychain item contents, print certificates in full, dump environment variables, or write raw command output.</action>
  <acceptance_criteria>
    - `scripts/alpha-signing-preflight.sh` contains `set -euo pipefail`
    - `scripts/alpha-signing-preflight.sh` contains `GRIDOS_ALPHA_PREFLIGHT`
    - `scripts/alpha-signing-preflight.sh` contains `GRIDOS_DEVELOPMENT_TEAM`
    - `scripts/alpha-signing-preflight.sh` contains `GRIDOS_SIGNING_IDENTITY`
    - `scripts/alpha-signing-preflight.sh` contains `ENABLE_HARDENED_RUNTIME`
    - `scripts/alpha-signing-preflight.sh` contains `security find-identity -v -p codesigning`
    - `scripts/alpha-signing-preflight.sh --dry-run` exits 0 on a normal developer machine without requiring signing variables
    - `bash -n scripts/alpha-signing-preflight.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/alpha-signing-preflight.sh && scripts/alpha-signing-preflight.sh --dry-run && rg 'GRIDOS_ALPHA_PREFLIGHT|GRIDOS_DEVELOPMENT_TEAM|GRIDOS_SIGNING_IDENTITY|ENABLE_HARDENED_RUNTIME|security find-identity -v -p codesigning' scripts/alpha-signing-preflight.sh && git diff --check</automated>
  </verify>
  <done>Signing preflight exists, is dry-run safe, and records signing absence as a first-class blocker.</done>
</task>

<task id="11-01-02" type="auto">
  <title>Create alpha evidence policy</title>
  <name>Create alpha evidence policy</name>
  <files>.planning/phases/11-alpha/evidence/README.md, docs/release.md</files>
  <read_first>.planning/phases/11-alpha/11-CONTEXT.md, .planning/phases/11-alpha/11-RESEARCH.md, docs/release.md, docs/privacy-data-inventory.md</read_first>
  <action>Create `.planning/phases/11-alpha/evidence/README.md` with heading `# Phase 11 alpha evidence` and exact sections `## Signing preflight`, `## Artifact verification`, `## Daily-driver UAT`, `## Known issues`, `## Blocker policy`, and `## Privacy boundaries`. State that committed evidence must be text-only and sanitized; build products stay outside git; signing absence is recorded as `SIGNING_BLOCKED`; Alpha cannot be marked complete with a high-severity terminal correctness blocker; and diagnostics/evidence must exclude shell history, terminal transcripts, environment variables, API keys, prompts, generated commands, raw terminal output, screenshots, traces, and user-specific paths. Update `docs/release.md` with a `## Phase 11 alpha` section linking this evidence README, the signing preflight script, the future build/verify scripts, daily-driver UAT, and known-issues workflow. Include the exact sentence `No artifacts committed: .app, .xcarchive, .dmg, .zip, .pkg, .trace, and screenshots stay out of source control.`</action>
  <acceptance_criteria>
    - `.planning/phases/11-alpha/evidence/README.md` contains `# Phase 11 alpha evidence`
    - `.planning/phases/11-alpha/evidence/README.md` contains `## Signing preflight`
    - `.planning/phases/11-alpha/evidence/README.md` contains `## Blocker policy`
    - `.planning/phases/11-alpha/evidence/README.md` contains `SIGNING_BLOCKED`
    - `.planning/phases/11-alpha/evidence/README.md` contains `No artifacts committed`
    - `docs/release.md` contains `## Phase 11 alpha`
    - `docs/release.md` contains `.planning/phases/11-alpha/evidence/README.md`
    - `docs/release.md` contains `scripts/alpha-signing-preflight.sh`
  </acceptance_criteria>
  <verify>
    <automated>rg 'Phase 11 alpha evidence|## Signing preflight|## Blocker policy|SIGNING_BLOCKED|No artifacts committed' .planning/phases/11-alpha/evidence/README.md && rg '## Phase 11 alpha|scripts/alpha-signing-preflight.sh|.planning/phases/11-alpha/evidence/README.md' docs/release.md && git diff --check</automated>
  </verify>
  <done>Alpha evidence rules and release-doc links are durable and privacy-safe.</done>
</task>

</tasks>

<verification>
Run after all tasks:

```sh
bash -n scripts/alpha-signing-preflight.sh
scripts/alpha-signing-preflight.sh --dry-run
rg 'GRIDOS_ALPHA_PREFLIGHT|GRIDOS_DEVELOPMENT_TEAM|GRIDOS_SIGNING_IDENTITY|ENABLE_HARDENED_RUNTIME' scripts/alpha-signing-preflight.sh
rg 'Phase 11 alpha evidence|Signing preflight|Blocker policy|No artifacts committed' .planning/phases/11-alpha/evidence/README.md docs/release.md
git diff --check
```
</verification>
