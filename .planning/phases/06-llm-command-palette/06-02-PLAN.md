---
phase: 06-llm-command-palette
plan: 02
title: Redaction and context preview
status: pending
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - Sources/CommandIntelligence/CommandContextPreview.swift
  - Sources/CommandIntelligence/CommandContextBuilder.swift
  - Sources/CommandIntelligence/SecretRedactor.swift
  - Tests/CommandIntelligenceTests/CommandContextPreviewTests.swift
  - Tests/CommandIntelligenceTests/SecretRedactorTests.swift
autonomous: true
requirements: ["PHASE-06", "LLM-02", "LLM-04", "LLM-05", "LLM-06", "LLM-10"]
requirements_addressed: ["D-09", "D-10", "D-11", "D-12", "D-13", "D-24"]
must_haves:
  truths:
    - "Context preview construction redacts before send and produces the approved provider payload."
    - "Redaction covers API keys, bearer/basic tokens, private key blocks, password assignments, .env values, and credential-looking URLs."
    - "The approved payload stores only redacted strings and never raw terminal context."
    - "Preview construction excludes shell history, environment variables, process lists, hidden files, SSH config, Keychain data, metrics snapshots, and unrequested scrollback."
  artifacts:
    - path: "Sources/CommandIntelligence/SecretRedactor.swift"
      provides: "Deterministic secret redaction"
      contains: "privateKey"
    - path: "Sources/CommandIntelligence/CommandContextPreview.swift"
      provides: "Preview and approved redacted payload models"
      contains: "approvedPayload"
    - path: "Sources/CommandIntelligence/CommandContextBuilder.swift"
      provides: "Flow-specific redacted preview construction"
      contains: "buildPreview"
    - path: "Tests/CommandIntelligenceTests/SecretRedactorTests.swift"
      provides: "Redaction regression coverage"
      contains: "testRedactsPrivateKeyBlocks"
  key_links:
    - from: "Sources/CommandIntelligence/CommandContextBuilder.swift"
      to: "Sources/CommandIntelligence/SecretRedactor.swift"
      via: "Builder redacts context before producing preview payload"
      pattern: "SecretRedactor"
    - from: "Sources/CommandIntelligence/CommandContextPreview.swift"
      to: "Sources/CommandIntelligence/LLMCommandProvider.swift"
      via: "Provider request accepts only approved preview payload"
      pattern: "approvedPayload"
---

<objective>
Create the Phase 6 redaction and context-preview slice on top of the Plan 06-01 contracts.

Purpose: No shell context may leave the machine until the user can inspect a redacted preview and approve the exact provider payload.
Output: `SecretRedactor`, `CommandContextPreview`, `CommandContextBuilder`, and XCTest coverage for redacted preview payloads.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-llm-command-palette/06-CONTEXT.md
@.planning/phases/06-llm-command-palette/06-RESEARCH.md
@.planning/phases/06-llm-command-palette/06-VALIDATION.md
@.planning/phases/06-llm-command-palette/06-UI-SPEC.md
@docs/security-privacy.md
@docs/architecture.md
@Sources/CommandIntelligence/CommandIntelligenceModels.swift
@Sources/CommandIntelligence/CommandContextPreview.swift
@Sources/CommandIntelligence/LLMCommandProvider.swift

<interfaces>
Contract from Plan 06-01:

```swift
public struct ApprovedCommandContextPayload: Equatable, Sendable {
    public let redactedSections: [CommandContextSource: String]
    public let redactionSummary: [String: Int]
    public let blockedReasons: [String]
}
```
</interfaces>
</context>

<tasks>

<task id="06-02-01" type="auto" tdd="true">
  <title>Add redaction and context preview construction</title>
  <name>Add redaction and context preview construction</name>
  <files>Sources/CommandIntelligence/CommandContextPreview.swift, Sources/CommandIntelligence/CommandContextBuilder.swift, Sources/CommandIntelligence/SecretRedactor.swift, Tests/CommandIntelligenceTests/CommandContextPreviewTests.swift, Tests/CommandIntelligenceTests/SecretRedactorTests.swift</files>
  <read_first>.planning/phases/06-llm-command-palette/06-CONTEXT.md, .planning/phases/06-llm-command-palette/06-RESEARCH.md, .planning/phases/06-llm-command-palette/06-VALIDATION.md, .planning/phases/06-llm-command-palette/06-UI-SPEC.md, Sources/CommandIntelligence/CommandIntelligenceModels.swift, Sources/CommandIntelligence/CommandContextPreview.swift, Sources/CommandIntelligence/LLMCommandProvider.swift</read_first>
  <action>Create `SecretRedactor` with `public func redact(_ text: String) -> RedactionResult`. `RedactionResult` must contain `redactedText`, `[RedactionFinding]`, and `blockedReasons`; `RedactionFinding` must include `kind`, `label`, and `replacement`. Redaction kinds and visible labels must cover `apiKey`, `bearerToken`, `basicToken`, `privateKey`, `passwordAssignment`, `envValue`, and `credentialURL`. Add deterministic replacements like `[REDACTED API KEY]`, `[REDACTED BEARER TOKEN]`, `[REDACTED BASIC TOKEN]`, `[REDACTED PRIVATE KEY]`, `[REDACTED PASSWORD]`, `[REDACTED ENV VALUE]`, and `[REDACTED CREDENTIAL URL]`. Cover API-key-like values such as `sk-ant-`, `sk-`, `xoxb-`, and `ghp_`; bearer/basic authorization headers; `-----BEGIN ... PRIVATE KEY-----` blocks; `password=`, `PASS=`, `TOKEN=`, `API_KEY=`, `SECRET=`, `.env`-style assignments; and URLs like `https://user:pass@example.com/path`. Expand `CommandContextPreview` and `ApprovedCommandContextPayload` so the approved payload stores only redacted strings and never raw context while preserving the Plan 06-01 provider contract. Create `CommandContextBuilder` with `buildPreview(from:)` that includes the flow name, user prompt, working directory, selected or pasted output, failed command, failed output, character counts, redaction labels/counts, and blocked reasons. It must not include shell history, environment variables, process lists, hidden files, SSH config, Keychain data, metrics snapshots, or unrequested scrollback per D-09 through D-13. Tests must prove raw secrets are absent from `CommandContextPreview.approvedPayload`, the preview includes prompt/cwd/selected-output/failed-command blocks when supplied, `canSend` is false when blocked reasons exist, and the provider request can be constructed only from `ApprovedCommandContextPayload`.</action>
  <acceptance_criteria>
    - `Sources/CommandIntelligence/SecretRedactor.swift` contains `SecretRedactor`
    - `Sources/CommandIntelligence/SecretRedactor.swift` contains `apiKey`
    - `Sources/CommandIntelligence/SecretRedactor.swift` contains `bearerToken`
    - `Sources/CommandIntelligence/SecretRedactor.swift` contains `basicToken`
    - `Sources/CommandIntelligence/SecretRedactor.swift` contains `privateKey`
    - `Sources/CommandIntelligence/SecretRedactor.swift` contains `passwordAssignment`
    - `Sources/CommandIntelligence/SecretRedactor.swift` contains `envValue`
    - `Sources/CommandIntelligence/SecretRedactor.swift` contains `credentialURL`
    - `Sources/CommandIntelligence/CommandContextPreview.swift` contains `ApprovedCommandContextPayload`
    - `Sources/CommandIntelligence/CommandContextPreview.swift` contains `approvedPayload`
    - `Sources/CommandIntelligence/CommandContextBuilder.swift` contains `buildPreview`
    - `Tests/CommandIntelligenceTests/SecretRedactorTests.swift` contains `testRedactsPrivateKeyBlocks`
    - `Tests/CommandIntelligenceTests/SecretRedactorTests.swift` contains `testRedactsCredentialURLs`
    - `Tests/CommandIntelligenceTests/CommandContextPreviewTests.swift` contains `testPreviewPayloadContainsOnlyRedactedContext`
    - `Tests/CommandIntelligenceTests/CommandContextPreviewTests.swift` contains `testPreviewDoesNotIncludeShellHistoryOrMetrics`
    - `rg "shell history|environment variables|metrics snapshots|Keychain data" Sources/CommandIntelligence Tests/CommandIntelligenceTests` shows those phrases only in tests or deny-list comments, not as included context fields
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:CommandIntelligenceTests/SecretRedactorTests -only-testing:CommandIntelligenceTests/CommandContextPreviewTests &amp;&amp; rg "SecretRedactor|RedactionFinding|privateKey|Bearer|Basic|credentialURL|ApprovedCommandContextPayload|approvedPayload" Sources/CommandIntelligence Tests/CommandIntelligenceTests</automated>
  </verify>
  <done>Every context preview is built from redacted data and exposes the exact approved payload before any provider request can exist.</done>
</task>

</tasks>

<verification>
Run the redaction/preview gate:

```bash
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:CommandIntelligenceTests/SecretRedactorTests -only-testing:CommandIntelligenceTests/CommandContextPreviewTests
rg "SecretRedactor|RedactionFinding|privateKey|Bearer|Basic|credentialURL" Sources/CommandIntelligence Tests/CommandIntelligenceTests
rg "ApprovedCommandContextPayload|CommandContextPreview|approvedPayload|buildPreview|canSend" Sources/CommandIntelligence Tests/CommandIntelligenceTests
git diff --check
```
</verification>

<success_criteria>
- Redaction is deterministic and covered for every Phase 6 secret class.
- Context preview produces an approved payload with redacted-only data.
- Provider requests still depend on the approved payload contract from Plan 06-01.
- No invisible shell history, environment, process, hidden-file, SSH, Keychain, metrics, or scrollback capture is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/06-llm-command-palette/06-02-SUMMARY.md`.
</output>
