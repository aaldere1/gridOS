---
phase: 06-llm-command-palette
plan: 05
title: Command-K palette and terminal bridge
status: pending
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - gridOS.xcodeproj/project.pbxproj
  - Sources/TerminalCore/TerminalInteractionController.swift
  - Sources/TerminalCore/TerminalSurface.swift
  - Sources/GridOSApp/GridOSApp.swift
  - Sources/GridOSApp/RootView.swift
  - Sources/GridOSApp/SettingsView.swift
  - Sources/GridOSApp/CommandIntelligenceCommandCenter.swift
  - Sources/GridOSApp/CommandPaletteView.swift
  - Tests/TerminalCoreTests/TerminalInteractionControllerTests.swift
autonomous: true
requirements: ["PHASE-06", "LLM-01", "LLM-02", "LLM-05", "LLM-09", "LLM-10", "LLM-12"]
requirements_addressed: ["D-01", "D-02", "D-03", "D-04", "D-09", "D-10", "D-11", "D-13", "D-19", "D-20", "D-22", "D-23", "D-25"]
must_haves:
  truths:
    - "Command-K opens a compact Command Intelligence palette while the terminal remains visible."
    - "Terminal Clear remains visible and moves to Command-Option-K."
    - "GridOSApp does not import SwiftTerm."
    - "The palette exposes exactly Suggest Command, Explain Output, and Fix Failed Command."
    - "Preview Context is required before Send Request, and closing the palette restores terminal focus."
    - "The no-key `Open Command Intelligence Settings` action opens or focuses the Settings Command Intelligence section."
    - "Terminal selection, insert, run, and focus are accessed only through TerminalCore."
  artifacts:
    - path: "Sources/TerminalCore/TerminalInteractionController.swift"
      provides: "SwiftTerm-isolated terminal interaction seam"
      contains: "focusTerminal"
    - path: "Sources/GridOSApp/CommandPaletteView.swift"
      provides: "Compact one-shot command-intelligence palette shell"
      contains: "Suggest Command"
    - path: "Sources/GridOSApp/GridOSApp.swift"
      provides: "Command-K and moved Clear shortcut"
      contains: "CommandIntelligenceCommands"
    - path: "Sources/GridOSApp/SettingsView.swift"
      provides: "Focusable Command Intelligence settings section"
      contains: "command-intelligence-settings"
  key_links:
    - from: "Sources/GridOSApp/RootView.swift"
      to: "Sources/TerminalCore/TerminalInteractionController.swift"
      via: "RootView owns controller and passes it to TerminalSurface/palette"
      pattern: "TerminalInteractionController"
    - from: "Sources/GridOSApp/GridOSApp.swift"
      to: "Sources/GridOSApp/CommandIntelligenceCommandCenter.swift"
      via: "Command-K posts palette-open notification"
      pattern: "openCommandIntelligence"
    - from: "Sources/GridOSApp/CommandPaletteView.swift"
      to: "Sources/CommandIntelligence/CommandContextBuilder.swift"
      via: "Preview Context builds redacted preview before Send Request"
      pattern: "buildPreview"
    - from: "Sources/GridOSApp/CommandPaletteView.swift"
      to: "Sources/GridOSApp/SettingsView.swift"
      via: "No-key action posts settings-open request and Settings focuses Command Intelligence"
      pattern: "Open Command Intelligence Settings"
---

<objective>
Wire the app-facing command palette shell and terminal bridge: `Command-K`, non-conflicting Terminal Clear, TerminalCore interaction controller, compact three-flow palette, context-preview state, and focus restoration.

Purpose: The app should feel like a native terminal with opt-in assistance, not a chat agent or hidden terminal driver.
Output: TerminalCore interaction API, app command registration, palette shell, preview flow, and source/test checks for no SwiftTerm leakage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-llm-command-palette/06-CONTEXT.md
@.planning/phases/06-llm-command-palette/06-RESEARCH.md
@.planning/phases/06-llm-command-palette/06-VALIDATION.md
@.planning/phases/06-llm-command-palette/06-UI-SPEC.md
@docs/architecture.md
@docs/release.md
@Sources/GridOSApp/GridOSApp.swift
@Sources/GridOSApp/RootView.swift
@Sources/GridOSApp/SettingsView.swift
@Sources/TerminalCore/TerminalSurface.swift
@Sources/TerminalCore/TerminalCommandCenter.swift
@Sources/CommandIntelligence/CommandContextBuilder.swift

<interfaces>
Existing terminal command pattern:

```swift
public enum TerminalCommandCenter {
    public static func clear() {
        NotificationCenter.default.post(name: .gridOSTerminalClear, object: nil)
    }
}
```

Required new TerminalCore seam:

```swift
@MainActor
public final class TerminalInteractionController: ObservableObject {
    public func selectedText() -> String?
    public func insert(_ text: String)
    public func run(_ command: String)
    public func focusTerminal()
}
```
</interfaces>
</context>

<tasks>

<task id="06-05-01" type="auto" tdd="true">
  <title>Add TerminalInteractionController without SwiftTerm leakage</title>
  <name>Add TerminalInteractionController without SwiftTerm leakage</name>
  <files>Sources/TerminalCore/TerminalInteractionController.swift, Sources/TerminalCore/TerminalSurface.swift, Tests/TerminalCoreTests/TerminalInteractionControllerTests.swift</files>
  <read_first>.planning/phases/06-llm-command-palette/06-RESEARCH.md, .planning/phases/06-llm-command-palette/06-VALIDATION.md, docs/architecture.md, Sources/TerminalCore/TerminalSurface.swift, Sources/TerminalCore/TerminalCommandCenter.swift, Tests/TerminalCoreTests/TerminalSessionConfigurationTests.swift</read_first>
  <action>Create `TerminalInteractionController` in `TerminalCore` as `@MainActor public final class TerminalInteractionController: ObservableObject`. Its public API must be exactly `selectedText() -> String?`, `insert(_ text: String)`, `run(_ command: String)`, and `focusTerminal()`. Keep any SwiftTerm-specific storage `fileprivate` or internal to `TerminalCore`; do not expose `LocalProcessTerminalView`, `TerminalView`, or SwiftTerm types in public signatures. Add an internal/test seam such as `TerminalInteractionControllingTerminal` so XCTest can prove `insert("echo hi")` sends `echo hi` without a trailing newline, `run("echo hi")` sends `echo hi\n`, empty/whitespace selection returns nil, non-empty selection returns the selected text, and `focusTerminal()` asks the terminal to become first responder. Update `TerminalSurface` initializer to accept `interactionController: TerminalInteractionController? = nil`; when the SwiftTerm view is attached, attach it to the controller; on dismantle/shutdown, detach it. Do not change existing copy/paste/clear/reset notification behavior. This implements D-13 and D-25 while keeping SwiftTerm isolated in `TerminalCore`.</action>
  <acceptance_criteria>
    - `Sources/TerminalCore/TerminalInteractionController.swift` contains `public final class TerminalInteractionController`
    - `Sources/TerminalCore/TerminalInteractionController.swift` contains `selectedText() -> String?`
    - `Sources/TerminalCore/TerminalInteractionController.swift` contains `insert(_ text: String)`
    - `Sources/TerminalCore/TerminalInteractionController.swift` contains `run(_ command: String)`
    - `Sources/TerminalCore/TerminalInteractionController.swift` contains `focusTerminal()`
    - `Sources/TerminalCore/TerminalSurface.swift` contains `interactionController`
    - `Sources/TerminalCore/TerminalSurface.swift` contains `attach`
    - `Sources/TerminalCore/TerminalSurface.swift` contains `detach`
    - `Tests/TerminalCoreTests/TerminalInteractionControllerTests.swift` contains `testInsertSendsTextWithoutNewline`
    - `Tests/TerminalCoreTests/TerminalInteractionControllerTests.swift` contains `testRunAppendsSingleNewline`
    - `Tests/TerminalCoreTests/TerminalInteractionControllerTests.swift` contains `testBlankSelectionReturnsNil`
    - `! rg "import SwiftTerm" Sources/GridOSApp`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:TerminalCoreTests/TerminalInteractionControllerTests &amp;&amp; rg "TerminalInteractionController|selectedText|insert\\(_ text|run\\(_ command|focusTerminal|interactionController" Sources/TerminalCore Tests/TerminalCoreTests &amp;&amp; ! rg "import SwiftTerm" Sources/GridOSApp</automated>
  </verify>
  <done>GridOSApp can select, insert, run, and restore terminal focus through TerminalCore without importing SwiftTerm.</done>
</task>

<task id="06-05-02" type="auto">
  <title>Register Command-K and compose the compact palette overlay</title>
  <name>Register Command-K and compose the compact palette overlay</name>
  <files>Sources/GridOSApp/GridOSApp.swift, Sources/GridOSApp/RootView.swift, Sources/GridOSApp/SettingsView.swift, Sources/GridOSApp/CommandIntelligenceCommandCenter.swift, Sources/GridOSApp/CommandPaletteView.swift</files>
  <read_first>.planning/phases/06-llm-command-palette/06-CONTEXT.md, .planning/phases/06-llm-command-palette/06-UI-SPEC.md, Sources/GridOSApp/GridOSApp.swift, Sources/GridOSApp/RootView.swift, Sources/GridOSApp/SettingsView.swift, Sources/TerminalCore/TerminalInteractionController.swift, Sources/TerminalCore/TerminalSurface.swift, Sources/RenderCore/VisualTheme.swift</read_first>
  <action>Move Terminal Clear from `.keyboardShortcut("k", modifiers: [.command])` to the visible non-conflicting shortcut `.keyboardShortcut("k", modifiers: [.command, .option])`, preserving the `Clear` Terminal menu item and `TerminalCommandCenter.clear()` action per D-04. Add `CommandIntelligenceCommands` to the app `.commands` block. Implement `CommandIntelligenceCommandCenter` using narrow notification bridges: `Notification.Name("gridOS.commandIntelligence.open")` with `openCommandIntelligence()` for Command-K, and `Notification.Name("gridOS.commandIntelligence.openSettings")` with `openCommandIntelligenceSettings()` for the no-key setup action. Add a `Button("Command Intelligence")` with `.keyboardShortcut("k", modifiers: [.command])`; source checks must show no other Command-K shortcut remains. In `RootView`, create `@StateObject private var terminalInteractionController = TerminalInteractionController()` and `@State private var isCommandPalettePresented = false`; pass the controller into `TerminalWorkspaceView` and then `TerminalSurface`; observe the command-intelligence open notification; present `CommandPaletteView` as a compact overlay in the existing `ZStack` while terminal content remains visible around it. Also inject an `onOpenCommandIntelligenceSettings` closure into `CommandPaletteView` that calls `CommandIntelligenceCommandCenter.openCommandIntelligenceSettings()`, opens or focuses the native Settings window using SwiftUI `openSettings` or `NSApp.sendAction(Selector(("showSettingsWindow:")), to: nil, from: nil)`, and dismisses the palette without losing terminal usability. In `SettingsView`, make the Command Intelligence section from Plan 06-04 focusable with a stable source-visible id such as `command-intelligence-settings`; observe the same settings-open notification and scroll/focus/highlight that section when possible. Palette max size must follow the UI spec: width `min(720px, windowWidth - 96px)` and height `min(620px, windowHeight - 96px)` using SwiftUI `frame(maxWidth: 720, maxHeight: 620)` plus outer padding rather than a full-screen chat surface. Dismissal by `Escape`, `Close Preview`, cancel, or overlay close must set `isCommandPalettePresented = false` and call `terminalInteractionController.focusTerminal()` on the main actor. The palette title must be `Command Intelligence`, with accessibility label `Command Intelligence`, and the flow selector must expose exactly `Suggest Command`, `Explain Output`, and `Fix Failed Command`. Do not add conversational shell mode, assistant avatar, persistent chat rail, or any deferred provider/plugin surfaces.</action>
  <acceptance_criteria>
    - `Sources/GridOSApp/GridOSApp.swift` contains `CommandIntelligenceCommands`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.keyboardShortcut("k", modifiers: [.command])`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.keyboardShortcut("k", modifiers: [.command, .option])`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Clear`
    - `Sources/GridOSApp/CommandIntelligenceCommandCenter.swift` contains `gridOS.commandIntelligence.open`
    - `Sources/GridOSApp/CommandIntelligenceCommandCenter.swift` contains `openCommandIntelligence`
    - `Sources/GridOSApp/CommandIntelligenceCommandCenter.swift` contains `gridOS.commandIntelligence.openSettings`
    - `Sources/GridOSApp/CommandIntelligenceCommandCenter.swift` contains `openCommandIntelligenceSettings`
    - `Sources/GridOSApp/RootView.swift` contains `TerminalInteractionController`
    - `Sources/GridOSApp/RootView.swift` contains `isCommandPalettePresented`
    - `Sources/GridOSApp/RootView.swift` contains `focusTerminal()`
    - `Sources/GridOSApp/RootView.swift` contains `onOpenCommandIntelligenceSettings`
    - `Sources/GridOSApp/SettingsView.swift` contains `command-intelligence-settings`
    - `Sources/GridOSApp/SettingsView.swift` contains `gridOS.commandIntelligence.openSettings`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Command Intelligence`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Open Command Intelligence Settings`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `onOpenCommandIntelligenceSettings`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Suggest Command`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Explain Output`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Fix Failed Command`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `frame(maxWidth: 720`
    - `! rg "chat|assistant avatar|conversation|autonomous|plugin|marketplace" Sources/GridOSApp/CommandPaletteView.swift`
    - `rg 'keyboardShortcut\\("k", modifiers: \\[\\.command\\]\\)' Sources/GridOSApp` returns exactly the Command Intelligence shortcut
  </acceptance_criteria>
  <verify>
    <automated>xcodegen generate --use-cache &amp;&amp; xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg 'keyboardShortcut\\("k", modifiers: \\[\\.command\\]\\)|keyboardShortcut\\("k", modifiers: \\[\\.command, \\.option\\]\\)|CommandIntelligenceCommands|Clear|Command Intelligence|Suggest Command|Explain Output|Fix Failed Command|Open Command Intelligence Settings|openCommandIntelligenceSettings|command-intelligence-settings|gridOS.commandIntelligence.openSettings|onOpenCommandIntelligenceSettings' Sources/GridOSApp &amp;&amp; ! rg "import SwiftTerm|conversation|autonomous shell|assistant avatar" Sources/GridOSApp</automated>
  </verify>
  <done>Command-K opens a compact one-shot Command Intelligence overlay, Clear remains discoverable on Command-Option-K, and the no-key setup action opens or focuses Command Intelligence Settings.</done>
</task>

<task id="06-05-03" type="auto">
  <title>Implement Preview Context state before send</title>
  <name>Implement Preview Context state before send</name>
  <files>Sources/GridOSApp/CommandPaletteView.swift, Sources/GridOSApp/RootView.swift</files>
  <read_first>.planning/phases/06-llm-command-palette/06-CONTEXT.md, .planning/phases/06-llm-command-palette/06-UI-SPEC.md, Sources/GridOSApp/CommandPaletteView.swift, Sources/GridOSApp/RootView.swift, Sources/CommandIntelligence/CommandContextBuilder.swift, Sources/CommandIntelligence/CommandContextPreview.swift, Sources/TerminalCore/TerminalInteractionController.swift</read_first>
  <action>Wire `CommandPaletteView` to build a local `CommandAssistanceInput` and call `CommandContextBuilder.buildPreview(from:)` only when the user chooses `Preview Context`. Use exact labels: `Preview Context`, `Send Request`, `Close Preview`, and `Edit Context`. `Command-Return` may trigger `Preview Context` or `Send Request`, but it must never call terminal `run` or insert generated command text. For `Suggest Command`, collect only the natural-language prompt and current working-directory assumption when available. For `Explain Output`, call `terminalInteractionController.selectedText()` through a closure supplied by `RootView`; if it returns nil/empty, show the paste fallback with placeholder `Select terminal output or paste it here.` and failure copy `Selection unavailable` / `Paste the output into the field to continue.` rather than scraping scrollback or using the clipboard. For `Fix Failed Command`, use explicit pasted failed command/output fields with placeholder `Paste the failed command and output.` Preview must display flow name, prompt or failed command, working directory, selected/pasted output character count, failed-output character count, redaction labels/counts, and blocked reasons. `Send Request` must be disabled when `preview.canSend == false`; `Close Preview` returns to compose without sending; `Edit Context` returns to compose. At this plan stage, if provider orchestration is not yet wired, `Send Request` may call an injected async closure that Plan 06-06 will implement, but there must be no network request before preview approval. Closing the palette from compose or preview must restore terminal focus through `RootView`.</action>
  <acceptance_criteria>
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Preview Context`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Send Request`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Close Preview`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Edit Context`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Select terminal output or paste it here.`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Paste the failed command and output.`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `Selection unavailable`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `buildPreview`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `canSend`
    - `Sources/GridOSApp/CommandPaletteView.swift` contains `redaction`
    - `Sources/GridOSApp/RootView.swift` contains `selectedText`
    - `Sources/GridOSApp/RootView.swift` contains `focusTerminal()`
    - `! rg "scrollback|copy\\(|NSPasteboard|full shell history|environment variables|process lists|metricsSnapshot" Sources/GridOSApp/CommandPaletteView.swift`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg "Preview Context|Send Request|Close Preview|Edit Context|Selection unavailable|buildPreview|canSend|redaction" Sources/GridOSApp Sources/CommandIntelligence &amp;&amp; ! rg "scrollback|NSPasteboard|full shell history|environment variables|process lists|metricsSnapshot|import SwiftTerm" Sources/GridOSApp</automated>
  </verify>
  <done>The palette requires a visible redacted context preview before any request can be sent, with paste fallback for unsupported selection.</done>
</task>

</tasks>

<verification>
Run the palette/terminal bridge gate:

```bash
xcodegen generate --use-cache
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
rg 'keyboardShortcut\("k", modifiers: \[\.command\]\)|keyboardShortcut\("k", modifiers: \[\.command, \.option\]\)|CommandIntelligenceCommands|Clear' Sources/GridOSApp
rg "Open Command Intelligence Settings|openCommandIntelligenceSettings|gridOS.commandIntelligence.openSettings|command-intelligence-settings|onOpenCommandIntelligenceSettings" Sources/GridOSApp
rg "TerminalInteractionController|getSelection|sendText|focusTerminal|selectedText" Sources/TerminalCore Sources/GridOSApp Tests/TerminalCoreTests
rg "Suggest Command|Explain Output|Fix Failed Command|Preview Context|Send Request|Close Preview|Edit Context" Sources/GridOSApp
! rg "import SwiftTerm|scrollback|NSPasteboard|conversation|autonomous shell|assistant avatar" Sources/GridOSApp
git diff --check
```
</verification>

<success_criteria>
- Command-K opens the Command Intelligence palette; Terminal Clear uses Command-Option-K.
- The no-key `Open Command Intelligence Settings` action opens or focuses the Command Intelligence Settings section.
- Palette has exactly the three Phase 6 flows and a preview-first state machine.
- Terminal selected text, insertion, run, and focus are available only through TerminalCore.
- No shell context is sent or gathered invisibly; unsupported selection falls back to paste.
</success_criteria>

<output>
After completion, create `.planning/phases/06-llm-command-palette/06-05-SUMMARY.md`.
</output>
