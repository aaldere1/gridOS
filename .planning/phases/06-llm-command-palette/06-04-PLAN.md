---
phase: 06-llm-command-palette
plan: 04
title: Anthropic provider and Keychain setup
status: pending
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - gridOS.xcodeproj/project.pbxproj
  - Sources/CommandIntelligence/AnthropicCommandProvider.swift
  - Sources/CommandIntelligence/KeychainCommandCredentialStore.swift
  - Sources/GridOSKit/GridOSAppPreferences.swift
  - Sources/GridOSApp/SettingsView.swift
  - Sources/GridOSApp/CommandIntelligenceSettingsView.swift
  - Tests/CommandIntelligenceTests/AnthropicCommandProviderTests.swift
  - Tests/CommandIntelligenceTests/CommandCredentialStoreTests.swift
  - Tests/GridOSKitTests/GridOSAppPreferencesTests.swift
autonomous: true
requirements: ["PHASE-06", "LLM-03", "LLM-04", "LLM-08", "LLM-11"]
requirements_addressed: ["D-05", "D-06", "D-07", "D-08", "D-22", "D-24"]
user_setup:
  - service: anthropic
    why: "Optional live hosted command intelligence provider"
    env_vars: []
    dashboard_config:
      - task: "Create an Anthropic API key if live provider testing is desired"
        location: "Anthropic Console API keys"
must_haves:
  truths:
    - "Anthropic is available behind the provider abstraction without adding an SDK dependency."
    - "Provider requests use POST /v1/messages with x-api-key and anthropic-version: 2023-06-01."
    - "API keys are saved, read, replaced, and deleted through Keychain generic-password items, not AppStorage or UserDefaults."
    - "Settings exposes a calm Command Intelligence setup state and never echoes saved API keys."
    - "No-key and provider error states remain human-readable and non-blocking."
  artifacts:
    - path: "Sources/CommandIntelligence/AnthropicCommandProvider.swift"
      provides: "Anthropic Messages API adapter"
      contains: "anthropic-version"
    - path: "Sources/CommandIntelligence/KeychainCommandCredentialStore.swift"
      provides: "Security.framework credential store"
      contains: "com.aaldere1.gridos.command-intelligence"
    - path: "Sources/GridOSApp/CommandIntelligenceSettingsView.swift"
      provides: "Provider/key setup UI"
      contains: "Command Intelligence"
    - path: "Tests/CommandIntelligenceTests/AnthropicCommandProviderTests.swift"
      provides: "Mocked provider request/response/failure coverage"
      contains: "testAnthropicRequestUsesRequiredHeaders"
  key_links:
    - from: "Sources/GridOSApp/CommandIntelligenceSettingsView.swift"
      to: "Sources/CommandIntelligence/KeychainCommandCredentialStore.swift"
      via: "Settings saves and deletes provider key through credential store"
      pattern: "saveAPIKey"
    - from: "Sources/CommandIntelligence/AnthropicCommandProvider.swift"
      to: "Sources/CommandIntelligence/LLMCommandProvider.swift"
      via: "Provider implements LLMCommandProvider"
      pattern: "complete"
    - from: "Sources/GridOSKit/GridOSAppPreferences.swift"
      to: "Sources/GridOSApp/SettingsView.swift"
      via: "Non-secret provider/model preferences only"
      pattern: "commandIntelligence"
---

<objective>
Add the first hosted provider path and opt-in credential setup: Anthropic Messages API, Keychain-backed API key storage, non-secret provider/model preferences, and Settings UI for the no-key/configured states.

Purpose: Phase 6 needs one useful live provider while preserving no-key offline usability and keeping secrets out of app storage.
Output: Anthropic adapter, Security.framework credential store, Settings provider setup UI, and mocked provider/credential tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-llm-command-palette/06-CONTEXT.md
@.planning/phases/06-llm-command-palette/06-RESEARCH.md
@.planning/phases/06-llm-command-palette/06-VALIDATION.md
@.planning/phases/06-llm-command-palette/06-UI-SPEC.md
@docs/security-privacy.md
@docs/architecture.md
@project.yml
@Sources/CommandIntelligence/LLMCommandProvider.swift
@Sources/CommandIntelligence/CommandCredentialStore.swift
@Sources/CommandIntelligence/CommandIntelligenceFailure.swift
@Sources/GridOSKit/GridOSAppPreferences.swift
@Sources/GridOSApp/SettingsView.swift

<interfaces>
Provider and credential contracts from Plan 01:

```swift
public protocol LLMCommandProvider: Sendable {
    var providerID: LLMProviderID { get }
    func complete(_ request: LLMCommandRequest, apiKey: String) async throws -> LLMCommandResponse
}

public protocol CommandCredentialStore: Sendable {
    func apiKey(for provider: LLMProviderID) async throws -> String?
    func saveAPIKey(_ apiKey: String, for provider: LLMProviderID) async throws
    func deleteAPIKey(for provider: LLMProviderID) async throws
}
```
</interfaces>
</context>

<tasks>

<task id="06-04-01" type="auto" tdd="true">
  <title>Implement Anthropic Messages provider adapter</title>
  <name>Implement Anthropic Messages provider adapter</name>
  <files>Sources/CommandIntelligence/AnthropicCommandProvider.swift, Tests/CommandIntelligenceTests/AnthropicCommandProviderTests.swift</files>
  <read_first>.planning/phases/06-llm-command-palette/06-RESEARCH.md, .planning/phases/06-llm-command-palette/06-VALIDATION.md, Sources/CommandIntelligence/LLMCommandProvider.swift, Sources/CommandIntelligence/CommandContextPreview.swift, Sources/CommandIntelligence/CommandIntelligenceFailure.swift, Tests/CommandIntelligenceTests/CommandIntelligenceModelTests.swift</read_first>
  <action>Create `AnthropicCommandProvider` implementing `LLMCommandProvider` with no external SDK. It must use Foundation `URLSession`, default base URL `https://api.anthropic.com`, endpoint `POST /v1/messages`, header `x-api-key: <apiKey>`, header `anthropic-version: 2023-06-01`, header `content-type: application/json`, default model `claude-sonnet-4-6`, and a bounded `max_tokens` value such as `1200`. The request body must be built from `LLMCommandRequest.approvedPreview`; do not accept raw shell context. Include a system instruction that responses must match the local `LLMCommandResponse` fields for summary, explanation, and optional command objects, but still validate decoded JSON locally. If using Anthropic structured output fields, keep the schema in this file and covered by tests; if the exact docs changed, keep the required headers and endpoint from research unless implementation re-verifies official docs. Map `URLError.notConnectedToInternet`, `.cannotFindHost`, `.timedOut`, and `.networkConnectionLost` to `.offline`; HTTP 401/403 to `.providerError`; HTTP 429 to `.rateLimited`; 5xx to `.providerError`; refusals to `.providerRefusal`; stop reason/max token truncation to `.truncatedResponse`; malformed JSON or missing command fields to `.invalidProviderResponse`. Preserve provider request ID from response headers when available, but do not log prompts, approved payloads, API keys, generated commands, or provider responses by default per D-08. Tests must use a custom `URLProtocol` or injectable HTTP transport to assert method, URL, headers, body model, decoded response, 401/429/5xx/network mapping, invalid JSON mapping, refusal/truncation mapping, and that raw API keys are not reflected in thrown error descriptions.</action>
  <acceptance_criteria>
    - `Sources/CommandIntelligence/AnthropicCommandProvider.swift` contains `public struct AnthropicCommandProvider`
    - `Sources/CommandIntelligence/AnthropicCommandProvider.swift` contains `https://api.anthropic.com`
    - `Sources/CommandIntelligence/AnthropicCommandProvider.swift` contains `/v1/messages`
    - `Sources/CommandIntelligence/AnthropicCommandProvider.swift` contains `x-api-key`
    - `Sources/CommandIntelligence/AnthropicCommandProvider.swift` contains `anthropic-version`
    - `Sources/CommandIntelligence/AnthropicCommandProvider.swift` contains `2023-06-01`
    - `Sources/CommandIntelligence/AnthropicCommandProvider.swift` contains `claude-sonnet-4-6`
    - `Sources/CommandIntelligence/AnthropicCommandProvider.swift` contains `approvedPreview`
    - `Tests/CommandIntelligenceTests/AnthropicCommandProviderTests.swift` contains `testAnthropicRequestUsesRequiredHeaders`
    - `Tests/CommandIntelligenceTests/AnthropicCommandProviderTests.swift` contains `testRateLimitMapsToHumanReadableFailure`
    - `Tests/CommandIntelligenceTests/AnthropicCommandProviderTests.swift` contains `testInvalidJSONMapsToInvalidProviderResponse`
    - `Tests/CommandIntelligenceTests/AnthropicCommandProviderTests.swift` contains `testNetworkErrorMapsToOffline`
    - `rg "print\\(|NSLog|debugPrint|approvedPayload|apiKey" Sources/CommandIntelligence/AnthropicCommandProvider.swift` shows no logging of keys, prompts, payloads, or responses
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:CommandIntelligenceTests/AnthropicCommandProviderTests &amp;&amp; rg "AnthropicCommandProvider|POST|/v1/messages|x-api-key|anthropic-version|2023-06-01|claude-sonnet-4-6" Sources/CommandIntelligence Tests/CommandIntelligenceTests</automated>
  </verify>
  <done>Anthropic can complete approved command-intelligence requests through the provider protocol with deterministic request and failure behavior.</done>
</task>

<task id="06-04-02" type="auto" tdd="true">
  <title>Implement Keychain credential store and non-secret provider preferences</title>
  <name>Implement Keychain credential store and non-secret provider preferences</name>
  <files>Sources/CommandIntelligence/KeychainCommandCredentialStore.swift, Sources/GridOSKit/GridOSAppPreferences.swift, Tests/CommandIntelligenceTests/CommandCredentialStoreTests.swift, Tests/GridOSKitTests/GridOSAppPreferencesTests.swift</files>
  <read_first>.planning/phases/06-llm-command-palette/06-CONTEXT.md, .planning/phases/06-llm-command-palette/06-RESEARCH.md, .planning/phases/06-llm-command-palette/06-VALIDATION.md, docs/security-privacy.md, Sources/CommandIntelligence/CommandCredentialStore.swift, Sources/GridOSKit/GridOSAppPreferences.swift, Tests/GridOSKitTests/GridOSAppPreferencesTests.swift</read_first>
  <action>Create `KeychainCommandCredentialStore` using `Security.framework` and the `CommandCredentialStore` protocol. Use exact service `com.aaldere1.gridos.command-intelligence`, class `kSecClassGenericPassword`, account equal to provider ID raw value such as `anthropic`, accessibility `kSecAttrAccessibleWhenUnlockedThisDeviceOnly`, and include `kSecUseDataProtectionKeychain: true` in queries where applicable. `saveAPIKey(_:for:)` must trim whitespace, reject empty values through a human-readable failure, call `SecItemUpdate` when an item already exists, and avoid duplicate keychain items. `apiKey(for:)` returns nil for `errSecItemNotFound`; `deleteAPIKey(for:)` treats not-found as success. Keep Keychain calls out of SwiftUI body evaluation by leaving this API async. Add a tiny injectable `KeychainSecItemClient` or equivalent so tests can cover add/update/copy/delete query dictionaries without writing the real user keychain. Extend `GridOSAppPreferences` with non-secret keys only: `commandIntelligenceProviderStorageKey = "commandIntelligence.providerID"`, `commandIntelligenceModelStorageKey = "commandIntelligence.modelID"`, `defaultCommandIntelligenceProviderID = "anthropic"`, `defaultCommandIntelligenceModelID = "claude-sonnet-4-6"`, and normalization helpers for provider/model raw values. Do not store API keys, key fragments, prompt text, selected output, generated commands, or provider responses in `@AppStorage`, `UserDefaults`, files, logs, or `GridOSKit`.</action>
  <acceptance_criteria>
    - `Sources/CommandIntelligence/KeychainCommandCredentialStore.swift` contains `KeychainCommandCredentialStore`
    - `Sources/CommandIntelligence/KeychainCommandCredentialStore.swift` contains `com.aaldere1.gridos.command-intelligence`
    - `Sources/CommandIntelligence/KeychainCommandCredentialStore.swift` contains `kSecClassGenericPassword`
    - `Sources/CommandIntelligence/KeychainCommandCredentialStore.swift` contains `kSecAttrAccessibleWhenUnlockedThisDeviceOnly`
    - `Sources/CommandIntelligence/KeychainCommandCredentialStore.swift` contains `kSecAttrAccount`
    - `Sources/CommandIntelligence/KeychainCommandCredentialStore.swift` contains `SecItemUpdate`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `commandIntelligence.providerID`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `commandIntelligence.modelID`
    - `Sources/GridOSKit/GridOSAppPreferences.swift` contains `claude-sonnet-4-6`
    - `Tests/CommandIntelligenceTests/CommandCredentialStoreTests.swift` contains `testKeychainStoreUsesExpectedServiceClassAccountAndAccessibility`
    - `Tests/CommandIntelligenceTests/CommandCredentialStoreTests.swift` contains `testKeychainStoreUpdatesExistingItemInsteadOfDuplicating`
    - `Tests/GridOSKitTests/GridOSAppPreferencesTests.swift` contains `testCommandIntelligencePreferencesStoreOnlyProviderAndModel`
    - `! rg "apiKey.*AppStorage|UserDefaults.*api|anthropic.*AppStorage|commandIntelligence\\.apiKey|prompt.*UserDefaults|generated.*UserDefaults" Sources Tests`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:CommandIntelligenceTests/CommandCredentialStoreTests -only-testing:GridOSKitTests/GridOSAppPreferencesTests &amp;&amp; rg "CommandCredentialStore|KeychainCommandCredentialStore|kSecClassGenericPassword|kSecAttrAccessibleWhenUnlockedThisDeviceOnly|com\\.aaldere1\\.gridos\\.command-intelligence|commandIntelligence\\.providerID|commandIntelligence\\.modelID" Sources/CommandIntelligence Sources/GridOSKit Tests &amp;&amp; ! rg "apiKey.*AppStorage|UserDefaults.*api|anthropic.*AppStorage|commandIntelligence\\.apiKey" Sources Tests</automated>
  </verify>
  <done>Provider credentials are Keychain-only, while provider/model selection is stored as non-secret local preference data.</done>
</task>

<task id="06-04-03" type="auto">
  <title>Add Command Intelligence Settings setup UI</title>
  <name>Add Command Intelligence Settings setup UI</name>
  <files>Sources/GridOSApp/SettingsView.swift, Sources/GridOSApp/CommandIntelligenceSettingsView.swift</files>
  <read_first>.planning/phases/06-llm-command-palette/06-UI-SPEC.md, .planning/phases/06-llm-command-palette/06-CONTEXT.md, Sources/GridOSApp/SettingsView.swift, Sources/GridOSKit/GridOSAppPreferences.swift, Sources/CommandIntelligence/CommandCredentialStore.swift, Sources/CommandIntelligence/KeychainCommandCredentialStore.swift</read_first>
  <action>Import `CommandIntelligence` in Settings code and add a `Command Intelligence` settings section while preserving the existing `Terminal`, `Appearance`, and `Recovery` sections. Prefer a focused `CommandIntelligenceSettingsView.swift` subview embedded by `SettingsView`. The UI must use existing grouped Settings grammar and exact labels/copy from `06-UI-SPEC.md`: section title `Command Intelligence`, provider label `Provider`, provider option `Anthropic`, model default `claude-sonnet-4-6`, key entry label `Anthropic API key`, save action `Save Provider Key`, delete action `Remove Provider Key`, configured status `Provider configured`, no-key heading `Provider not configured`, no-key body `Add a provider key in Settings to use command intelligence. The terminal still works normally.`, and no-key action text `Open Command Intelligence Settings` for the palette action wired in Plan 06-05. Store non-secret provider/model values with `@AppStorage(GridOSAppPreferences.commandIntelligenceProviderStorageKey)` and `@AppStorage(GridOSAppPreferences.commandIntelligenceModelStorageKey)`, but store the API key only through `KeychainCommandCredentialStore`. Do not display or echo a saved key after save; clear the text field after a successful save; show product-level failure text from `CommandIntelligenceFailure` on save/delete errors; keep the terminal usable and Settings dismissible when no key exists. Do not introduce OpenAI/local provider UI beyond protocol-ready future space, because D-06 locks Anthropic as the first live hosted provider and deferred ideas exclude broader provider/plugin surfaces.</action>
  <acceptance_criteria>
    - `Sources/GridOSApp/SettingsView.swift` contains `CommandIntelligenceSettingsView`
    - `Sources/GridOSApp/CommandIntelligenceSettingsView.swift` contains `Command Intelligence`
    - `Sources/GridOSApp/CommandIntelligenceSettingsView.swift` contains `Provider not configured`
    - `Sources/GridOSApp/CommandIntelligenceSettingsView.swift` contains `Provider configured`
    - `Sources/GridOSApp/CommandIntelligenceSettingsView.swift` contains `Anthropic API key`
    - `Sources/GridOSApp/CommandIntelligenceSettingsView.swift` contains `Save Provider Key`
    - `Sources/GridOSApp/CommandIntelligenceSettingsView.swift` contains `Remove Provider Key`
    - `Sources/GridOSApp/CommandIntelligenceSettingsView.swift` contains `Open Command Intelligence Settings`
    - `Sources/GridOSApp/CommandIntelligenceSettingsView.swift` contains `KeychainCommandCredentialStore`
    - `Sources/GridOSApp/CommandIntelligenceSettingsView.swift` contains `commandIntelligenceProviderStorageKey`
    - `Sources/GridOSApp/CommandIntelligenceSettingsView.swift` contains `commandIntelligenceModelStorageKey`
    - `rg "OpenAI|LocalProvider|plugin|marketplace" Sources/GridOSApp/CommandIntelligenceSettingsView.swift` returns no matches
    - `! rg "apiKey.*AppStorage|UserDefaults.*api|commandIntelligence\\.apiKey" Sources/GridOSApp Sources/GridOSKit`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodegen generate --use-cache &amp;&amp; xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg "Command Intelligence|Provider not configured|Provider configured|Anthropic API key|Save Provider Key|Remove Provider Key|KeychainCommandCredentialStore" Sources/GridOSApp Sources/CommandIntelligence &amp;&amp; ! rg "apiKey.*AppStorage|UserDefaults.*api|commandIntelligence\\.apiKey" Sources/GridOSApp Sources/GridOSKit Sources/CommandIntelligence</automated>
  </verify>
  <done>Settings has an opt-in provider setup surface with Keychain-backed key save/delete and a normal no-key state.</done>
</task>

</tasks>

<verification>
Run the hosted-provider/keychain gate:

```bash
xcodegen generate --use-cache
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
rg "AnthropicCommandProvider|POST|/v1/messages|x-api-key|anthropic-version|2023-06-01|claude-sonnet-4-6" Sources/CommandIntelligence Tests/CommandIntelligenceTests
rg "CommandCredentialStore|KeychainCommandCredentialStore|kSecClassGenericPassword|kSecAttrAccessibleWhenUnlockedThisDeviceOnly|com\\.aaldere1\\.gridos\\.command-intelligence" Sources/CommandIntelligence Tests/CommandIntelligenceTests
rg "Command Intelligence|Provider not configured|Provider configured|Anthropic API key|Save Provider Key|Remove Provider Key" Sources/GridOSApp
! rg "apiKey.*AppStorage|UserDefaults.*api|anthropic.*AppStorage|commandIntelligence\\.apiKey|prompt.*UserDefaults|generated.*UserDefaults" Sources Tests
git diff --check
```
</verification>

<success_criteria>
- Anthropic provider adapter is covered by mocked network tests and uses the required endpoint/header contract.
- Keychain store uses the exact service, class, accessibility, and account rules.
- Settings can save/remove provider keys without storing or echoing secrets.
- App remains normal when no API key is configured.
</success_criteria>

<output>
After completion, create `.planning/phases/06-llm-command-palette/06-04-SUMMARY.md`.
</output>
