---
phase: 07-multi-pane-session-management
plan: 03
title: Multi-pane workspace UI and native commands
status: pending
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - gridOS.xcodeproj/project.pbxproj
  - Sources/GridOSApp/GridOSApp.swift
  - Sources/GridOSApp/RootView.swift
  - Sources/GridOSApp/TerminalWorkspaceView.swift
  - Sources/GridOSApp/TerminalWorkspaceCommands.swift
  - Sources/GridOSApp/CommandPaletteView.swift
autonomous: true
requirements: ["PHASE-07"]
requirements_addressed: ["D-01", "D-04", "D-05", "D-06", "D-07", "D-08", "D-09", "D-10", "D-11", "D-21", "D-22", "D-23", "D-24", "D-26", "D-27"]
must_haves:
  truths:
    - "RootView composes a multi-pane terminal workspace instead of one TerminalSurface."
    - "Native menu commands expose split right/down, duplicate, close, focus next/previous, and resize active pane."
    - "Existing copy/paste/clear/reset and Command Intelligence closures target the active pane only."
    - "GridOSApp still does not import SwiftTerm."
  artifacts:
    - path: "Sources/GridOSApp/TerminalWorkspaceView.swift"
      provides: "Recursive split-pane SwiftUI workspace"
      contains: "TerminalWorkspaceView"
    - path: "Sources/GridOSApp/TerminalWorkspaceCommands.swift"
      provides: "Focused command bridge for native menus"
      contains: "TerminalWorkspaceCommandsValue"
    - path: "Sources/GridOSApp/GridOSApp.swift"
      provides: "Native multi-pane command menu and shortcuts"
      contains: "Split Right"
    - path: "Sources/GridOSApp/RootView.swift"
      provides: "Active-pane Command Intelligence closures and pane activity routing"
      contains: "insertInActivePane"
  key_links:
    - from: "Sources/GridOSApp/CommandPaletteView.swift"
      to: "Sources/TerminalCore/TerminalWorkspaceController.swift"
      via: "RootView closures call active-pane insert/run/selected text"
      pattern: "insertInActivePane"
---

<objective>
Replace the single terminal workspace with a readable native multi-pane workspace and native pane commands.

Purpose: Users need practical split-pane workflows while preserving terminal-first focus, Command Intelligence safety, and current app-frame layout.
Output: SwiftUI workspace composition, active-pane visual indication, focused command menu bridge, and active-pane Command Intelligence wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-multi-pane-session-management/07-CONTEXT.md
@.planning/phases/07-multi-pane-session-management/07-RESEARCH.md
@.planning/phases/07-multi-pane-session-management/07-VALIDATION.md
@.planning/phases/07-multi-pane-session-management/07-UI-SPEC.md
@docs/architecture.md
@Sources/GridOSApp/GridOSApp.swift
@Sources/GridOSApp/RootView.swift
@Sources/GridOSApp/CommandPaletteView.swift
@Sources/TerminalCore/TerminalWorkspaceModel.swift
@Sources/TerminalCore/TerminalWorkspaceController.swift
@Sources/TerminalCore/TerminalSurface.swift
</context>

<tasks>

<task id="07-03-01" type="auto">
  <title>Compose recursive split-pane TerminalWorkspaceView</title>
  <name>Compose recursive split-pane TerminalWorkspaceView</name>
  <files>Sources/GridOSApp/RootView.swift, Sources/GridOSApp/TerminalWorkspaceView.swift</files>
  <read_first>.planning/phases/07-multi-pane-session-management/07-UI-SPEC.md, Sources/GridOSApp/RootView.swift, Sources/TerminalCore/TerminalWorkspaceModel.swift, Sources/TerminalCore/TerminalWorkspaceController.swift, Sources/TerminalCore/TerminalSurface.swift, Sources/RenderCore/VisualTheme.swift</read_first>
  <action>Move the current private single-terminal `TerminalWorkspaceView` out of `RootView.swift` into a new `Sources/GridOSApp/TerminalWorkspaceView.swift` and replace it with a multi-pane implementation. The new public-to-file SwiftUI view must be `struct TerminalWorkspaceView: View` with inputs `workspaceController: TerminalWorkspaceController`, `theme: VisualTheme`, `onActivity: TerminalSurface.ActivityHandler`, and closures for pane command requests if needed. It must recursively render `TerminalPaneLayout.pane` as a pane view containing `TerminalSurface(paneID: paneID, configuration: descriptor.configuration, interactionController: workspaceController.controller(for: paneID), onActivity: onActivity)`. It must render `.split(axis: .horizontal, ...)` with `HSplitView` and `.split(axis: .vertical, ...)` with `VSplitView`, or equivalent native split views. Each pane must activate itself on click/focus by calling `workspaceController.activatePane(paneID)` and visually indicate active pane with a subtle `RoundedRectangle` or edge stroke using `theme.palette.primaryAccent`; inactive panes use `theme.panel.borderOpacity`. Each pane needs `.accessibilityLabel("Terminal pane")` and `.accessibilityValue("Active pane")` for the active pane. RootView must replace `@StateObject private var terminalInteractionController` with `@StateObject private var workspaceController = TerminalWorkspaceController(state: TerminalWorkspaceState(defaultConfiguration: TerminalSessionConfiguration.fromProcessArguments()))` or equivalent initialization, pass `workspaceController` to `TerminalWorkspaceView`, and handle terminal activity with both `paneID` and `TerminalActivityEvent`. Remove the old single `currentWorkingDirectory` state and use `workspaceController.state.panesByID[workspaceController.activePaneID]?.lastWorkingDirectory` for active pane context.</action>
  <acceptance_criteria>
    - `Sources/GridOSApp/TerminalWorkspaceView.swift` contains `struct TerminalWorkspaceView: View`
    - `Sources/GridOSApp/TerminalWorkspaceView.swift` contains `TerminalPaneLayout`
    - `Sources/GridOSApp/TerminalWorkspaceView.swift` contains `HSplitView`
    - `Sources/GridOSApp/TerminalWorkspaceView.swift` contains `VSplitView`
    - `Sources/GridOSApp/TerminalWorkspaceView.swift` contains `TerminalSurface(paneID:`
    - `Sources/GridOSApp/TerminalWorkspaceView.swift` contains `activatePane`
    - `Sources/GridOSApp/TerminalWorkspaceView.swift` contains `Active pane`
    - `Sources/GridOSApp/RootView.swift` contains `TerminalWorkspaceController`
    - `Sources/GridOSApp/RootView.swift` contains `handleTerminalActivity(_ activity: TerminalActivityEvent, from paneID: TerminalPaneID)`
    - `Sources/GridOSApp/RootView.swift` contains `workspaceController.handleActivity`
    - `! rg 'currentWorkingDirectory' Sources/GridOSApp/RootView.swift`
    - `! rg 'import SwiftTerm' Sources/GridOSApp`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodegen generate --use-cache &amp;&amp; xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg 'TerminalWorkspaceView|TerminalPaneLayout|HSplitView|VSplitView|TerminalSurface\\(paneID:|activatePane|Active pane|workspaceController.handleActivity' Sources/GridOSApp Sources/TerminalCore &amp;&amp; ! rg 'currentWorkingDirectory|import SwiftTerm' Sources/GridOSApp</automated>
  </verify>
  <done>RootView renders a real multi-pane terminal workspace with active-pane indication and pane-scoped activity.</done>
</task>

<task id="07-03-02" type="auto">
  <title>Add native pane commands and active-pane Command Intelligence wiring</title>
  <name>Add native pane commands and active-pane Command Intelligence wiring</name>
  <files>Sources/GridOSApp/GridOSApp.swift, Sources/GridOSApp/TerminalWorkspaceCommands.swift, Sources/GridOSApp/RootView.swift, Sources/GridOSApp/CommandPaletteView.swift</files>
  <read_first>.planning/phases/07-multi-pane-session-management/07-UI-SPEC.md, Sources/GridOSApp/GridOSApp.swift, Sources/GridOSApp/RootView.swift, Sources/GridOSApp/CommandPaletteView.swift, Sources/TerminalCore/TerminalWorkspaceController.swift</read_first>
  <action>Create `Sources/GridOSApp/TerminalWorkspaceCommands.swift` with `struct TerminalWorkspaceCommandsValue` containing closures `splitRight`, `splitDown`, `duplicatePane`, `closePane`, `focusNextPane`, `focusPreviousPane`, `resizePaneLeft`, `resizePaneRight`, `resizePaneUp`, `resizePaneDown`, `copy`, `paste`, `clear`, and `reset`. Add a `FocusedValues` key named `terminalWorkspaceCommands`. In `TerminalWorkspaceView`, attach `.focusedValue(\.terminalWorkspaceCommands, TerminalWorkspaceCommandsValue(...))` so commands target the active workspace. Refactor `GridOSApp.swift` `TerminalCommands` to use `@FocusedValue(\.terminalWorkspaceCommands) private var terminalWorkspaceCommands`. Add menu items with exact labels and shortcuts: `Split Right` `.keyboardShortcut("d", modifiers: [.command])`, `Split Down` `.keyboardShortcut("d", modifiers: [.command, .shift])`, `Duplicate Pane` `.keyboardShortcut("d", modifiers: [.command, .option])`, `Close Pane` `.keyboardShortcut("w", modifiers: [.command])`, `Focus Next Pane` `.keyboardShortcut("]", modifiers: [.command])`, `Focus Previous Pane` `.keyboardShortcut("[", modifiers: [.command])`, `Resize Pane Left` `.keyboardShortcut(.leftArrow, modifiers: [.command, .control])`, `Resize Pane Right` `.keyboardShortcut(.rightArrow, modifiers: [.command, .control])`, `Resize Pane Up` `.keyboardShortcut(.upArrow, modifiers: [.command, .control])`, and `Resize Pane Down` `.keyboardShortcut(.downArrow, modifiers: [.command, .control])`. Existing `Copy`, `Paste`, `Clear`, and `Reset` buttons must call focused command closures instead of `TerminalCommandCenter` static notification methods. In `RootView`, update Command Intelligence closures so `selectedTextProvider`, `workingDirectoryProvider`, `onInsertCommand`, `onRunCommand`, and palette dismissal use `workspaceController.selectedTextInActivePane()`, active pane last directory, `insertInActivePane`, `runInActivePane`, and `focusActivePane()`. If the active pane changes while the palette is open, capture `commandPalettePaneID` on open and route that palette session to the captured pane if `TerminalWorkspaceController` exposes pane-specific APIs; otherwise document and source-comment that Phase 7 uses current active pane at action time. Do not import SwiftTerm in GridOSApp.</action>
  <acceptance_criteria>
    - `Sources/GridOSApp/TerminalWorkspaceCommands.swift` contains `TerminalWorkspaceCommandsValue`
    - `Sources/GridOSApp/TerminalWorkspaceCommands.swift` contains `terminalWorkspaceCommands`
    - `Sources/GridOSApp/GridOSApp.swift` contains `@FocusedValue`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Split Right`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Split Down`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Duplicate Pane`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Close Pane`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Focus Next Pane`
    - `Sources/GridOSApp/GridOSApp.swift` contains `Focus Previous Pane`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.keyboardShortcut("d", modifiers: [.command])`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.keyboardShortcut("d", modifiers: [.command, .shift])`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.keyboardShortcut("d", modifiers: [.command, .option])`
    - `Sources/GridOSApp/GridOSApp.swift` contains `.keyboardShortcut("w", modifiers: [.command])`
    - `Sources/GridOSApp/RootView.swift` contains `selectedTextInActivePane`
    - `Sources/GridOSApp/RootView.swift` contains `insertInActivePane`
    - `Sources/GridOSApp/RootView.swift` contains `runInActivePane`
    - `Sources/GridOSApp/RootView.swift` contains `focusActivePane`
    - `! rg 'TerminalCommandCenter\\.copy|TerminalCommandCenter\\.paste|TerminalCommandCenter\\.clear|TerminalCommandCenter\\.reset' Sources/GridOSApp/GridOSApp.swift`
    - `! rg 'import SwiftTerm' Sources/GridOSApp`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg 'Split Right|Split Down|Duplicate Pane|Close Pane|Focus Next Pane|Focus Previous Pane|TerminalWorkspaceCommandsValue|@FocusedValue|selectedTextInActivePane|insertInActivePane|runInActivePane|focusActivePane' Sources/GridOSApp Sources/TerminalCore &amp;&amp; ! rg 'TerminalCommandCenter\\.copy|TerminalCommandCenter\\.paste|TerminalCommandCenter\\.clear|TerminalCommandCenter\\.reset|import SwiftTerm' Sources/GridOSApp</automated>
  </verify>
  <done>Native app commands and Command Intelligence target the active pane through focused workspace command routing.</done>
</task>

</tasks>

<verification>
Run the UI and active-command gate:

```bash
xcodegen generate --use-cache
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
rg 'TerminalWorkspaceView|TerminalPaneLayout|HSplitView|VSplitView|Active pane' Sources/GridOSApp
rg 'Split Right|Split Down|Duplicate Pane|Close Pane|Focus Next Pane|Focus Previous Pane|TerminalWorkspaceCommandsValue|@FocusedValue' Sources/GridOSApp
rg 'selectedTextInActivePane|insertInActivePane|runInActivePane|focusActivePane' Sources/GridOSApp Sources/TerminalCore
! rg 'TerminalCommandCenter\.copy|TerminalCommandCenter\.paste|TerminalCommandCenter\.clear|TerminalCommandCenter\.reset|import SwiftTerm' Sources/GridOSApp
git diff --check
```
</verification>

<success_criteria>
- Multi-pane workspace replaces the single terminal surface.
- Active pane is visible and accessible.
- Native pane commands are available and route to active pane only.
- Command Intelligence still works through active-pane terminal closures.
</success_criteria>

<output>
After completion, create `.planning/phases/07-multi-pane-session-management/07-03-SUMMARY.md`.
</output>

