---
phase: 07-multi-pane-session-management
plan: 01
title: TerminalCore pane and session model foundation
status: pending
type: execute
wave: 1
depends_on: []
files_modified:
  - gridOS.xcodeproj/project.pbxproj
  - Sources/TerminalCore/TerminalWorkspaceModel.swift
  - Tests/TerminalCoreTests/TerminalWorkspaceModelTests.swift
autonomous: true
requirements: ["PHASE-07"]
requirements_addressed: ["D-01", "D-02", "D-03", "D-12", "D-13", "D-14", "D-15", "D-16", "D-25"]
must_haves:
  truths:
    - "TerminalCore exposes a pure Codable multi-pane workspace model before UI work begins."
    - "The model supports split right/down, close pane, duplicate pane, focus next/previous, working-directory updates, and readable split fraction clamping."
    - "Restorable workspace snapshots contain layout, pane descriptors, active pane, and recent directories, but no process IDs."
  artifacts:
    - path: "Sources/TerminalCore/TerminalWorkspaceModel.swift"
      provides: "Pane identity, layout, descriptor, snapshot, recent-directory, and pure workspace state operations"
      contains: "TerminalWorkspaceState"
    - path: "Tests/TerminalCoreTests/TerminalWorkspaceModelTests.swift"
      provides: "Regression coverage for split/close/duplicate/focus/persistence model behavior"
      contains: "testSplitRightCreatesSecondPane"
  key_links:
    - from: "Sources/TerminalCore/TerminalWorkspaceModel.swift"
      to: "Sources/TerminalCore/TerminalSessionConfiguration.swift"
      via: "Each pane descriptor carries a TerminalSessionConfiguration"
      pattern: "TerminalSessionConfiguration"
---

<objective>
Create the pure TerminalCore model foundation for Phase 7 multi-pane sessions.

Purpose: Later UI, routing, process lifecycle, and persistence work need a tested pane/session model instead of ad-hoc arrays in `RootView`.
Output: `TerminalWorkspaceModel.swift` plus XCTest coverage for layout, active pane, duplicate, close, working directory, snapshot, and recent-directory behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-multi-pane-session-management/07-CONTEXT.md
@.planning/phases/07-multi-pane-session-management/07-RESEARCH.md
@.planning/phases/07-multi-pane-session-management/07-VALIDATION.md
@docs/architecture.md
@Sources/TerminalCore/TerminalSessionConfiguration.swift
@Sources/TerminalCore/TerminalSessionState.swift
@Tests/TerminalCoreTests/TerminalSessionConfigurationTests.swift

<interfaces>
Required public model names:

```swift
public struct TerminalPaneID: RawRepresentable, Codable, Hashable, Sendable
public enum TerminalSplitAxis: String, Codable, Equatable, Sendable
public indirect enum TerminalPaneLayout: Codable, Equatable, Sendable
public struct TerminalPaneDescriptor: Codable, Equatable, Identifiable, Sendable
public struct TerminalWorkspaceSnapshot: Codable, Equatable, Sendable
public struct TerminalWorkspaceState: Equatable, Sendable
```
</interfaces>
</context>

<tasks>

<task id="07-01-01" type="auto" tdd="true">
  <title>Add pure TerminalCore workspace model and tests</title>
  <name>Add pure TerminalCore workspace model and tests</name>
  <files>gridOS.xcodeproj/project.pbxproj, Sources/TerminalCore/TerminalWorkspaceModel.swift, Tests/TerminalCoreTests/TerminalWorkspaceModelTests.swift</files>
  <read_first>.planning/phases/07-multi-pane-session-management/07-CONTEXT.md, .planning/phases/07-multi-pane-session-management/07-RESEARCH.md, .planning/phases/07-multi-pane-session-management/07-VALIDATION.md, Sources/TerminalCore/TerminalSessionConfiguration.swift, Sources/TerminalCore/TerminalSessionState.swift, Tests/TerminalCoreTests/TerminalSessionConfigurationTests.swift, project.yml</read_first>
  <action>Create `Sources/TerminalCore/TerminalWorkspaceModel.swift`. Add `TerminalPaneID` as `RawRepresentable`, `Codable`, `Hashable`, `Sendable`, `ExpressibleByStringLiteral`, with `rawValue: String`, `init(rawValue:)`, and `static func generated() -> TerminalPaneID` returning a lowercase UUID string. Add `TerminalSplitAxis` cases `horizontal` and `vertical`; document `horizontal` as left/right and `vertical` as top/bottom. Add indirect `TerminalPaneLayout` cases `pane(TerminalPaneID)` and `split(axis: TerminalSplitAxis, fraction: Double, first: TerminalPaneLayout, second: TerminalPaneLayout)`. Add helpers `paneIDsInVisualOrder`, `contains(_:)`, `replacingPane(_:with:)`, `removingPane(_:)`, and `clampedFraction(_:)` with exact clamp range `0.20...0.80`. Add `TerminalPaneDescriptor` with fields `id`, `configuration: TerminalSessionConfiguration`, `title: String?`, `lastWorkingDirectory: String?`, and `isRestored: Bool`; do not add `pid`, `processID`, `shellPid`, or `childfd`. Add `TerminalWorkspaceSnapshot` with fields `schemaVersion: Int`, `layout`, `panes`, `activePaneID`, and `recentDirectories`, with `static let currentSchemaVersion = 1`. Add `TerminalWorkspaceState` with `layout`, `panesByID`, `activePaneID`, `recentDirectories`, `init(defaultConfiguration:)`, `init(snapshot:fallbackConfiguration:directoryExists:)`, `snapshot()`, `splitActivePane(axis:newPaneID:)`, `duplicateActivePane(newPaneID:)`, `closeActivePane()`, `focusNextPane()`, `focusPreviousPane()`, `updateWorkingDirectory(_:for:)`, and `recordRecentDirectory(_:)`. New panes and duplicates must start from the active pane configuration and last known working directory when present. Missing restored directories must fall back to `fallbackConfiguration.workingDirectory` and clear that pane's invalid `lastWorkingDirectory`. Recent directories must trim whitespace, ignore empty values, dedupe by moving the newest path to the front, and cap at 10 entries. Add `Tests/TerminalCoreTests/TerminalWorkspaceModelTests.swift` with XCTest cases named exactly `testDefaultWorkspaceStartsWithOnePane`, `testSplitRightCreatesSecondPane`, `testSplitDownCreatesVerticalSplit`, `testClosePanePromotesSiblingAndKeepsOnePane`, `testDuplicatePaneCopiesConfigurationWithNewIdentity`, `testFocusNextAndPreviousFollowVisualOrder`, `testSplitFractionsAreClamped`, `testSnapshotRoundTripsWithoutProcessIdentifiers`, `testMissingRestoredDirectoryFallsBackToDefault`, and `testRecentDirectoriesNormalizeDedupeAndCapAtTen`. Run `xcodegen generate --use-cache` so the generated project includes the new files.</action>
  <acceptance_criteria>
    - `Sources/TerminalCore/TerminalWorkspaceModel.swift` contains `public struct TerminalPaneID`
    - `Sources/TerminalCore/TerminalWorkspaceModel.swift` contains `public enum TerminalSplitAxis`
    - `Sources/TerminalCore/TerminalWorkspaceModel.swift` contains `public indirect enum TerminalPaneLayout`
    - `Sources/TerminalCore/TerminalWorkspaceModel.swift` contains `public struct TerminalPaneDescriptor`
    - `Sources/TerminalCore/TerminalWorkspaceModel.swift` contains `public struct TerminalWorkspaceSnapshot`
    - `Sources/TerminalCore/TerminalWorkspaceModel.swift` contains `public struct TerminalWorkspaceState`
    - `Sources/TerminalCore/TerminalWorkspaceModel.swift` contains `currentSchemaVersion = 1`
    - `Sources/TerminalCore/TerminalWorkspaceModel.swift` contains `0.20`
    - `Sources/TerminalCore/TerminalWorkspaceModel.swift` contains `0.80`
    - `Sources/TerminalCore/TerminalWorkspaceModel.swift` contains `recordRecentDirectory`
    - `Tests/TerminalCoreTests/TerminalWorkspaceModelTests.swift` contains `testSplitRightCreatesSecondPane`
    - `Tests/TerminalCoreTests/TerminalWorkspaceModelTests.swift` contains `testSnapshotRoundTripsWithoutProcessIdentifiers`
    - `Tests/TerminalCoreTests/TerminalWorkspaceModelTests.swift` contains `testMissingRestoredDirectoryFallsBackToDefault`
    - `Tests/TerminalCoreTests/TerminalWorkspaceModelTests.swift` contains `testRecentDirectoriesNormalizeDedupeAndCapAtTen`
    - `! rg 'shellPid|processID|childfd|pid' Sources/TerminalCore/TerminalWorkspaceModel.swift`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodegen generate --use-cache &amp;&amp; xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:TerminalCoreTests/TerminalWorkspaceModelTests &amp;&amp; rg 'TerminalPaneID|TerminalSplitAxis|TerminalPaneLayout|TerminalPaneDescriptor|TerminalWorkspaceSnapshot|TerminalWorkspaceState|recordRecentDirectory' Sources/TerminalCore Tests/TerminalCoreTests &amp;&amp; ! rg 'shellPid|processID|childfd|pid' Sources/TerminalCore/TerminalWorkspaceModel.swift</automated>
  </verify>
  <done>TerminalCore has a tested pure multi-pane workspace model with no process resurrection state.</done>
</task>

</tasks>

<verification>
Run the model foundation gate:

```bash
xcodegen generate --use-cache
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
rg 'TerminalPaneID|TerminalSplitAxis|TerminalPaneLayout|TerminalPaneDescriptor|TerminalWorkspaceSnapshot|TerminalWorkspaceState' Sources/TerminalCore Tests/TerminalCoreTests
! rg 'shellPid|processID|childfd|pid' Sources/TerminalCore/TerminalWorkspaceModel.swift
git diff --check
```
</verification>

<success_criteria>
- Pane/session model is pure, Codable, Equatable, Sendable, and test-covered.
- Split, close, duplicate, focus, recent directories, and snapshot restore are deterministic.
- Restorable session state contains no process IDs or live-process resurrection data.
</success_criteria>

<output>
After completion, create `.planning/phases/07-multi-pane-session-management/07-01-SUMMARY.md`.
</output>

