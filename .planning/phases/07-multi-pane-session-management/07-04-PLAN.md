---
phase: 07-multi-pane-session-management
plan: 04
title: Session restore, recent directories, and recovery copy
status: pending
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - gridOS.xcodeproj/project.pbxproj
  - Sources/TerminalCore/TerminalWorkspacePersistence.swift
  - Sources/GridOSApp/RootView.swift
  - Sources/GridOSApp/SettingsView.swift
  - Tests/TerminalCoreTests/TerminalWorkspacePersistenceTests.swift
  - docs/architecture.md
  - docs/release.md
autonomous: true
requirements: ["PHASE-07"]
requirements_addressed: ["D-12", "D-13", "D-14", "D-15", "D-16", "D-20", "D-21", "D-24", "D-25", "D-26"]
must_haves:
  truths:
    - "Session layout and recent directories persist locally in app support JSON."
    - "Relaunch restores layout/directories with fresh shell processes and falls back for missing directories."
    - "No shell history, command output, environment variables, or process IDs are persisted."
    - "Settings recovery copy explicitly says running shell processes are not restored after relaunch."
  artifacts:
    - path: "Sources/TerminalCore/TerminalWorkspacePersistence.swift"
      provides: "Local app-support snapshot and recent-directory persistence"
      contains: "session-v1.json"
    - path: "Tests/TerminalCoreTests/TerminalWorkspacePersistenceTests.swift"
      provides: "Persistence round-trip, corrupt-file, missing-directory, and privacy regression tests"
      contains: "testSnapshotStoreRoundTripsWorkspace"
    - path: "Sources/GridOSApp/SettingsView.swift"
      provides: "Recovery copy for pane/session restore"
      contains: "Running shell processes are not restored after relaunch."
  key_links:
    - from: "Sources/GridOSApp/RootView.swift"
      to: "Sources/TerminalCore/TerminalWorkspacePersistence.swift"
      via: "RootView loads and saves TerminalWorkspaceSnapshot through TerminalCore persistence"
      pattern: "TerminalWorkspaceSnapshotStore"
---

<objective>
Persist and restore the Phase 7 workspace honestly: layout and directories survive relaunch, running shell processes do not.

Purpose: The app should feel session-aware without pretending to resurrect live processes or collecting private shell data.
Output: TerminalCore persistence layer, RootView load/save wiring, recent-directory behavior, Settings recovery copy, docs updates, and persistence tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-multi-pane-session-management/07-CONTEXT.md
@.planning/phases/07-multi-pane-session-management/07-RESEARCH.md
@.planning/phases/07-multi-pane-session-management/07-VALIDATION.md
@.planning/phases/07-multi-pane-session-management/07-UI-SPEC.md
@docs/architecture.md
@docs/release.md
@Sources/TerminalCore/TerminalWorkspaceModel.swift
@Sources/TerminalCore/TerminalWorkspaceController.swift
@Sources/GridOSApp/RootView.swift
@Sources/GridOSApp/SettingsView.swift
</context>

<tasks>

<task id="07-04-01" type="auto" tdd="true">
  <title>Add local workspace snapshot persistence</title>
  <name>Add local workspace snapshot persistence</name>
  <files>Sources/TerminalCore/TerminalWorkspacePersistence.swift, Tests/TerminalCoreTests/TerminalWorkspacePersistenceTests.swift</files>
  <read_first>Sources/TerminalCore/TerminalWorkspaceModel.swift, Sources/TerminalCore/TerminalWorkspaceController.swift, Tests/TerminalCoreTests/TerminalWorkspaceModelTests.swift, .planning/phases/07-multi-pane-session-management/07-CONTEXT.md</read_first>
  <action>Create `Sources/TerminalCore/TerminalWorkspacePersistence.swift` with `public struct TerminalWorkspaceSnapshotStore: Sendable`. Its initializer must accept `baseDirectory: URL` for tests and `fileManager: FileManager = .default`; add `public static func defaultBaseDirectory(fileManager:) -> URL` that resolves the user Application Support directory and appends `gridOS`. Use exact filenames `session-v1.json` and `recent-directories-v1.json`. Add methods `loadSnapshot(fallbackConfiguration:directoryExists:) throws -> TerminalWorkspaceSnapshot?`, `saveSnapshot(_:) throws`, `loadRecentDirectories() throws -> [String]`, `saveRecentDirectories(_:) throws`, and `deleteStoredSession() throws`. Use `JSONEncoder` and `JSONDecoder`, create the app support directory before writes, write atomically, and treat missing files as nil/empty rather than errors. Corrupt JSON must not crash the app: implement `TerminalWorkspacePersistenceError.unreadableSnapshot` and make `loadSnapshot` catch decode failures, move the corrupt file aside to `session-v1.corrupt.json`, and return nil. Add `Tests/TerminalCoreTests/TerminalWorkspacePersistenceTests.swift` using a temporary directory with tests named exactly `testSnapshotStoreRoundTripsWorkspace`, `testMissingSnapshotReturnsNil`, `testRecentDirectoriesRoundTrip`, `testDeleteStoredSessionRemovesFiles`, `testCorruptSnapshotDoesNotCrash`, and `testPersistenceDoesNotEncodeProcessIdentifiers`. The encoded JSON must not contain `shellPid`, `processID`, `childfd`, `commandOutput`, `shellHistory`, or `environment`.</action>
  <acceptance_criteria>
    - `Sources/TerminalCore/TerminalWorkspacePersistence.swift` contains `public struct TerminalWorkspaceSnapshotStore`
    - `Sources/TerminalCore/TerminalWorkspacePersistence.swift` contains `session-v1.json`
    - `Sources/TerminalCore/TerminalWorkspacePersistence.swift` contains `recent-directories-v1.json`
    - `Sources/TerminalCore/TerminalWorkspacePersistence.swift` contains `Application Support`
    - `Sources/TerminalCore/TerminalWorkspacePersistence.swift` contains `saveSnapshot`
    - `Sources/TerminalCore/TerminalWorkspacePersistence.swift` contains `loadSnapshot`
    - `Sources/TerminalCore/TerminalWorkspacePersistence.swift` contains `deleteStoredSession`
    - `Tests/TerminalCoreTests/TerminalWorkspacePersistenceTests.swift` contains `testSnapshotStoreRoundTripsWorkspace`
    - `Tests/TerminalCoreTests/TerminalWorkspacePersistenceTests.swift` contains `testCorruptSnapshotDoesNotCrash`
    - `Tests/TerminalCoreTests/TerminalWorkspacePersistenceTests.swift` contains `testPersistenceDoesNotEncodeProcessIdentifiers`
    - `! rg 'shellPid|processID|childfd|commandOutput|shellHistory|environment' Sources/TerminalCore/TerminalWorkspacePersistence.swift Tests/TerminalCoreTests/TerminalWorkspacePersistenceTests.swift`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO test -only-testing:TerminalCoreTests/TerminalWorkspacePersistenceTests &amp;&amp; rg 'TerminalWorkspaceSnapshotStore|session-v1.json|recent-directories-v1.json|Application Support|saveSnapshot|loadSnapshot|deleteStoredSession' Sources/TerminalCore Tests/TerminalCoreTests &amp;&amp; ! rg 'shellPid|processID|childfd|commandOutput|shellHistory|environment' Sources/TerminalCore/TerminalWorkspacePersistence.swift Tests/TerminalCoreTests/TerminalWorkspacePersistenceTests.swift</automated>
  </verify>
  <done>TerminalCore can persist and restore workspace snapshots and recent directories without private shell data.</done>
</task>

<task id="07-04-02" type="auto">
  <title>Wire restore, recent directories, Settings copy, and docs</title>
  <name>Wire restore, recent directories, Settings copy, and docs</name>
  <files>Sources/GridOSApp/RootView.swift, Sources/GridOSApp/SettingsView.swift, docs/architecture.md, docs/release.md</files>
  <read_first>Sources/GridOSApp/RootView.swift, Sources/GridOSApp/SettingsView.swift, Sources/TerminalCore/TerminalWorkspacePersistence.swift, Sources/TerminalCore/TerminalWorkspaceController.swift, docs/architecture.md, docs/release.md, .planning/phases/07-multi-pane-session-management/07-UI-SPEC.md</read_first>
  <action>In `RootView`, initialize `TerminalWorkspaceController` from `TerminalWorkspaceSnapshotStore.loadSnapshot(fallbackConfiguration:directoryExists:)` when available; otherwise use `TerminalWorkspaceState(defaultConfiguration:)`. On terminal working-directory activity, use `workspaceController.handleActivity(_:from:)` and schedule a debounced save of `workspaceController.snapshot()` to `TerminalWorkspaceSnapshotStore` after a short delay such as `0.5` seconds. Also save after split, duplicate, close, focus, and app scene/task lifecycle events where the workspace changes. The restored shell processes must be fresh: do not store or reuse any prior PID. In `SettingsView` Recovery section, replace the old single sentence with exact copy `Pane layout and directories are restored on relaunch.`, `Running shell processes are not restored after relaunch.`, and `Directory unavailable. Starting in your default directory.` Add a `Reset Saved Session` button if straightforward; it must call a RootView/app notification or persistence helper to delete `session-v1.json` and `recent-directories-v1.json`, but do not block plan completion if Settings-to-Root reset routing would require disproportionate scope. Update `docs/architecture.md` with a `Phase 7 architecture target` section naming `TerminalPaneID`, `TerminalPaneLayout`, `TerminalWorkspaceState`, `TerminalWorkspaceController`, and `TerminalWorkspaceSnapshotStore`. Update `docs/release.md` with a `Phase 7 multi-pane/session smoke` section listing build/test/source gates and the manual smoke steps for split, active-pane targeting, close cleanup, restore, and no orphan shell processes.</action>
  <acceptance_criteria>
    - `Sources/GridOSApp/RootView.swift` contains `TerminalWorkspaceSnapshotStore`
    - `Sources/GridOSApp/RootView.swift` contains `loadSnapshot`
    - `Sources/GridOSApp/RootView.swift` contains `saveSnapshot`
    - `Sources/GridOSApp/RootView.swift` contains `0.5`
    - `Sources/GridOSApp/SettingsView.swift` contains `Pane layout and directories are restored on relaunch.`
    - `Sources/GridOSApp/SettingsView.swift` contains `Running shell processes are not restored after relaunch.`
    - `Sources/GridOSApp/SettingsView.swift` contains `Directory unavailable. Starting in your default directory.`
    - `docs/architecture.md` contains `Phase 7 architecture target`
    - `docs/architecture.md` contains `TerminalWorkspaceSnapshotStore`
    - `docs/release.md` contains `Phase 7 multi-pane/session smoke`
    - `docs/release.md` contains `no orphan`
    - `! rg 'shell history|environment variables|command output|UserDefaults.*history|UserDefaults.*output' Sources/GridOSApp Sources/TerminalCore Sources/GridOSKit`
    - `xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test &amp;&amp; rg 'TerminalWorkspaceSnapshotStore|loadSnapshot|saveSnapshot|Pane layout and directories are restored on relaunch.|Running shell processes are not restored after relaunch.|Phase 7 architecture target|Phase 7 multi-pane/session smoke|no orphan' Sources docs &amp;&amp; ! rg 'shell history|environment variables|command output|UserDefaults.*history|UserDefaults.*output' Sources/GridOSApp Sources/TerminalCore Sources/GridOSKit</automated>
  </verify>
  <done>The app restores pane layout/directories locally and explains recovery honestly.</done>
</task>

</tasks>

<verification>
Run the persistence/restore gate:

```bash
xcodegen generate --use-cache
xcodebuild -quiet -project gridOS.xcodeproj -scheme gridOS -destination 'platform=macOS,arch=arm64' CODE_SIGNING_ALLOWED=NO build test
rg 'TerminalWorkspaceSnapshotStore|session-v1.json|recent-directories-v1.json|Application Support|loadSnapshot|saveSnapshot' Sources/TerminalCore Sources/GridOSApp Tests/TerminalCoreTests
rg 'Pane layout and directories are restored on relaunch.|Running shell processes are not restored after relaunch.|Directory unavailable. Starting in your default directory.' Sources
rg 'Phase 7 architecture target|Phase 7 multi-pane/session smoke|no orphan' docs
! rg 'shell history|environment variables|command output|UserDefaults.*history|UserDefaults.*output|shellPid|processID|childfd' Sources/GridOSApp Sources/TerminalCore Sources/GridOSKit
git diff --check
```
</verification>

<success_criteria>
- Session layout and recent directories persist locally.
- Relaunch restores layout/directories with fresh shells only.
- Recovery copy is honest and source-visible.
- Docs describe the Phase 7 architecture and smoke path.
</success_criteria>

<output>
After completion, create `.planning/phases/07-multi-pane-session-management/07-04-SUMMARY.md`.
</output>
