import GridOSKit

public enum RenderCoreStatus {
    public static let module = FoundationModuleStatus(
        id: "render-core",
        title: "RenderCore",
        state: .scaffolded,
        detail: "Metal visual identity boundary reserved."
    )
}
